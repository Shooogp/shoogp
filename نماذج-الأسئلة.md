# نماذج الأسئلة — المرجع الموحّد لصيغة البيانات

> هذا الملف مرجع **صيغة البيانات** للأنواع **المنفّذة حالياً في الكود** فقط (المذكورة أدناه) — وهي ١٨ نوعاً مشتركاً وخاصاً بالعلوم (① – ⑱) + **١٣ نوعاً خاصاً بالرياضيات** (⑲ – ㉛) + **٥ أنواعٍ خاصةٍ باللغة العربية** (㉜ – ㊱). أمّا **كتالوج** أنواع الأسئلة (فئاتها وتنظيمها بحسب النطاق) فمرجعه `question-types.md` وهو الحَكَم في وصف الأنواع، و**القواعد الملزمة** (التوزيع، المستوى المعرفي، ربط الهدف، بناء الأنواع الجديدة، نطاق المواد) مرجعها `CLAUDE.md`.

كل أسئلة الدروس تُكتب في `js/questions.js` بالصيغة:

```js
window.QUESTIONS = {
  "معرّف-الدرس": [ /* مصفوفة أسئلة */ ]
};
```

- **المفتاح** = معرّف الدرس (`file`) من `data/index.json` — مثل `"g4s-1-2"`.
- **القيمة** = مصفوفة أسئلة، كل سؤال كائن فيه `type` و`objective` وحقول نوعه.
- الدرس بلا مدخل في `QUESTIONS` يعرض نشاطاً افتراضياً (تسمية أجزاء النبتة).

## الحقول المشتركة لكل سؤال
| الحقل | إلزامي | الوصف |
|------|:---:|------|
| `type` | ✅ | نوع السؤال (أحد الأنواع المنفّذة حالياً أدناه) |
| `objective` | ✅ | نصّ الهدف من وثيقة الأهداف (يُسجَّل مع السؤال) |
| `level` | ✅ | المستوى المعرفي للسؤال — إحدى القيم المشتركة: `"knowledge"` (معرفة) أو `"application"` (تطبيق) أو `"reasoning"` (استدلال)، **وقيمة رابعة `"evaluation"` (تقويم) في اللغة العربية «أحب لغتي» وحدَها** (ممنوعة في العلوم والرياضيات). خاصية للسؤال الفرديّ لا للنوع؛ والتوزيع الإلزامي للأسئلة الأساسية الخمسة: العلوم ٣ معرفة + ١ تطبيق + ١ استدلال، والرياضيات ٢ معرفة + ٢ تطبيق + ١ استدلال، واللغة العربية ١ `knowledge` + ١ `reasoning` + ٢ `application` + ١ `evaluation` (المرجع `CLAUDE.md`) |
| `prompt` | غالباً | نصّ السؤال المعروض (في «صواب وخطأ» يُستخدم `statement` بدلاً منه) |

## القواعد
- **قاعدة التوزيع لكل درس**: مرجعها الملزم `CLAUDE.md` (٥ أساسية إلزامية + إثرائي اختياري = ٥ أو ٦ أسئلة، للعلوم حالياً). لا تُكرَّر هنا حتى لا تتعارض.
- تدرّج من السهل إلى الأصعب.
- كل سؤال مبنيّ على هدف ومستمدّ من الكتاب المدرسي (تأليف أصلي لا نسخ حرفي).

---

## ① سحب وإفلات — `drag-drop`
الطالب يسحب بطاقات النصّ ويضعها على مواضعها في صورة/رسم.

> **قاعدة تصميم معتمدة (`DESIGN_RULES.md` §خطوط التوصيل):** السلوك الافتراضي لكل المواد — خط واصل ونقطة بين منطقة الاستقبال المنفصلة (النوع أ) وموضعها على الرسم، حتى في الرياضيات. **استثناء الرياضيات للنوع (ب) فقط**: الهدف المقابل لخانة فارغة بحدّ متقطّع **داخل رسم السؤال** (كخانات صف لوحة المائة الناقصة) لا خط له ولا نقطة، والسحب مباشر إلى الخانة نفسها بين الأرقام. التصنيف تلقائي من الرسم، مطبَّق مركزياً في `renderDragDrop` وترثه كل الأسئلة تلقائياً.

| الحقل | الوصف |
|------|------|
| `image` | مسار صورة الخلفية (أو `svg` لرسم مضمّن) |
| `fit` | `"height"` للأشكال الطويلة النحيلة (كالهيكل)، أو `"width"` للصور العريضة (كصفّ هياكل جنباً إلى جنب). كلاهما يحافظ على نسبة الصورة وتُحاذى الأهداف عليها. احذفه لتملأ الصورة الملعب |
| `bg` | (اختياري) لون خلفية الملعب |
| `targets[]` | `{ answer, box:{x,y}, dot:{x,y} }` — `answer` نصّ البطاقة الصحيحة، و`box` مركز **صندوق الاستقبال** (نسبة مئوية من منطقة النشاط)، و`dot` **نقطة الارتساء** على الصورة/الرسم (نسبة مئوية منها) التي يصل إليها الخط. البطاقات تُولَّد تلقائياً من `answer` وتُخلط. **الحقلان كلاهما إلزاميّ** — `renderDragDrop` يقرأ `t.box.x` مباشرةً، فصيغةُ `{answer,x,y}` المسطّحة تُسقط بناءَ السؤال |

```js
{
  type: "drag-drop",
  objective: "4Bh7: يستنتج أنّ العظام ترتبط لتكوّن الهيكل العظمي",
  level: "application",
  prompt: "اسحب اسم كل عظم إلى مكانه في الهيكل.",
  image: "images/skeleton.png",
  fit: "height",
  bg: "#fdf9ee",
  targets: [
    { answer: "الأضلاع",      box: { x: 84, y: 26 }, dot: { x: 53, y: 27 } },
    { answer: "العمود الفقري", box: { x: 16, y: 38 }, dot: { x: 50, y: 36 } },
    { answer: "عظام الورك",   box: { x: 84, y: 52 }, dot: { x: 50, y: 46 } },
    { answer: "قصبة الساق",   box: { x: 16, y: 82 }, dot: { x: 58, y: 83 } }
  ]
}
```

> **رسمٌ أفقيٌّ طويل؟** إن كانت نسبة `viewBox` (عرض ÷ ارتفاع) أكبر من **2.2** يُرفَع ارتفاعه
> حتى ≤ 2.2 ويُعاد توزيع محتواه، **ويُعاد حساب `targets[].dot.y`** — القاعدة كاملةً في
> `.claude/skills/shoogp-ui/SKILL.md` §١.١٠.

---

## ② توصيل — `matching`
الطالب يضغط عنصراً من العمود الأيمن ثم ما يناسبه من الأيسر.

| الحقل | الوصف |
|------|------|
| `pairs[]` | `{ a, b }` — `a` العنصر، `b` ما يوصَل به. تُخلط الأعمدة تلقائياً |

```js
{
  type: "matching",
  objective: "4Bh6: يلاحظ أنّ للعظام أشكالاً وأحجاماً مختلفة",
  level: "knowledge",
  prompt: "صِل كل عظم بنوعه.",
  pairs: [
    { a: "عظمة الفخذ",   b: "عظم طويل" },
    { a: "عظام الأصابع", b: "عظم قصير" },
    { a: "الجُمجمة",     b: "عظم مسطّح" },
    { a: "الفقرة",       b: "عظم غير منتظم" }
  ]
}
```

---

## ③ اختيار من متعدد — `mcq`
سؤال بـ٣–٤ خيارات، واحد صحيح. الخيارات تُخلط تلقائياً.

| الحقل | الوصف |
|------|------|
| `options[]` | مصفوفة الخيارات النصّية |
| `answer` | فهرس الخيار الصحيح في `options` (يبدأ من 0)، **أو مصفوفةُ فهارسَ** فيصحُّ أيٌّ منها — انظر التوسعة ② أدناه |
| `audio` | (اختياريّ) مسارُ ملفٍّ صوتيّ يُعرَضُ مشغّلُه فوقَ الخيارات — انظر التوسعة ① أدناه |

```js
{
  type: "mcq",
  objective: "4Bh6: يلاحظ أنّ للعظام أشكالاً وأحجاماً مختلفة",
  level: "knowledge",
  prompt: "كم عدد العظام في هيكل جسم الإنسان؟",
  options: ["١٠٦ عظمة", "٢٠٦ عظمة", "٣٠٦ عظمة", "٤٠٦ عظمة"],
  answer: 1
}
```

### توسعتانِ اختياريتانِ على `mcq` — متوافقتانِ تماماً مع القديم

كلتاهما **لا تعملُ إلا إذا وُجِدَ الحقلُ الجديد**. والأسئلةُ القائمةُ لا تحملُه فتمرُّ على
المسارِ نفسِه حرفياً — مُتحقَّقٌ منه بتشغيلِ **كلِّ أسئلةِ `mcq` الـ١٦٩ ونقرِ كلِّ خيارٍ
فيها** ومقارنةِ الحكمِ بمنطقِ ما قبلَ التوسعة: **صفرُ اختلاف**.

#### ① `audio` — مشغّلُ صوتٍ فوقَ الخيارات

مسارُ ملفٍّ صوتيّ. يخدمُ **أسئلةَ الاستماعِ في اللغةِ العربية**: يسمعُ الطالبُ النصَّ ثمّ
يجيب. غيابُ الحقلِ = **لا عنصرَ في DOM أصلاً** (لا زرَّ ولا غلاف). والتشغيلُ **بضغطةٍ
صريحة** لا تلقائياً (المتصفّحاتُ تحجبُ التشغيلَ التلقائيّ)، وهو **مستقلٌّ عن زرِّ كتمِ
أصواتِ التغذيةِ الراجعة**. وإن فشلَ تحميلُ الملفِّ **بقيَ السؤالُ قابلاً للتصحيحِ كاملاً**
(الخطأُ مُبتلَعٌ عمداً) — مُتحقَّقٌ منه بمسارٍ مفقودٍ عمداً.

```js
{
  type: "mcq",
  objective: "يُجيب عن أسئلة تتعلّق بما استمع إليه",
  level: "knowledge",
  prompt: "استمعْ إلى النصِّ، ثمّ أجبْ: مَنْ طلبَ من النَّسرِ أن يبحثَ عن أجملِ مكان؟",
  audio: "audio/استماع-أجمل-مكان.mp3",
  options: ["المَلِكُ", "الصَّيّادُ", "الرّاعي", "التّاجِرُ"],
  answer: 0
}
```

> **زرُّ المشغّلِ هو زرُّ النوعِ الصوتيِّ نفسُه** (`.aplay`) من دالّةٍ مشتركةٍ واحدة، فالهويةُ
> البصريةُ موحّدةٌ ولا نسخةَ ثانيةً من المنطق. **وملفُّ الصوتِ من مسؤوليةِ المعلّم** —
> مجلّدُ `audio/` اليومَ فيه أصواتُ الواجهةِ فقط (`correct.mp3` · `wrong.mp3`) و`sound-bird.wav`.

#### ② `answer` مصفوفةً — أكثرُ من إجابةٍ صحيحة

يقبلُ `answer` **مصفوفةَ فهارس** بدلَ فهرسٍ واحد، فأيُّ فهرسٍ فيها يُحتسَبُ صحيحاً ويُنهي
السؤالَ بالفوز. يخدمُ أسئلةَ **الإبداعِ والتحليل** في اللغةِ العربية: «اقترحْ عنواناً آخرَ
للنصّ» و«أيُّ الجملتَينِ تصلحُ بديلاً؟» — حيثُ تصحُّ أكثرُ من إجابةٍ بحقّ، وحصرُها في
واحدةٍ يُفسدُ السؤال.

```js
{
  type: "mcq",
  objective: "يُحاكي الأساليبَ التعبيريةَ في إنتاجِ جُمَل",
  level: "application",
  prompt: "أيُّ العناوينِ يصلحُ عنواناً آخرَ للنصّ؟ (أكثرُ من إجابةٍ صحيحة)",
  options: [
    "الوطنُ أجملُ مكان",
    "رحلةُ النَّسرِ الطويلة",
    "النَّسرُ والصَّيّاد",
    "أنواعُ الطُّيورِ الجارحة"
  ],
  answer: [0, 1]
}
```

> ⚠️ **نبّهِ الطالبَ في `prompt` أنّ الإجاباتِ أكثرُ من واحدة** — وإلا ظنَّ أنّ ما اختارَه
> هو الوحيدُ الصحيح. والسؤالُ **ينتهي بأوّلِ إجابةٍ صحيحة** (لا يُطلَبُ منه اختيارُ كلِّ
> الصحيحةِ)، فصياغةُ الأمرِ تكونُ «أيُّ العناوينِ **يصلحُ**» لا «اختَرْ **كلَّ** ما يصلح».

> **والرقمُ المفردُ يعملُ كما كان بلا حرفٍ واحدٍ من التغيير** — لا تُحوَّلْ أسئلةٌ قائمةٌ
> إلى مصفوفةٍ بلا سببٍ تأليفيّ.

---

## ④ صواب وخطأ — `true-false`
عبارة يحكم عليها الطالب بصواب أو خطأ.

| الحقل | الوصف |
|------|------|
| `statement` | نصّ العبارة (يُعرض مكان `prompt`) |
| `answer` | `true` إن كانت العبارة صحيحة، `false` إن كانت خاطئة |

```js
{
  type: "true-false",
  objective: "4Bh7: يستنتج أنّ العظام ترتبط لتكوّن الهيكل العظمي",
  level: "knowledge",
  statement: "الجُمجمة تتكوّن من عظمة واحدة فقط.",
  answer: false
}
```

---

## ⑤ تحديد الأجزاء — `hotspot`
الطالب ينقر على الموضع الصحيح في صورة/رسم.

| الحقل | الوصف |
|------|------|
| `image` | مسار الصورة (أو `svg`) — تُوضع دائماً داخل غلاف يحافظ على نسبتها |
| `bg` | (اختياري) لون خلفية الملعب |
| `spot` | منطقة الإجابة الصحيحة — **شكلان**: دائريّ `{ x, y, r }` أو مستطيل `{ x, y, w, h }`. و`x`/`y` **مركزُ المنطقة** في الحالتَين (نسبة مئوية من إطار الرسم)، و`r` نصفُ القطر (الافتراضي 10)، و`w`/`h` العرضُ والارتفاعُ **كاملَين**. وإن ذُكر أحدُ البعدَين وحدَه فالآخرُ مثلُه (مربّع). **اختر الشكلَ بشكلِ الهدف** (§أدناه) |

```js
{
  type: "hotspot",
  objective: "4Bh7: يستنتج أنّ العظام ترتبط لتكوّن الهيكل العظمي",
  level: "knowledge",
  prompt: "انقر على القفص الصدري (الأضلاع) الذي يحمي القلب والرئتين.",
  image: "images/skeleton.png",
  fit: "height",
  bg: "#fdf9ee",
  spot: { x: 53, y: 27, r: 11 }
}
```

> **رسمٌ أفقيٌّ طويل؟** إن كانت نسبة `viewBox` (عرض ÷ ارتفاع) أكبر من **2.2** يُرفَع ارتفاعه
> حتى ≤ 2.2 ويُعاد توزيع محتواه، **ويُعاد حساب `spot.y`** (`spot.r` لا يُمَسّ) — القاعدة كاملةً
> في `.claude/skills/shoogp-ui/SKILL.md` §١.١٠.

> **⚠️ شكلُ `spot` يتبعُ شكلَ الهدفِ لا العكس (قرار المالك ٢٠٢٦-٠٨-٢٧):**
> الهدفُ **المربَّعُ أو المستطيلُ** — بطاقةٌ، لوحٌ، صندوقٌ، شريطُ نصٍّ، سطرٌ في جدول،
> بابٌ، سبّورةٌ — منطقتُه **مستطيلةٌ `{w,h}`**. والهدفُ **المستديرُ أو العضويُّ** —
> جمجمةٌ، عجلةٌ، جذرُ نبتةٍ، حيوانٌ، علامةٌ على مقياس — منطقتُه **دائريّةٌ `{r}`**.
>
> **والعلّةُ مقيسة:** الدائرةُ المحيطةُ بهدفٍ مربَّعٍ تقتطعُ أركانَه الأربعة، فيقعُ نحوَ
> **٣٧٪** من البطاقةِ الصحيحةِ خارجَها ويُرَدُّ نقرُ التلميذِ عليها خطأً وهو **مصيب** —
> وفي شريطٍ نصّيٍّ رفيعٍ بلغَ المهدورُ **٧٥٪**. وتوسيعُ `r` لِيَسَعَ الأركانَ يبتلعُ ما
> حولَ الهدفِ فيُقبَلُ نقرٌ خاطئ. فلا علاجَ إلا بمطابقةِ الشكل.
>
> **وقياسُ الأبعاد — والصورةُ النقطيّةُ كالرسمِ المبنيِّ سواءً بسواء:** الحقلُ نسبةٌ من
> إطارِ العرضِ لا من محتواه، فيعملُ مع `image` كما يعملُ مع `svg` بلا فرق.
> في الـSVG تُؤخذُ الأبعادُ من هندسةِ العنصرِ نفسِه (`x,y,width,height` منسوبةً إلى
> `viewBox`). وفي الصورةِ النقطيّةِ بأحدِ طريقَين: **فحصِ البكسل** حيث الهدفُ متميّزُ
> اللونِ (كبابِ المدرسةِ البرتقاليِّ وخطوطِ الممرِّ البيضاء)، أو **شبكةِ نِسَبٍ مئويةٍ
> مرقَّمةٍ تُرسَمُ فوقَ الصورةِ** فتُقرأُ حدودُ الهدفِ منها (وهي الطريقةُ الصالحةُ في
> الصورِ الفوتوغرافيةِ والمشاهدِ المزدحمة). **ثمّ يُرسَمُ المستطيلُ المقترَحُ فوقَ الصورةِ
> ويُنظَرُ إليه** قبلَ الاعتماد — ولا تُكتَبُ قيمةٌ بالعينِ المجرّدة.
>
> **وحدُّ المنطقةِ حدُّ ما ينطقُ به السؤال:** «انقرْ على الهيكلِ البالغ» ⇦ الهيكلُ كلُّه
> من الجمجمةِ إلى القدم؛ «الذراعُ التي انقبضت» ⇦ الذراعُ كلُّها لا عضلتُها؛ «المِرطبان» ⇦
> جسمُه وغطاؤه. **والتضييقُ أشيعُ من التوسيع.**
>
> **⛔ وسؤالُ تحديدٍ إجابتُه لفظٌ مكتوبٌ لا يُصوَّرُ ولا يُطلَبُ له رسم:** حين تكونُ
> الخياراتُ **حروفاً** (ك · ب · م) أو **أسماءَ أعلامٍ** (عليٌّ · ليلى · خليلٌ) أو **عباراتٍ
> منطوقةً** (السلامُ عليكم · مع السلامة)، فالمكتوبُ هو **المهارةُ المقيسةُ نفسُها** لا بديلٌ
> عن صورةٍ غائبة. فيبقى الرسمُ **طباعةً** بلوحاتٍ مكتوبة، وهو صنفٌ قائمٌ بذاتِه لا نقصٌ
> يُستدرَك. *(في «أحب لغتي ١» ثلاثةٌ من ٢٣ — والحرفُ المدروسُ يسكنُ خياراتِها: ل في ليلى
> وخليل، وس في السلام والسلامة.)*

> **والفحصُ ذو طرفَين:** أيغطّي الهدفَ كلَّه (فلا تُرَدُّ نقرةٌ صائبة)؟ **وأيبقى خارجَ
> جيرانِه** (فلا تُقبَلُ نقرةٌ خاطئة)؟ — الطرفُ الثاني ليس نظرياً: دائرةُ «الكائنِ الحيّ»
> في `g1s-1-1` كانت تبلغُ مؤخّرةَ السيّارةِ فيُحسَبُ نقرُ الجمادِ صواباً.
>
> **والحقلانِ لا يجتمعان:** وجودُ `w` (أو `h`) يُلغي `r` تماماً.

> **✳️ وشكلٌ ثالثٌ يُبنى من الأوّلَين — `spot` مصفوفةً (٢٠٢٦-٠٨-٢٨):**
> ```js
> spot: [ { x: 27.3, y: 38.6, w: 29.5, h: 17.2 },
>         { x: 72.3, y: 37.4, w: 28.6, h: 16.7 } ]     // اتّحادُ المنطقتَين
> ```
> النقرةُ صائبةٌ إن وقعت في **أيٍّ** من مناطقِ المصفوفة، وكلُّ عنصرٍ فيها دائريٌّ أو
> مستطيلٌ بالقواعدِ أعلاه نفسِها. **والحقلُ المفردُ حالةٌ خاصّةٌ منه** فلم يتغيّرْ سلوكُه
> ولا حاجةَ لتعديلِ سؤالٍ قائم.
>
> **وعلّتُه أنّ هدفاً واحداً في السؤالِ قد يكونُ أجساماً متفرّقةً في الرسم:** «اضغَطْ على
> الجُزءِ الَّذي يَصنَعُ الغِذاءَ» جوابُه **أوراقُ النبتةِ الأربعُ** المتوزّعةُ حولَ الساق.
> فالمستطيلُ الواحدُ الجامعُ لها **يبتلعُ الساقَ في وسطِه** — والساقُ **جوابُ سؤالٍ آخرَ في
> الوحدةِ نفسِها** (`g3s-1-3` #٤) — فيُقبَلُ نقرُها خطأً؛ وتضييقُه إلى ورقةٍ واحدةٍ يَرُدُّ
> نقرَ التلميذِ على الثلاثِ الباقياتِ وهو مصيب. فالمصفوفةُ ترفعُ طرفَي الفحصِ معاً.
>
> **⚠️ وهي للأجسامِ المتفرّقةِ التي يجمعُها اسمٌ واحدٌ في السؤال، لا حيلةً لتوسيعِ منطقةٍ
> واحدة** — ولا تُستعمَلُ لتغطيةِ جوابَين مختلفَين.
>
> **وينطبقُ هذا على «اكتشف الخطأ» ⑫ كذلك** (الدالّةُ `hitsSpot` واحدةٌ للنوعَين).

---

## ⑥ الترتيب التسلسلي — `sequence`
الطالب يرتّب بطاقات مبعثرة بالتسلسل الصحيح **بسحبها لتغيير أماكنها** (فأرة + لمس على السبورة). البطاقات تُخلط تلقائياً عند العرض، وتنزاح لإفساح مكان البطاقة المسحوبة.

**الآلية المعتمدة:** الأرقام (١، ٢، ٣…) تظهر في **عمود ثابت خارج الصناديق بجانبها** ولا تتحرك؛ الطالب يسحب كل بطاقة إلى جوار رقمها الصحيح، وعند اكتمال الترتيب الصحيح تتطابق البطاقات مع أرقامها. التحقّق يقارن ترتيب البطاقات الحالي بترتيب `steps`.

| الحقل | الوصف |
|------|------|
| `steps[]` | مصفوفة نصوص الخطوات **بالترتيب الصحيح**. تُعرض الأرقام في عمود ثابت خارج البطاقات، والتحقّق يقارن ترتيب البطاقات الحالي بهذا الترتيب |

```js
{
  type: "sequence",
  objective: "4Bh10: يتتبّع مسار الدواء في الجسم حتى وصوله إلى موضع الألم",
  level: "reasoning",
  prompt: "رتّب رحلة الدواء في الجسم من البداية إلى النهاية.",
  steps: [
    "تناول الدواء",
    "وصوله إلى المعدة",
    "انتقاله إلى الدم",
    "وصوله إلى مكان الألم"
  ]
}
```

---

## ⑦ التصنيف في مجموعات — `classify`
الطالب يسحب عناصر متعددة من بنك العناصر إلى **صندوق مجموعتها الصحيحة**. الصناديق جنباً إلى جنب بلا صورة مركزية ولا خطوط واصلة (النمط المعتمد للتصنيف). العناصر تُخلط تلقائياً، ويمكن سحبها بين المجموعات أو إعادتها للبنك. يعمل بالسحب (فأرة + لمس على السبورة).

**المواصفات المعتمدة:** كل صندوق = **عنوان المجموعة** (شريط علوي شفّاف) فوق **منطقة إلقاء بيضاء** تُوضع فيها العناصر. عند التحقّق: العنصر في مجموعته الصحيحة يتحوّل **أخضر**، والخاطئ **أحمر**. عناصر كبيرة تناسب اللمس على السبورة، واتجاه RTL.

| الحقل | الوصف |
|------|------|
| `groups[]` | `{ name, items }` — `name` اسم المجموعة (عنوان الصندوق)، و`items` مصفوفة العناصر التي تنتمي إليها. عند التحقّق يُقارن موضع كل عنصر باسم مجموعته الصحيحة |
| `pics` | (اختياري) `true` ← تُعرض كل بطاقة **رسماً كبيراً والكلمةُ تجاورُه** بدل النصّ وحده — §رسومُ الإجابات أدناه |

```js
{
  type: "classify",
  objective: "4Bh1: يستنتج أنّ للإنسان وبعض الحيوانات هيكلاً عظميًّا داخل الجسم",
  level: "reasoning",
  prompt: "صنّف الحيوانات حسب هيكلها.",
  groups: [
    { name: "فقاريات",   items: ["إنسان", "سمكة", "طائر"] },
    { name: "لافقاريات", items: ["حلزون", "دودة", "حشرة"] }
  ]
}
```

> **⚠️ قيدُ سعةٍ على التأليف — مشتقٌّ من القياس (الرياضيات):** التصنيفُ الثقيلُ يُسقِطُ السؤالَ
> إلى الحاويةِ المرنةِ (`qflex`) فيمتدُّ ذيلُه (سطرُ التغذيةِ الراجعة) **تحتَ نهايةِ رسمِ الإطارِ**
> على شريطِ التعبئةِ الخامِ فيبدو خارجَ الحاوية. الحدُّ المقيسُ على مصفوفةِ المنافذِ الخمسة:
> حتى **٣ مجموعاتٍ و٤ عناصرَ إجمالاً** يدخلُ إطارَ `tall` الحقيقيَّ؛ **٥ عناصرُ فأكثرُ**
> (أو ٦ بمجموعتين) يسقط. (عولجَ فعلياً في `g2m-8-1` س٥ — commit `318fd85`.)
> وفي العلومِ التصنيفُ ذو **٦ عناصرَ فأكثرَ** مرشّحٌ للسقوطِ كذلك.
> **بعدَ تأليفِ أيِّ تصنيفٍ افحصْ صنفَ الإطارِ** (قاعدةُ فحصِ الإطارِ في `CLAUDE.md`).

---

## ⑧ ملء الفراغ بالسحب — `fill-blank`
جملة (أو أكثر) فيها فراغات، والطالب يسحب الكلمة المناسبة من بنك الكلمات إلى كل فراغ. يُوضع في البنك كلمات مموّهة (distractors) لزيادة التحدّي. يعمل بالسحب (فأرة + لمس)؛ **نقر الفراغ الممتلئ يفرّغه** ويعيد كلمته للبنك. عند التحقّق يتحوّل الفراغ الصحيح **أخضر** والخاطئ **أحمر**.

| الحقل | الوصف |
|------|------|
| `text` | نصّ الجملة، وكل فراغ يُكتب كعلامة `{}` بالترتيب. مثال: `"النبات يحتاج {} و {} لينمو"` |
| `answers[]` | الكلمات الصحيحة **بترتيب الفراغات** (`answers[0]` للفراغ الأول وهكذا) |
| `distractors[]` | (اختياري) كلمات مموّهة تُضاف للبنك ولا تنتمي لأي فراغ |

```js
{
  type: "fill-blank",
  objective: "4Bh11: يدرك أهمية أخذ الدواء بالجرعة والوقت المناسبين وبإشراف بالغ",
  level: "application",
  prompt: "أكمل الجمل بسحب الكلمات المناسبة.",
  text: "يجب أخذ الدواء بـ {} المحددة، وفي {} المناسب، ولا نتناول دواءً دون إشراف {}",
  answers: ["الجرعة", "الوقت", "شخص بالغ"],
  distractors: ["اللون", "الحجم"]
}
```

> **⚠️ قيدُ سعةٍ على التأليف — مشتقٌّ من القياس (الرياضيات):** ثلاثةُ فراغاتٍ في الجملةِ
> الواحدةِ على **حافّةِ اتساعِ** إطارِ `tall` فيتأرجحُ السؤالُ بينَه وبينَ الحاويةِ المرنةِ
> (`qflex`) التي يمتدُّ ذيلُها تحتَ رسمِ الإطار؛ **فراغانِ** آمنانِ بثبات (مقيسٌ على مصفوفةِ
> المنافذِ الخمسة؛ عولجَ فعلياً في `g2m-8-2` س٤ — commit `318fd85`). إن احتاجَ الدرسُ ثلاثَ
> معلوماتٍ وُزِّعت على سؤالين أو نوعٍ آخرَ بدلَ حشرِها في جملةٍ واحدة.
> **بعدَ تأليفِ أيِّ ملءِ فراغٍ افحصْ صنفَ الإطارِ** (قاعدةُ فحصِ الإطارِ في `CLAUDE.md`).

---

## ⑨ الاستبعاد (الدخيل) — `exclude`
عناصر تشترك في صفة واحدة وبينها عنصر **دخيل** لا ينتمي، والطالب يضغط الدخيل. الخيارات تُخلط تلقائياً. النقر على عنصر منتمٍ يعلّمه **أحمر** ويعطّله (تصحيح ذاتي)، والنقر على الدخيل يعلّمه **أخضر** ويُنهي السؤال. يُنمّي المقارنة والتفكير النقدي.

| الحقل | الوصف |
|------|------|
| `options[]` | مصفوفة العناصر النصّية (منها الدخيل والبقية تشترك في صفة) |
| `answer` | فهرس العنصر الدخيل في `options` (يبدأ من 0) |
| `reason` | (اختياري) سبب عدم انتماء الدخيل، يُعرض في رسالة النجاح لترسيخ المفهوم |
| `pics` | (اختياري) `true` ← تُعرض كل بطاقة **رسماً كبيراً والكلمةُ تجاورُه** بدل النصّ وحده — §رسومُ الإجابات أدناه |

### رسومُ الإجابات — `pics` (يخدمُ سبعةَ أنواع)

بطاقةُ الإجابةِ نصٌّ خامٌّ افتراضاً. وبِـ`pics: true` تُعرَضُ **رسماً والكلمةَ تجاورُه في
سطرٍ واحد**: الرسمُ يقودُ والنصُّ يسنِد.

**والسبعةُ تُخدَمُ بدالّةٍ واحدةٍ** — `qFace` في `js/app.js` — فالمنطقُ مكتوبٌ مرّةً:
التصنيفُ (بطاقاتُ البنك) · الاستبعادُ (الخيارات) · الاختيارُ من متعدد (الخيارات) ·
**التوصيلُ (طرفا كلِّ زوجٍ، أو طرفٌ واحد)** · **الترتيبُ التسلسليُّ (كلُّ خطوة)** ·
بطاقاتُ الذاكرة (وجهُ كلِّ بطاقة) · **الخريطةُ الذهنية (كلُّ بطاقةٍ في البنك)**.
وفي الترتيبِ يقعُ الوجهُ داخلَ `.seq-txt` لا في البطاقةِ نفسِها، لأنّها تحملُ مِقبَضَ
السحبِ `≡` إلى جانبِه.

> **حدُّ التوصيل — الوحدةُ فيه العمودُ لا السؤال:** إن صلحَ **العمودانِ** أجساماً فَـ`pics: true`؛
> وإن صلحَ **عمودٌ دونَ عمود** فَـ`pics: "a"` أو `"b"` يُفعِّلُ الصالحَ وحدَه؛ وإن كانا
> عبارتَين («الضَّغطُ ↔ تَنبَعِجُ المادَّةُ») بقيَ نصّاً. **والممنوعُ العمودُ المختلط**
> — بعضُ بطاقاتِه مرسومٌ وبعضُها نصّ — فذلك يوحي أنّ المرسومَ هو المقصود.

> **✳️ وللخريطةِ الذهنيةِ حدٌّ خاصٌّ في الفحصِ لا في المحتوى (٢٠٢٦-٠٨-٢٨):** بطاقتُها
> **تنتقلُ بعينِها** إلى خانةِ الفرعِ عندَ السحب، **فارتفاعُ السؤالِ يزيدُ حينَ تمتلئُ
> الخانات** — والحالةُ الفارغةُ لا تكفي حَكَماً على الإطار. **فيُفحَصُ في الحالتَين.**
> وشبكةُ الأمانِ `contentRO` تُكبّرُ الإطارَ عندَ الامتلاءِ تلقائياً (مقيسٌ: `qf-m` ←
> `qf-l` و`qf-l` ← `qf-tall`)، **فإن كانَ الفارغُ على `qf-tall` سلفاً فلا مكانَ لينمو**.
>
> ⚠️ **ويُقاسُ بفتحِ الدرسِ كما يفتحُه المعلّمُ** (‏`openLesson` بعدَ فكِّ القفل) لا
> بـ`renderQuestions` وحدَه — فالثاني يتخطّى سياقَ الكتابِ فيختارُ إطاراً مختلفاً.

**العلّةُ تربويةٌ لا تزيينيّة:** تلميذُ الصفَّينِ الأولِ والثاني لَمّا يُتقنِ القراءةَ
بعدُ، فبطاقةٌ نصّيةٌ خالصةٌ تُحوِّلُ سؤالَ العلومِ إلى **اختبارِ قراءةٍ** يقيسُ غيرَ ما
وُضِعَ له. وهو تطبيقٌ لِما في `CLAUDE.md` §فروقِ الصفوف: «**الخياراتُ صوراً لا كلماتٍ
كلّما أمكن**» في الصفِّ الأول، و«**صورٌ مساندة**» في الثاني.

| البند | القاعدة |
|------|---------|
| **مصدرُ الرسم** | سِجِلٌّ مشترَكٌ في **`js/qpics.js`**، مفتاحُه **الكلمةُ نفسُها بعدَ التجريد** (بلا تشكيلٍ ولا «ال»). فالرسمُ يُكتَبُ مرّةً ويخدمُ كلَّ درسٍ يذكرُ الكلمة |
| **صورتانِ لا واحدة** | المدخلُ إمّا **SVG مضمَّنٌ** مبنيٌّ يدوياً، وإمّا **`<img>` إلى ملفٍّ في `images/`** مولَّدٍ خارجياً. والبطاقةُ لا مناطقَ تفاعليةَ فيها (التفاعلُ نقرُها أو سحبُها كلِّها)، فينطبقُ عليها «أين يقعُ التفاعل»: **الاستبدالُ مسموح** — سابقةُ `letter-picture` نفسُها. والملفُّ **يجبُ أن يكونَ شفّافَ الخلفية**، وإلّا ظهرَ مربّعاً أبيضَ على الكبسولةِ الزرقاء (وقعَ فعلاً ٢٠٢٦-٠٨-٢٧) |
| **وسمُ النسخة** | يُلتقَطُ في `qpics.js` من رابطِ السكربتِ نفسِه كما يفعلُ `imgURL()`، فلا يُرقَّى يدوياً |
| **`pics: "a"` / `"b"`** | في **التوصيلِ وحدَه**: تصويرُ عمودٍ واحدٍ بدلَ العمودَين. موضعُه حالتان: ① **تصويرُ العمودَينِ يُسرّبُ الجواب** — «العَينُ ↔ البَصَرُ» والحاسّةُ تُحالُ إلى عضوِها، فتظهرُ الصورةُ نفسُها في الطرفَينِ ويُطابقُها الطفلُ بلا تفكير؛ ② **طرفٌ يُرسَمُ وطرفٌ عبارةٌ** لا تُرسَم («اليَدُ ↔ نُمسِكُ بِها»). ⛔ **وليس نقضاً لقاعدةِ «نصفِ الرسوم»** — تلك في **مجموعةٍ واحدةٍ** يختارُ منها التلميذ، وهنا عمودانِ دورُهما مختلف |
| **`picMap`** | حقلٌ اختياريٌّ على السؤال يحوّلُ كلمةً إلى مفتاحٍ آخرَ **في هذا السؤالِ وحدَه**: `picMap: { "الساقُ": "رجل" }`. موضعُه الوحيدُ **الكلمةُ المشترَكةُ بينَ معنيَين** — «الساقُ» ساقُ نبتةٍ في درسِ النباتِ وساقُ إنسانٍ في درسِ الجسم، فأيُّ رسمٍ عُلِّقَ بالمفتاحِ المشترَكِ أخطأَ الدرسَ الآخر. والتحويلُ على الكلمةِ المجرّدة، فيستوي أن تُكتَبَ مشكولةً أو خالية. ⛔ **ولا يُستعمَلُ لتفضيلِ رسمٍ على رسمٍ** — ذلك يُفكّكُ وحدةَ السجلّ |
| **الاشتراكُ صريح** | لا يُفعَّلُ إلا بـ`pics: true` على السؤال. فدرسٌ لم يُطلَبْ له ذلك لا يتغيّرُ ولو تشاركَ الكلمةَ نفسَها |
| **الكلمةُ بلا رسم** | تبقى نصّاً كما كانت — السِّجِلُّ الناقصُ لا يكسرُ سؤالاً ولا يترك فراغاً |
| **الأسلوبُ البصريّ** | ورقةُ المواصفاتِ المعتمدةُ في `CLAUDE.md` بحذافيرِها: حدُّ ‎#111111‎، تدرّجُ ٣–٤ درجاتٍ بحزمٍ حادّةٍ من السلالمِ المعتمدة، لمعةٌ بيضاء، بلا خطِّ أرضٍ ولا ظلٍّ مُلقًى، **وبلا ملامحِ وجهٍ إطلاقاً** |
| **سماكةُ الحدّ** | ‎3‎ خارجيّ و‎2‎ داخليّ على لوحةِ **‎100‎** لا ‎600‎ — الأيقونةُ تُعرَضُ نحوَ ‎60px‎، فهذه القيمةُ تُبقي **السماكةَ المعروضةَ** مطابقةً لرسومِ الأسئلةِ الكبيرة. القاعدةُ محفوظةٌ في أثرِها المعروضِ لا في رقمِها المجرّد |

> **⚠️ الكلمةُ تجاورُ الرسمَ ولا تعلوه — والسببُ شكلُ الزرّ (قرارُ المالك ٢٠٢٦-٠٨-٢٧):**
> أزرارُ المنصّةِ مكسوّةٌ بصورةٍ تُفرَدُ بـ`border-image` بتقطيعٍ **رأسيُّه صفر**، فحافّةُ
> الزرِّ المرسومةُ **تكبُرُ بكِبَرِ الزرِّ** ولا تبقى ثابتة (مقيسٌ على كبسولةِ العربية:
> ‎١٦٪‎ أعلى و‎١٤٪‎ أسفل). فبطاقةٌ من سطرَينِ تُطيلُ الزرَّ رأسياً فتتّسعُ الحافّةُ معه
> وتضيقُ المساحةُ الصالحة، حتى تنزلقَ الكلمةُ وتشكيلُها **تحتَ الحافّة** — رصدَه المالكُ
> على لوحيّ. والصفُّ الواحدُ يُبقي المحتوى سطراً واحداً في وسطِ اللوحِ حيثُ هو أعرض.
>
> **والبطاقةُ تمتدُّ عرضاً لا رأسياً**، فحدُّ السَّعةِ صارَ عرضَ الصفِّ لا ارتفاعَه:
> الكلمةُ الطويلةُ («مَشْروبٌ غازِيٌّ») تُوسّعُ بطاقتَها فتقلُّ البطاقاتُ في الصفِّ الواحد.
> **افحصْ صنفَ الإطارِ بعدَ كلِّ تفعيل** (قاعدةُ فحصِ الإطارِ في `CLAUDE.md`). مقيسٌ على
> خمسةِ منافذَ (‎1280×800‎ · ‎1024×768‎ · ‎1920×1080‎ · ‎900×1400‎ · ‎800×1280‎): تسعةُ
> أسئلةِ «أحب لغتي ١» بينَ `qf-m` و`qf-tall`، وثلاثةُ أسئلةِ العلومِ `qf-s` — ولا `qflex`.

> **⚠️ ولا يُفعَّلُ حيثُ لا يفرِّقُ الرسم:** كلماتٌ رسمُها واحدٌ أو متقاربٌ لا تصلحُ
> بطاقةً مصوَّرة (مِلحٌ/سُكَّر، أنواعُ الصخورِ بعضُها من بعض) — الكلمةُ الصغيرةُ تحتَه
> تُنقذُ المعنى، لكنّ الرسمَ حينَها زينةٌ لا إفادة. أخبِرِ المالكَ قبلَ تفعيلِه فيها.

```js
{
  type: "exclude",
  objective: "يستخدم صفات الكائنات ليميّز الدخيل عن مجموعة تشترك في صفة",
  level: "reasoning",
  prompt: "أيّها لا ينتمي؟",
  options: ["عصفور", "نسر", "بطة", "قطة"],
  answer: 3,
  reason: "القطة ليست طائراً"
}
```

---

## ⑩ ترتيب الحروف — `arrange`
حروف مبعثرة والطالب يسحب كل حرف إلى خانته بالترتيب (قراءة **يمين→يسار**) لتكوين الكلمة العلمية. الحروف تُعرض **منفصلة (مفردة)** أثناء الترتيب (أوضح للأطفال)، وعند اكتمال الترتيب الصحيح تُكشف **الكلمة كاملة متّصلة** ليرى الطفل شكلها النهائي. تُحرّك بطاقة الحرف نفسها إلى الخانة (يدعم الحروف المكرّرة)، ونقر الخانة الممتلئة يعيد حرفها للبنك. يعمل بالسحب (فأرة + لمس). عند التحقّق: الحرف في مكانه الصحيح يتحوّل **أخضر** والخاطئ **أحمر**. يرسّخ المصطلحات العلمية وإملاءها.

| الحقل | الوصف |
|------|------|
| `word` | الكلمة الصحيحة. تُشتقّ منها الخانات والترتيب الصحيح (قراءة يمين→يسار). رسالة النجاح تعرضها |
| `letters` | (اختياري) الحروف المبعثرة في البنك. احذفه لتُشتقّ تلقائياً من حروف `word`. استخدمه لإضافة حرف مموّه أو للتحكّم في تقطيع كلمة فيها حروف متّصلة |
| `audio` | (اختياريّ) مسارُ ملفٍّ صوتيّ يُعرَضُ مشغّلُه فوقَ الخانات — انظر التوسعة أدناه |

```js
{
  type: "arrange",
  objective: "يرسّخ كتابة مصطلح «المادة» بترتيب حروفه ترتيباً صحيحاً",
  level: "knowledge",
  prompt: "رتّب الحروف لتكوين الكلمة الصحيحة.",
  word: "مادة"
}
```

### توسعةٌ اختيارية: `audio` — الإملاءُ المسموع

مسارُ ملفٍّ صوتيّ يُعرَضُ مشغّلُه (**«🔊 استمع للكلمة»**) فوقَ الخانات: **يسمعُ الطالبُ
الكلمةَ ثمّ يرتّبُ حروفَها** بلا أن يراها مكتوبة. يخدمُ **القضايا الإملائيةَ** في اللغةِ
العربية — وهو موضعُ **السؤالِ الإثرائيِّ في القالبِ اللغويِّ/الإملائيّ** (`CLAUDE.md`).

غيابُ الحقلِ = **لا عنصرَ في DOM أصلاً**، والسلوكُ كما كان حرفياً. وزرُّ **«إعادة ↺»
يُعيدُ بناءَ المشغّلِ تلقائياً** فلا يضيعُ بعدَ إعادةِ المحاولة. وفشلُ تحميلِ الملفِّ لا
يكسرُ السؤال.

```js
{
  type: "arrange",
  objective: "يوظّف الأنماط الإملائية: يكتب الكلمة بعد سماعها",
  level: "application",
  prompt: "استمعْ إلى الكلمة، ثمّ رتّبْ حروفَها.",
  audio: "audio/إملاء-مدرسة.mp3",
  word: "مدرسة"
}
```

> ⚠️ **لا تكتبِ الكلمةَ في `prompt` حين تستعملُ `audio`** — وإلا صارَ السؤالُ نسخاً لا
> إملاءً مسموعاً. اكتفِ بـ«استمعْ إلى الكلمة، ثمّ رتّبْ حروفَها». (والكلمةُ تبقى في `word`
> لأنّها مرجعُ التصحيح، ولا تُعرَضُ إلا عندَ الصواب.)

> **المشغّلُ هو مشغّلُ النوعِ الصوتيِّ نفسُه** (`.aplay`) من الدالّةِ المشتركة، ويُحقَنُ
> **بعدَ** محرّكِ الترتيبِ المشترك (`renderTokenOrder`) فلا يتأثّرُ النوعُ `sentence`
> الذي يشاركُه المحرّك. **وملفُّ الصوتِ من مسؤوليةِ المعلّم.**

---

## ⑪ الخريطة الذهنية الناقصة — `mindmap`
عقدة مركزية (`center`) تتفرّع إلى فروع، لكلّ فرع **عنوان ثابت** (`label`) و**خانة فارغة** يسحب إليها الطالب الكلمة الصحيحة من بنك الكلمات لبناء خريطة المفاهيم. تُرسم خطوط منحنية من المركز إلى كل فرع وتُحدَّث تلقائياً عند تغيّر الحجم. تُحرّك بطاقة الكلمة نفسها إلى الخانة (تدعم التكرار)، ونقر الخانة الممتلئة يعيد كلمتها للبنك. يعمل بالسحب (فأرة + لمس). عند التحقّق: الخانة الصحيحة **خضراء** والخاطئة **حمراء**. يبني التفكير المنظومي وربط المفاهيم.

| الحقل | الوصف |
|------|------|
| `center` | نصّ العقدة المركزية (المفهوم الرئيس) |
| `branches[]` | `{ label, answer }` — `label` عنوان الفرع الثابت، و`answer` الكلمة الصحيحة التي تُسحب إلى خانته |
| `distractors[]` | (اختياري) كلمات مموّهة تُضاف للبنك ولا تنتمي لأيّ فرع |

```js
{
  type: "mindmap",
  objective: "يربط كل حالة من حالات المادة بمثال مناسب لها",
  level: "application",
  prompt: "أكمل الخريطة الذهنية بسحب المثال المناسب إلى كل فرع.",
  center: "حالات المادة",
  branches: [
    { label: "صلبة",  answer: "طوب" },
    { label: "سائلة", answer: "ماء" },
    { label: "غازية", answer: "هواء" }
  ],
  distractors: ["نار", "ضوء"]
}
```

---

## ⑫ اكتشف الخطأ — `find-error`
صورة فيها **خطأ علمي واحد** (كائن في موطن خاطئ، أو عنصر في غير موضعه)، والطالب يضغط على موضع الخطأ. نفس آليّة **النقطة الساخنة** بصرياً: نقرة داخل دائرة الخطأ (`spot`) تفوز، وخارجها تُشجّع على التدقيق. الصورة توضَع دائماً داخل غلاف يحافظ على نسبتها. ينمّي الملاحظة والتفكير النقدي.

| الحقل | الوصف |
|------|------|
| `image` | مسار الصورة (أو `svg`) |
| `fit` | (اختياري) `"width"` للصور العريضة؛ يُحاذى الغلاف على نسبة الصورة |
| `bg` | (اختياري) لون خلفية الملعب |
| `spot` | منطقة موضع الخطأ — **شكلان**: دائريّ `{ x, y, r }` أو مستطيل `{ x, y, w, h }`، بالضبط كما في **تحديد الأجزاء ⑤** (المرجع هناك) |

```js
{
  type: "find-error",
  objective: "يميّز الكائن الموجود في موطن غير مناسب له",
  level: "reasoning",
  prompt: "في هذا المشهد كائن وُضع في الموطن الخطأ — اضغط على الخطأ.",
  image: "images/خطأ-الموطن.png",
  spot: { x: 30, y: 26, r: 13 }
}
```

> **رسمٌ أفقيٌّ طويل؟** إن كانت نسبة `viewBox` (عرض ÷ ارتفاع) أكبر من **2.2** يُرفَع ارتفاعه
> حتى ≤ 2.2 ويُعاد توزيع محتواه، **ويُعاد حساب `spot.y`** (`spot.r` لا يُمَسّ) — القاعدة كاملةً
> في `.claude/skills/shoogp-ui/SKILL.md` §١.١٠.

---

## ⑬ السؤال الصوتي — `audio-q`
يُشغَّل صوت (صوت حيوان مثلاً) بزر **«استمع»**، والطالب يختار مصدره من **صور الخيارات** (تُخلط تلقائياً كنمط الاختيار من متعدد). النقر على الصورة الصحيحة → فوز، والخاطئة → تُعطّل ويُشجَّع على الاستماع ثانيةً. صوت السؤال يُشغَّل بضغطة صريحة على الزر و**لا يخضع لكتم أصوات التغذية الراجعة** (correct/wrong) كي تبقى مسموعة. إن تعذّر تحميل صورة خيار تُخفى وتبقى **تسميتها** ظاهرة (تدرّج سليم قبل توليد الصور). يدرّب حاسة السمع.

| الحقل | الوصف |
|------|------|
| `sound` | مسار ملف الصوت في `audio/` (mp3 أو wav) |
| `options[]` | `{ image, label }` — `image` مسار صورة الخيار في `images/`، و`label` اسمه (يظهر كتسمية وكبديل إن غابت الصورة) |
| `answer` | فهرس الخيار الصحيح في `options` (يبدأ من 0) |

```js
{
  type: "audio-q",
  objective: "يميّز مصدر الصوت من بين الحيوانات بالاستماع",
  level: "knowledge",
  prompt: "استمع إلى الصوت، ثم اختر الحيوان الذي أصدره.",
  sound: "audio/sound-bird.wav",
  options: [
    { image: "images/حيوان-طائر.png", label: "طائر" },
    { image: "images/حيوان-قطة.png",  label: "قطة" },
    { image: "images/حيوان-ضفدع.png", label: "ضفدع" }
  ],
  answer: 0
}
```

---

## ⑭ التلوين بالتعليمات — `color`
رسم **SVG بأجزاء منفصلة** (كل جزء عنصر `<g class="cpart" data-name="…">` قابل للتلوين — بنيته على نمط مجموعات `<g>` في دورة حياة النبات، **لا صورة نقطية خارجية ولا برومبت**). الطالب يختار لوناً من **لوحة ألوان كبيرة** ثم يضغط الجزء فيتلوّن بالكامل. عند التحقّق: كل جزء مطلوب باللون الصحيح → **فوز**، وإلّا يُحاط الجزء الخاطئ بإطار أحمر متقطّع ويُعرض عدد الصحيح. **الأجزاء غير المذكورة في `parts` تبقى حرّة** (تلوينها أو تركها لا يؤثّر في النتيجة). عناصر كبيرة تناسب اللمس على السبورة واتجاه RTL، والصوت (correct.mp3/wrong.mp3) عبر `qWin/qFail` خاضعاً لزرّ الكتم العامّ.

**بنية الرسم:** كل جزء قابل للتلوين هو `<g class="cpart" data-name="اسم الجزء" id="part-…">` يحوي أشكاله؛ تُترك الأشكال **بلا `fill` خاصّ بها** لترث لون المجموعة (الكود يضبط `fill` على `<g>` عند الضغط). التفاصيل الثابتة (كعيون الجمجمة، خطوط الأضلاع) تُعطى `fill` أو `fill="none"` صريحاً فلا يطالها التلوين.

> **✳️ والجزءُ الواحدُ قد يكونُ عدّةَ عناصرَ لا عنصراً واحداً (٢٠٢٦-٠٨-٢٨):** كلُّ عنصرٍ
> يحملُ `data-name` نفسَه فهو من الجزءِ نفسِه — النقرُ على أيِّها يلوّنُ الجميعَ، والتحقّقُ
> يقرأُ أوّلَها، وإطارُ الخطأِ يُحيطُها كلَّها. **والعنصرُ المفردُ حالةٌ خاصّةٌ من ذلك**
> فلم يتغيّرْ سلوكُ سؤالٍ قائم.
>
> **وعلّتُه أنّ الرسمَ المتتبَّعَ بـ`vtracer` لا يُخرِجُ الجزءَ مساراً واحداً دائماً:**
> أزهارُ `images/نبتة-كاملة.svg` **خمسةُ مسارات**، ولا تُدمَجُ في مسارٍ واحدٍ لأنّ التتبّعَ
> **مكدَّسٌ** — بعضُ مساراتِ الجزءِ مرسومٌ فوقَ حدٍّ أسودَ وبعضُها تحتَه، فدمجُها في موضعِ
> أوّلِها يدفنُ ما كان فوقَ الحدِّ فيبتلعُه السوادُ (مقيسٌ: جُرِّبَ فاسودَّ الرسم).
> **فالحلُّ في الكود لا في هندسةِ الرسم.**
>
> **وحينَها يُوضَعُ `class="cpart"` على `<path>` نفسِه** مع `fill` سمةً عليه (لا `<g>`)،
> فالنمطُ السطريُّ `style.fill` الذي يضبطُه الكودُ **يعلو على سمةِ العنصرِ نفسِه** —
> بخلافِ الوراثةِ من `<g>` التي **لا تعلو** على سمةِ `fill` في الابن.

| الحقل | الوصف |
|------|------|
| `svg` | رسم SVG مضمّن، كل جزء قابل للتلوين `<g class="cpart" data-name="…">`. `data-name` يُطابق اسم الجزء في `parts` |
| `palette[]` | `{ name, color }` — ألوان اللوحة المتاحة (`name` اسم اللون، `color` قيمته السداسية) |
| `parts[]` | `{ name, color }` — لكل جزء **مطلوب** اسمه (يطابق `data-name`) ولونه الصحيح. الأجزاء غير المذكورة هنا حرّة |
| `bg` | (اختياري) لون خلفية الملعب |
| `showLabels` | (اختياري) التحكّم بشارات أسماء الأجزاء فوق الرسم: `false` يخفيها كلها، `true` يُظهرها. **القاعدة: التسمية تُعرَض فقط إن كانت معلومة تعليمية** — إن لم يُعيَّن الحقل، تُخفى الشارات **تلقائياً** حين تكون كل أسماء `parts` معرّفات تقنية متسلسلة على نمط «خانة N» (الطالب يلوّن بالموضع لا بالاسم، كما في إطارات العشرة `g2m-5-1`)؛ والأسماء الحقيقية (أعداد، أعضاء…) تبقى ظاهرة. `data-name` يبقى في الرسم دائماً — التحقّق ومساحة اللمس لا يتأثران بالإخفاء. التعيين الصريح يتقدّم على الافتراض |

```js
{
  type: "color",
  objective: "4Bh7: يستنتج أنّ العظام ترتبط لتكوّن الهيكل العظمي",
  level: "knowledge",
  prompt: "لوّن كل جزء من الهيكل حسب التعليمات: اختر لوناً من اللوحة ثم اضغط الجزء.",
  bg: "#fdf9ee",
  palette: [
    { name: "أزرق", color: "#2f6fb0" },
    { name: "أحمر", color: "#cf3b3b" },
    { name: "أخضر", color: "#3e9b4f" }
  ],
  parts: [
    { name: "الجمجمة",       color: "#2f6fb0" },
    { name: "القفص الصدري",  color: "#cf3b3b" },
    { name: "العمود الفقري", color: "#3e9b4f" }
  ],
  svg: `<svg viewBox="0 0 220 430" xmlns="http://www.w3.org/2000/svg">
    <g class="cpart" data-name="الجمجمة" id="part-skull"> … </g>
    <g class="cpart" data-name="القفص الصدري" id="part-ribs"> … </g>
    <g class="cpart" data-name="العمود الفقري" id="part-spine"> … </g>
    <g class="cpart" data-name="عظمة فخذ" id="part-femur"> … </g>
  </svg>`
}
```

> ملاحظة: هذا النوع لا يحتاج برومبت رسم ولا ملف صورة خارجي؛ الرسم مضمّن في البيانات كأجزاء `<g>` قابلة للتلوين. انظر المثال الحيّ الكامل في `js/questions.js` تحت `"g4s-1-2"`.

> **رسمٌ أفقيٌّ طويل؟** إن كانت نسبة `viewBox` (عرض ÷ ارتفاع) أكبر من **2.2** يُرفَع ارتفاعه
> حتى ≤ 2.2 **وتُعاد أجزاء `g.cpart` توزيعاً على الارتفاع الجديد** — القاعدة كاملةً في
> `.claude/skills/shoogp-ui/SKILL.md` §١.١٠.

### نمطُ تأليفٍ للغة العربية: تلوينُ الكلماتِ بدلَ أجزاءِ الرسم

**صيغةُ النوعِ لا تتغيّرُ بحرف** — لا حقلَ جديدٌ ولا سلوكٌ جديد. النوعُ لا يشترطُ أن يكونَ
الهدفُ *شكلاً*، بل أن يكونَ **عنصراً يحملُ `class="cpart"` و`data-name`**؛ و`<text>` في SVG
يحملُهما. فيُستعملُ النوعُ كما هو في أسئلةٍ نحويةٍ وإملائيةٍ مثل «لوّنِ الاسمَ بالأحمرِ
والفعلَ بالأزرق» أو «لوّنِ الحروفَ الشمسيةَ بلونٍ والقمريةَ بآخر».

**بنيةُ الكلمةِ الواحدة — ثلاثةُ عناصرَ بأدوارٍ مفصولة:**

| العنصر | دورُه | لماذا لا يُستغنى عنه |
|-------|------|---------------------|
| ① بطاقةٌ مرئية | `<rect>` بـ`fill` و`stroke` **خاصَّين بها** | تدلُّ الطالبَ أنّ الكلمةَ تُنقَر، وخصائصُها الذاتيةُ تمنعُ التلوينَ من طَمْسِها |
| ② مستطيلٌ شفّاف | `<rect fill="none" pointer-events="all">` | الافتراضُ في `<text>` أنّ **بكسلاتِ الحروفِ المرسومةِ وحدَها** تُنقَر، فالنقرُ في الفراغِ بينها يخطئ. هذا يوسّعُ مساحةَ اللمسِ إلى البطاقةِ كلِّها — وهو شرطٌ على السبّورةِ الذكية |
| ③ النصّ | `<text>` **بلا `fill` إطلاقاً** | ليرثَ لونَ المجموعةِ، وهو ما يضبطُه الكودُ عندَ النقر (`part.style.fill = chosen`) |

**و`style` سطريٌّ على المجموعةِ يؤدّي وظيفتَين:**

```
style="stroke:none;fill:#3a3226"
```

- **`stroke:none` يُبطلُ حدَّ الـ3px**: قاعدةُ `.csvg .cpart` في `css/style.css` تفرضُ
  `stroke:#111111; stroke-width:3` — وهي مناسبةٌ للأشكالِ وكارثةٌ على النصّ (حدٌّ أسودُ حولَ
  كلِّ حرفٍ يجعلُ الكلمةَ لطخةً في الأحجامِ الصغيرة). **والسطريُّ يعلو على قاعدةِ CSS**، بخلافِ
  سماتِ العرضِ (`stroke="none"` سمةً) التي **تخسرُ أمامَها** — فلا تُجزئُ هنا.
- **`fill:#3a3226` يضبطُ اللونَ الابتدائيَّ الحياديّ**: وإلا ورثتِ الكلمةُ `fill:#efe7d3`
  من القاعدةِ نفسِها (لونٌ شاحبٌ يكادُ يختفي على ملعبٍ فاتح).

> ⚠️ **المصيدةُ التي تُسقِطُ التحقّقَ بلا أثرٍ مرئيّ:** التحقّقُ يجري بـ
> `querySelector('.cpart[data-name="…"]')` وهو **مطابقةٌ حرفيّة**. فاكتبِ `data-name`
> **بلا حركاتٍ** واتركِ التشكيلَ للنصِّ المعروضِ وحدَه. حركةٌ واحدةٌ زائدةٌ في أحدِهما
> تجعلُ السؤالَ يبدو سليماً تماماً ثمّ **لا يُصحَّح أبداً** — ولا شيءَ على الشاشةِ يدلُّك
> على السبب.

> **السعةُ المقيسة:** ثلاثُ كلماتٍ في سطرٍ واحدٍ على `viewBox="0 0 600 280"` (نسبة ‏٢٫١٤،
> دونَ سقفِ ٢٫٢) تدخلُ إطارَ `qf-tall` بلا فيضانٍ إطلاقاً (`scrollHeight == clientHeight`)
> والذيلُ داخلَ الإطارِ بهامش ‏+157px. وللجملِ الأطولِ **يُوزَّع النصُّ على سطرَين** ويُرفَعُ
> الارتفاعُ حفاظاً على سقفِ النسبة، لا أن يُضغَطَ خطُّ الكلماتِ فيصغُر.

**مثالٌ كاملٌ جاهزٌ للنسخ:**

```js
{
  type: "color",
  objective: "يوظّف الأنماط اللغوية: يميّز الاسم من الفعل في الجملة",
  level: "application",
  prompt: "لَوِّنِ الاسْمَ بِالأَحْمَرِ وَالفِعْلَ بِالأَزْرَقِ.",
  bg: "#fdf9ee",
  showLabels: false,                    // وإلا تكرّرت الكلماتُ في شريطٍ فوقَ الرسم
  palette: [
    { name: "أحمر", color: "#c0392b" },
    { name: "أزرق", color: "#1f6feb" }
  ],
  parts: [                              // الأسماءُ بلا حركات — تطابقُ data-name حرفياً
    { name: "كتب",   color: "#1f6feb" },
    { name: "الولد", color: "#c0392b" },
    { name: "الدرس", color: "#c0392b" }
  ],
  svg: `<svg viewBox="0 0 600 280" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="جملة: كتب الولد الدرس">
  <g class="cpart" data-name="كتب" style="stroke:none;fill:#3a3226">
    <rect x="404" y="96" width="168" height="92" rx="18" fill="#fffdf6" stroke="#d8c9a4" stroke-width="2"/>
    <rect x="404" y="96" width="168" height="92" rx="18" fill="none" pointer-events="all"/>
    <text x="488" y="158" text-anchor="middle" font-family="Tajawal, sans-serif" font-weight="700" font-size="52">كَتَبَ</text>
  </g>
  <g class="cpart" data-name="الولد" style="stroke:none;fill:#3a3226">
    <rect x="216" y="96" width="176" height="92" rx="18" fill="#fffdf6" stroke="#d8c9a4" stroke-width="2"/>
    <rect x="216" y="96" width="176" height="92" rx="18" fill="none" pointer-events="all"/>
    <text x="304" y="158" text-anchor="middle" font-family="Tajawal, sans-serif" font-weight="700" font-size="52">الوَلَدُ</text>
  </g>
  <g class="cpart" data-name="الدرس" style="stroke:none;fill:#3a3226">
    <rect x="28" y="96" width="176" height="92" rx="18" fill="#fffdf6" stroke="#d8c9a4" stroke-width="2"/>
    <rect x="28" y="96" width="176" height="92" rx="18" fill="none" pointer-events="all"/>
    <text x="116" y="158" text-anchor="middle" font-family="Tajawal, sans-serif" font-weight="700" font-size="52">الدَّرْسَ</text>
  </g>
</svg>`
}
```

> **ترتيبُ الكلماتِ من اليمينِ إلى اليسار** يُضبَط بإحداثيّاتِ `x` لا باتّجاهِ النصّ: الكلمةُ
> الأولى في الجملةِ أكبرُ `x` (أقصى اليمين). و`text-anchor="middle"` معَ `x` في منتصفِ البطاقةِ
> يُبقي الكلمةَ متوسّطةً مهما طالت.

> **مقيسٌ ومُختبَر:** جُرّبَ هذا المثالُ حيّاً في المنصّة — الحدُّ خرجَ `none` واللونُ الابتدائيُّ
> `rgb(58,50,38)`، والتلوينُ المعكوسُ أعطى «الصحيح ٠ من ٣» بوسمِ الكلماتِ الثلاثِ بـ`cwrong`،
> والتلوينُ الصحيحُ أعطى «🎨 أحسنت! لوّنت كل جزء باللون الصحيح» بلا وسومِ خطأ. **ولم يُضَف
> السؤالُ إلى بنكِ الأسئلة** — هو نمطُ تأليفٍ مرجعيٌّ هنا، يُستعمَلُ عندَ الحاجةِ إليه.

---

## ⑮ البازل (تركيب الصورة) — `puzzle`

> **قاعدة تصميم معتمدة (`DESIGN_RULES.md` §أسئلة البازل — صورة القطع):** الصورة المصدر يجب أن تكون **ممتلئة المعالم بالكامل** — كل قطعة من قطع الشبكة تحمل معالم مميّزة يستدلّ بها الطالب على موضعها، ولا يُسمح بقطعة خالية. وزّع المحتوى على كل الخلايا (إطار يمرّ بالحواف، رموز مختلفة في الأركان، عناصر تعبر حدود القطع). حارس مركزي في `renderPuzzle` يحذّر في الطرفية من أي خلية شبه خالية.

**صورة واحدة كاملة** يقسّمها الكود برمجياً إلى شبكة `grid.cols × grid.rows` قطعاً (٣×٣ مثلاً) عبر خاصية `background-position` — **لا حاجة لصور متعددة، صورة واحدة تكفي**. تُبعثَر القطع في صينية سفلية، ويعيد الطالب ترتيبها بالسحب (فأرة + لمس) إلى لوح الشبكة؛ إفلات قطعة على خانة مشغولة يبدّل ساكنها إلى الصينية. عند التحقّق: كل قطعة في خانتها الصحيحة → **مكافأة** (تختفي الفواصل والحدود فتظهر الصورة صورةً واحدة متّصلة) وصوت `correct.mp3`، وإلّا يُحاط كل خانة خاطئة بإطار أحمر ويُعرض عدد الصحيح. لوح الشبكة `direction:ltr` كي تطابق أعمدةُ الشبكة أعمدةَ الصورة (الصورة محتوى بصري لا نصّ) بينما تبقى بقيّة الواجهة RTL، والقطع كبيرة تناسب اللمس على السبورة، والصوت (`correct.mp3`/`wrong.mp3`) عبر `qWin/qFail` خاضعاً لزرّ الكتم العامّ.

| الحقل | الوصف |
|------|------|
| `image` | مسار **صورة واحدة كاملة** (يُفضَّل أن يملأ الكائن الإطار ويتوسّطه)؛ الكود يقسّمها ويبعثرها. اللوح يأخذ نسبة الصورة الحقيقية تلقائياً |
| `grid` | `{ cols, rows }` — عدد أعمدة وصفوف التقسيم (الافتراضي `{ cols: 3, rows: 3 }`). عدد القطع = `cols × rows` |
| `bg` | (اختياري) لون خلفية اللوح |

```js
{
  type: "puzzle",
  objective: "يتعرّف على الشكل العام للهيكل العظمي وأجزائه",
  level: "application",
  prompt: "ركّب صورة الهيكل العظمي: اسحب كل قطعة إلى مكانها الصحيح.",
  image: "images/هيكل-بازل.png",
  bg: "#fdf9ee",
  grid: { cols: 3, rows: 3 }
}
```

> ملاحظة: يحتاج هذا النوع **صورة واحدة** فقط (برومبتها في `برومبتات-الرسومات.docx` بأسلوب باقي البرومبتات ومرساة الأسلوب المسطّح)؛ الكود يتكفّل بالتقسيم والبعثرة وإعادة التركيب. انظر المثال الحيّ في `js/questions.js` تحت `"g4s-1-2"`.

---

## ⑯ الشريط المتدرّج — `slider`
**شريط أفقيّ** بمؤشّر دائريّ كبير يسحبه الطالب لتحديد **قيمة رقمية** على **تدريج مرئيّ واضح بأرقام كبيرة** (كمسطرة/مقياس حرارة). يبدأ المؤشّر في **منتصف الشريط** (لا عند الإجابة)، ويعمل بالسحب (فأرة + لمس على السبورة)، كما **ينقر الطالب أيّ موضع على الشريط فينتقل المؤشّر إليه مباشرةً**. تُعرض القيمة المختارة بأرقام كبيرة أعلى الشريط لحظةً بلحظة. عند التحقّق: إذا كان **الفرق بين القيمة المختارة والصحيحة ≤ `tolerance`** يفوز الطالب (يتحوّل المؤشّر أخضر)، وإلّا يُعرض تلميح إن كانت قيمته **أصغر** أو **أكبر** من المطلوب. يعلّم التقدير والقياس للمفاهيم الرقمية.

**هامش القبول (`tolerance`)**: لأنّ اللمس على السبورة الذكية **غير دقيق**، يُسمح بهامش (±) حول القيمة الصحيحة كي لا يُحرَم الطالب لخطأ لمسٍ بسيط. مثال: `answer: 100, tolerance: 2` يقبل كلّ قيمة من ٩٨ إلى ١٠٢.

**الاتجاه**: الشريط `dir:ltr` كي **تتصاعد الأرقام يساراً→يميناً** كمسطرة (الأدنى يساراً والأقصى يميناً)، بينما تبقى بقيّة الواجهة (النصّ والأزرار) RTL. عناصر كبيرة (مؤشّر ٤٦px وأرقام واضحة) تناسب اللمس على السبورة، والصوت (`correct.mp3`/`wrong.mp3`) عبر `qWin/qFail` خاضعاً لزرّ الكتم العامّ.

| الحقل | الوصف |
|------|------|
| `min` | أدنى قيمة على الشريط (طرفه الأيسر) |
| `max` | أقصى قيمة على الشريط (طرفه الأيمن) |
| `answer` | القيمة الصحيحة المطلوبة |
| `tolerance` | (اختياري) هامش القبول ± حول `answer` (الافتراضي 0). يُنصح به على السبورة لعدم دقّة اللمس |
| `unit` | (اختياري) لاحقة تُعرض بعد كل رقم (مثل `"°"` للحرارة أو `"cm"`) |
| `step` | (اختياري) خطوة تقريب القيمة عند السحب (الافتراضي 1) |
| `ticks` | (اختياري) المسافة بين علامات التدريج الكبيرة وأرقامها. احذفه ليُشتقّ تلقائياً نحو ٦ فترات |
| `svg` / `image` | (اختياري) رسمٌ يُعرض **فوق الشريط** مرجعاً بصرياً للمقدار المطلوب تقديره (قلمٌ يُقدَّر طولُه، إناءٌ تُقدَّر سعتُه). `svg` مباشر أو `image` مسارُ صورة في `images/`. يُحذف من DOM كلياً إن غاب |

> **قاعدة الرسم في هذا النوع:** الرسمُ **مرجعُ تقديرٍ لا أداةُ قياس** — يُرسم **بلا تدريجٍ ولا مسطرةٍ ولا محاذاةٍ لتدريج الشريط**. فالسؤال تقديرٌ («تقريباً»)، ولو حوذي الرسمُ بالتدريج لصار قراءةً مباشرةً وانحدر مستواه المعرفيّ من التطبيق إلى المعرفة. ومن أراد قراءةَ قياسٍ حقيقيةً فنوعُها `measure-tool` لا هذا.
> وعرضُ الرسم محدودٌ بـ`360px` في `.sldfig` (‏css/style.css) كي لا يزاحمَ الشريطَ ولا يدفعَ الإطارَ إلى الحاوية المرنة.

```js
{
  type: "slider",
  objective: "يحدد درجة غليان الماء على مقياس الحرارة",
  level: "knowledge",
  prompt: "اسحب المؤشر إلى درجة غليان الماء على مقياس الحرارة.",
  min: 0,
  max: 120,
  answer: 100,
  tolerance: 2,
  unit: "°",
  ticks: 20
}
```

> ملاحظة: هذا النوع لا يحتاج صورة ولا برومبت رسم؛ الشريط والتدريج مبنيّان بالكود بالكامل من `min`/`max`/`ticks`. انظر المثال الحيّ في `js/questions.js` تحت `"g4s-3-6"`.

---

## ⑰ بطاقات الذاكرة — `memory`
شبكة **بطاقات مقلوبة**؛ كل زوج `{ a, b }` يولّد **بطاقتين تشتركان في مفتاح** (فهرس الزوج) وتُخلط كل البطاقات مقلوبة. يقلب الطالب **بطاقتين في كل دور**: إن تطابقتا (نفس المفتاح) **تبقيان مكشوفتين** بلون النجاح (صوت `correct.mp3` + نجمة)، وإن لم تتطابقا **تُقلبان ثانيةً بعد لحظة** (صوت `wrong.mp3`). ينتهي السؤال عند **كشف كل الأزواج**. البطاقات كبيرة مربّعة تناسب اللمس على السبورة، والواجهة RTL، والصوت (`correct.mp3`/`wrong.mp3`) عبر `playCorrectSound`/`qWin` خاضعاً لزرّ الكتم العامّ.

**النسخة الحالية نصّية** (مطابقة **كلمة بكلمة** مرتبطة، مثل طائر ← صفته) — **لا تحتاج صوراً ولا برومبت رسم**. عدد الأعمدة يُشتقّ تلقائياً من عدد البطاقات (٦ بطاقات ← ٣ أعمدة، ٨ ← ٤).

| الحقل | الوصف |
|------|------|
| `pairs[]` | `{ a, b }` — طرفا الزوج المتطابق (`a` و`b`). كل زوج يولّد بطاقتين تُكشفان معاً عند تطابق مفتاحهما. عدد البطاقات = `pairs.length × 2` (٤ أزواج = ٨ بطاقات) |

```js
{
  type: "memory",
  objective: "يربط كل طائر بصفته المميزة",
  level: "knowledge",
  prompt: "اعثر على الأزواج المتطابقة: اقلب بطاقتين في كل دور لتطابق كل طائر بصفته.",
  pairs: [
    { a: "النعامة", b: "أكبر طائر" },
    { a: "البطريق", b: "لا يطير" },
    { a: "الطنّان",  b: "أصغر طائر" },
    { a: "البومة",  b: "تصطاد ليلاً" }
  ]
}
```

> ملاحظة: النسخة الحالية نصّية بالكامل (بطاقتا كل زوج نصّان) فلا تحتاج صوراً؛ يمكن لاحقاً توسعة النوع لمطابقة **صورة ↔ اسم** بإضافة صور للبطاقات. انظر المثال الحيّ في `js/questions.js` تحت `"g4s-2-1"` (الطيور المُدهشة).

---

## ⑱ العدسة المكبّرة — `lens`
**نوع خاصّ بالعلوم** (انظر قسمه في `question-types.md`) — حالته: **مبني، قيد الاختبار**.
صورتان PNG **متطابقتا الأبعاد**: العلوية (`image`) هي **المشهد الظاهر**، والسفلية (`hidden`) هي **الطبقة الخفية** تحته. العدسة **إطار صوري** (`images/عدسة-إطار.png` — فتحة شفافة الجوف بمقبض)، ودائرة الكشف `clip-path` (قطر **~160px**) مطابقة لفتحة الإطار موقعاً وقطراً؛ يسحبها الطالب من جسم الإطار أو المقبض (فأرة + لمس معاً — الاستخدام الأساسي سبورة تفاعلية بالأصابع) فتكشف الطبقة السفلية أينما تحرّكت. العناصر المخفية **نقاط ساخنة** (`spots`): يضغط الطالب العنصر **وهو ظاهر داخل العدسة** — الصحيح = صوت النجاح المعتمد + **يثبت العنصر مكشوفاً مع تسميته** + تقدّم عدّاد «اكتشفت ٢ من ٥»؛ والخاطئ = تغذية الخطأ المعتمدة (`qFail`: اختناق محرّك الصاروخ، و`wrong.mp3` في واجهة بلا صاروخ). يكتمل السؤال **باكتشاف كل العناصر**. واجهة عربية RTL وعناصر كبيرة للسبورة.

**اصطلاح تسمية الصورتين:** الصورة العلوية باسم موضوع السؤال (بشَرطات بلا مسافات)، والسفلية **بنفس الاسم + لاحقة `-خفي`** — مثال: `طفل-أشعة.png` و`طفل-أشعة-خفي.png`.

| الحقل | الوصف |
|------|------|
| `image` | مسار الصورة **العلوية** (المشهد الظاهر) |
| `hidden` | مسار الصورة **السفلية** (الطبقة الخفية) — بنفس أبعاد العلوية تماماً كي تتطابق الطبقتان |
| `fit` | (اختياري) `"width"` للصور العريضة (كنمط hotspot) |
| `bg` | (اختياري) لون خلفية الملعب |
| `spots[]` | `{ label, x, y, r, alt? }` — العناصر المخفية: `label` التسمية التي تظهر عند الاكتشاف، و`x`/`y` مركز العنصر (نسبة مئوية على الصورة)، و`r` نصف القطر المقبول (نسبة مئوية، الافتراضي 10). `alt` (اختياري) = `{ x, y, r? }` دائرة **بديلة** لنفس العنصر تُقبل أيضاً — للأعضاء المزدوجة المتماثلة (كالذراعين والساقين) كي لا تُحتسب الجهة الأخرى خطأً؛ التسمية تظهر عند الدائرة الرئيسة. `labelBelow: true` (اختياري) يعرض التسمية **أسفل** العنصر بدل أعلاه — للعناصر المتجاورة كي لا تتراكب تسمياتها |

```js
{
  type: "lens",
  objective: "4Bh1: يستنتج أنّ للإنسان وبعض الحيوانات هيكلاً عظميًّا داخل الجسم",
  level: "application",
  prompt: "اسحب عدسة الأشعة السينية فوق جسم الطفل، واضغط على كل جزء من هيكله العظمي يظهر داخلها.",
  image: "images/طفل-أشعة.png",
  hidden: "images/طفل-أشعة-خفي.png",
  bg: "#fdf9ee",
  spots: [
    { label: "الجمجمة",       x: 49.5, y: 23.5, r: 8 },
    { label: "القفص الصدري",  x: 50,   y: 43,   r: 6 },
    { label: "العمود الفقري", x: 50,   y: 53,   r: 5.5 },
    { label: "عظام الذراعين", x: 40.5, y: 55,   r: 8,   alt: { x: 59.5, y: 55 },   labelBelow: true },
    { label: "عظام الساقين",  x: 45.5, y: 72.5, r: 9,   alt: { x: 53,   y: 72.5 }, labelBelow: true }
  ]
}
```

> ملاحظة: يحتاج هذا النوع **صورتين متطابقتي الأبعاد** (برومبتاهما بأسلوب ملف البرومبتات ومرساة الأسلوب المسطّح، مع تطابق الوضعية والإطار بين الصورتين). انظر المثال الحيّ في `js/questions.js` تحت `"g4s-1-1"` (الهياكل العظمية).

---

## ⑲ بناء المعادلة — `equation-builder`
**نوع خاصّ بالرياضيات** (انظر قسمه في `question-types.md`) — فئته **أساسية**.
معادلة رموزها في مصفوفة `tokens` يمثّل فيها `"__"` **خانة فارغة**، وبنك بطاقات `bank` (أرقام ورموز `+ − × ÷ =`) تُسحب إلى الخانات، والصواب في `answers` **بترتيب الخانات**. الرموز الثابتة تُطبع كبيرة واضحة، والخانات والبطاقات عناصر لمس كبيرة.

**التفاعل:** سحب البطاقة إلى الخانة (فأرة + لمس)، **أو** نقر البطاقة لاختيارها ثم نقر الخانة (أدقّ على السبورة)؛ ونقر الخانة الممتلئة **بلا بطاقة مختارة** يفرّغها ويعيد بطاقتها للبنك. منطق البنك هو **نفسه منطق `fill-blank`** (دالّة `wireBank` المشتركة في `js/app.js`) — بطاقة واحدة لا تُشطب إلا بهويتها، فتصحّ الأعداد المكرّرة في البنك.

**الوضعان (`mode`):**
- **`fill`** (إكمال الناتج أو المجهول) — كل خانة تُقارَن بـ`answers[i]`؛ الصحيحة **خضراء** والخاطئة **حمراء**، والفوز باكتمال الخانات كلها صحيحة.
- **`equivalence`** (الحكم على تكافؤ طرفين) — تُشقّ المعادلة عند `=` ويُحسب الطرفان، ويُقبل **أيّ ملء يجعلهما متساويين** لا الإجابة النموذجية وحدها (هذا معنى التكافؤ). الحساب يحترم أسبقية `×` و`÷` على `+` و`−`. و`answers` تبقى الإجابة النموذجية للمراجعة.

| الحقل | الوصف |
|------|------|
| `mode` | `"fill"` (الافتراضي) أو `"equivalence"` |
| `tokens[]` | رموز المعادلة بالترتيب؛ `"__"` = خانة فارغة، وما عداها رقم أو رمز عملية يُطبع ثابتاً |
| `bank[]` | بطاقات البنك (أرقام ورموز). تُخلط تلقائياً، ويجوز تكرار القيمة نفسها |
| `answers[]` | الإجابات الصحيحة **بترتيب الخانات** (`answers[0]` للخانة الأولى وهكذا) |

```js
{
  type: "equation-builder",
  mode: "fill",
  objective: "2Nc1: يجمع عددين ضمن العشرين",
  level: "application",
  prompt: "أكمِلِ المعادلةَ: اسحَبِ البِطاقةَ المُناسِبةَ إلى الخانةِ الفارِغةِ.",
  tokens: ["٤", "+", "٦", "=", "__"],
  bank: ["٨", "٩", "١٠", "١١"],
  answers: ["١٠"]
}
```

> ملاحظة: لا يحتاج هذا النوع صورة ولا برومبت رسم؛ المعادلة والبطاقات مبنيّة بالكود بالكامل من `tokens`/`bank`. **حدّ العرض المقيس:** نافذة إطار الرياضيات تسع معادلة من **خمسة رموز** في سطر واحد، وما زاد يلتفّ سطرين تلقائياً بلا فيض. انظر السؤال التجريبي في `js/questions.js` تحت `"tst-math-types"`.

---

## ⑳ خط الأعداد — `number-line`
**نوع خاصّ بالرياضيات** — فئته **أساسية**.
خطّ أعداد مرسوم SVG بالكامل: محور وتدريج وأرقام ومؤشّر. التدريج يُضبط بـ`min` و`max` و`step`، و`labelEvery` = **كل كم خطوةٍ تحمل العلامةُ رقماً** (مثلاً `step:1` و`labelEvery:5` ← تدريجة عند كل عدد ورقم عند كل خمسة).

**التفاعل:** نقر أيّ موضع على لوح الرسم أو سحب المؤشّر إليه (فأرة + لمس معاً)، والمؤشّر **يلتقط أقرب تدريجة** ويعرض قيمتها بأرقام كبيرة — فمسّة غير دقيقة تُرى فوراً وتُصحَّح بمسّة أخرى قبل الضغط على «تحقّق».

**الأوضاع الثلاثة (`mode`):**
- **`place`** — وضع عدد على الخط: الهدف `target`، والتحقّق `|القيمة − target| ≤ tolerance` مع تلميح «قبل/بعد المطلوب».
- **`jump`** — القفز بالعدّ: `jump:{start,size,count}`. ينقر الطالب موضع **كل قفزة بدورها**، فيُرسم قوس بسهم ومقدار القفزة عند كل نقرة صحيحة ويتقدّم عدّاد «القفزات ٢ من ٤»؛ والنقرة الخاطئة تُعطي تغذية الخطأ بلا قوس. يكتمل السؤال بآخر قفزة — **فلا زرّ «تحقّق» في هذا الوضع**، زرّ «إعادة» وحده (كنمط `memory` و`exclude`).
- **`round`** — التقريب لأقرب عشرة: `target` هو العدد المطلوب تقريبه ويُعلَّم **ثابتاً على الخط بلون مميّز** ليقارن الطالب بُعده عن العشرتين، و`roundTo` مقدار التقريب (الافتراضي `10`). الصواب = `Math.round(target/roundTo)*roundTo`.

| الحقل | الوصف |
|------|------|
| `mode` | `"place"` (الافتراضي) أو `"jump"` أو `"round"` |
| `min` / `max` | طرفا الخط |
| `step` | المسافة بين تدريجتين (الافتراضي 1) — وهي **وحدة الالتقاط** |
| `labelEvery` | كل كم خطوةٍ تحمل العلامةُ رقماً (الافتراضي 1 = كل التدريجات) |
| `target` | العدد المطلوب — في `place` موضعه، وفي `round` العدد المراد تقريبه |
| `jump` | `{ start, size, count }` — لوضع `jump` فقط |
| `roundTo` | مقدار التقريب في وضع `round` (الافتراضي 10) |
| `tolerance` | (اختياري) مساحة الخطأ المسموح بوحدة القيمة (الافتراضي 0) |

```js
{
  type: "number-line",
  mode: "place",
  objective: "2Nn10: يضع الأعداد حتى ١٠٠ على خط الأعداد",
  level: "application",
  prompt: "ضَعِ المُؤشِّرَ على العَدَدِ ١٣ في خَطِّ الأعدادِ.",
  min: 0, max: 20, step: 1, labelEvery: 5,
  target: 13
}
```

> **حدّ مقيس يخصّ التأليف:** عرض لوح الرسم **451px تصميميّ** (النافذة لا تتّسع أكثر — انظر `shoogp-ui`)، فتباعد التدريجات = `451 ÷ عدد الفترات`. عند **٢٠ فترة** التباعد **20.3px تصميميّ (19.1px معروض على 1920×1080)**. لذلك: **لا تتجاوز ~٢٠ فترة في السؤال الواحد**، واستعمل `tolerance: 1` مع التدريج الكثيف. المؤشّر **مؤشّر عرض لا مقبض** (اللمس على اللوح كلّه) فلا يسري عليه حدّ 60px — كـ`.hs-mark` في «تحديد الأجزاء».
> لا يحتاج النوع صورة ولا برومبت رسم. انظر السؤال التجريبي في `js/questions.js` تحت `"tst-math-types"`.

> **⏳ توحيد مؤجَّل (قرار مالك):** `number-line` و**`slider` (⑯)** يتقاسمان الأساس نفسه — شريط أفقيّ بتدريج ومؤشّر يُسحب — لكنّهما **محرّكان منفصلان اليوم**: `slider` مبنيّ على عناصر DOM (‏`.sld-track`)، و`number-line` مبنيّ على رسم SVG ومحرّك التدريج المشترك (`scaleTicks`/`svgPointer`). **يُؤجَّل توحيدهما في محرّك واحد إلى دفعة تنظيف `zoom-reveal`** (انظر «قرار إلغاء» في `question-types.md`)، فيُنجَز التنظيفان معاً في طلب مستقلّ. **حتى ذلك الحين لا تُبنى أوضاع جديدة في أيٍّ منهما اعتماداً على الآخر**، وصيغة بيانات كلٍّ منهما مستقلّة كما هي موثّقة هنا.

---

## ㉑ لوحة المائة — `hundred-chart`
**نوع خاصّ بالرياضيات** — فئته **أساسية**.
شبكة أعداد مرسومة SVG من `from` إلى `to` بعدد أعمدة `columns` (افتراضياً **١٠**)، تُملأ **من اليمين إلى اليسار** باتجاه القراءة العربية. النقر يلوّن الخلية، و**التحقّق يقع على مجموعة الخلايا لا على خلية واحدة**: لا يفوز الطالب بخلية صحيحة مع نقصٍ أو زيادة، ورسالة الخطأ تفصّل «الصحيح ٣ من ١٠، ولديك ١ خلية زائدة».

**الأوضاع الثلاثة (`mode`):**
- **`multiples`** — تلوين مضاعفات العدد `multiple`. الصواب = كل مضاعفاته داخل المدى.
- **`missing`** — خلايا ناقصة تُملأ من **بنك بطاقات أعداد**: ينقر الطالب البطاقة ثم الخلية الناقصة (أو يسحبها إليها)، ونقر الخلية الممتلئة **بلا بطاقة مختارة** يفرّغها. الخلايا المطبوعة ليست أهدافاً فنقرها لا يفعل شيئاً. الخلية الناقصة **غائرة بحدّ متقطّع** والبطاقة بارزة (قاعدة المحمول ≠ المستقبِل).
- **`more-less`** — إيجاد الأكثر أو الأقل بمقدار ١ أو ١٠: `asks[{base,delta}]`. تُعلَّم خلايا `base` بلون مميّز كنقاط انطلاق، ويُبنى **شريط مطالب** تلقائياً («٣٤ ← أكثرُ بـ١٠») فلا يحتاج الأمر شرحاً شفهياً. الصواب = `base + delta` لكل مطلوب.

| الحقل | الوصف |
|------|------|
| `mode` | `"multiples"` (الافتراضي) أو `"missing"` أو `"more-less"` |
| `from` / `to` | أول عدد وآخره في الشبكة (١ و١٠٠ لللوحة الكاملة) |
| `columns` | عدد الأعمدة (الافتراضي 10) |
| `multiple` | العدد المطلوب تلوين مضاعفاته — لوضع `multiples` |
| `missing[]` | الأعداد التي تُترك خلاياها فارغة — لوضع `missing` |
| `distractors[]` | (اختياري) أعداد مموّهة تُضاف لبنك وضع `missing` |
| `asks[]` | `{ base, delta }` — لوضع `more-less`؛ `delta` موجب = أكثر، سالب = أقل |

```js
{
  type: "hundred-chart",
  mode: "multiples",
  objective: "2Nn4: يعدّ قفزاً بالخمسات حتى ٥٠",
  level: "reasoning",
  prompt: "لَوِّنْ مُضاعَفاتِ العَدَدِ ٥ في اللَّوحةِ.",
  from: 1, to: 50, columns: 10,
  multiple: 5
}
```

> **حدّ مقيس يخصّ التأليف:** عرض اللوح **~600px تصميميّ**، فحجم الخلية = `600 ÷ columns`. عند **١٠ أعمدة** تكون الخلية **60px تصميميّاً** — أي **دون الحدّ التصميميّ 78px** (انظر `shoogp-ui` §٤‑ج). فإن كان الدرس للصفّ الأول أو الثاني ويحتاج خلايا أكبر، **قلّل الأعمدة** (٦ أعمدة ← خلية 100px) أو قلّل المدى؛ ونقر الخلية الخاطئة **قابل للتراجع بنقرة ثانية** قبل «تحقّق» فلا تُفقَد إجابة بخطأ لمس.
> لا يحتاج النوع صورة ولا برومبت رسم. انظر السؤال التجريبي في `js/questions.js` تحت `"tst-math-types"`.

---

## ㉒ المصفوفات — `array`
**نوع خاصّ بالرياضيات** — فئته **أساسية**. يمهّد للضرب عبر الجمع المتكرر والتنظيم البصري.
مصفوفة `rows` صفوفاً × `cols` أعمدة، مرسومة SVG وتُملأ **من اليمين إلى اليسار** باتجاه القراءة.

**الوضعان (`mode`):**
- **`build`** — يبني الطالب الشبكة بالنقر على **لوح فارغ أوسع من المطلوب** (الافتراضي `rows+2 × cols+2` بحدّ أقصى ٨×٨، ويُضبط بـ`grid:{rows,cols}`). الخانة الفارغة **غائرة بحدّ متقطّع**، وعند اختيارها **يصير الحدّ صلباً ويظهر القرص** — فارق شكليّ مع اللون لا لون فحسب. عدّاد «اخترتَ ٧ مربّعاً» يتقدّم مع كل نقرة، والنقرة الثانية تلغي الاختيار.
  **التحقّق ثلاثيّ التدرّج:** شكل غير مستطيل ← «يجب أن تكون مستطيلاً كاملاً بلا فراغات»؛ أبعاد معكوسة (`cols × rows`) ← «الصفوف والأعمدة معكوسة» (تمييز تربويّ مقصود بين ٣×٤ و٤×٣)؛ أبعاد أخرى ← «بنيت ٢×٥ والمطلوب ٣×٤». وعند الصواب **تُكشف `answerSentence`** تحت اللوح.
- **`read`** — تُعرض المصفوفة مبنيّةً ويختار الطالب جملتها من `options` (تُخلط تلقائياً)، والصواب هو الخيار **المطابق نصّاً لـ`answerSentence`** (لا فهرس، فلا يختلّ الربط إذا أُعيد ترتيب الخيارات). الخيارات `.opt` المعتادة فترث قشرة المادة وحدّها الأدنى.

| الحقل | الوصف |
|------|------|
| `mode` | `"build"` (الافتراضي) أو `"read"` |
| `rows` / `cols` | عدد الصفوف وعدد ما في كل صفّ |
| `answerSentence` | جملة الضرب أو الجمع المتكرر — تُكشف عند الصواب في `build`، **وهي الخيار الصحيح** في `read` |
| `options[]` | خيارات وضع `read` (يجب أن يكون فيها `answerSentence` حرفياً) |
| `grid` | (اختياري) `{ rows, cols }` مقاس لوح البناء (الافتراضي `rows+2 × cols+2`، بحدّ أقصى ٨) |

```js
{
  type: "array",
  mode: "build",
  objective: "2Nc16: يفهم الضرب على أنه جمع متكرر ومصفوفة",
  level: "application",
  prompt: "ابْنِ مَصفوفةً فيها ٣ صُفوفٍ، في كُلِّ صَفٍّ ٤ مُربَّعاتٍ.",
  rows: 3, cols: 4,
  answerSentence: "٣ × ٤ = ١٢"
}
```

> **مقيس:** خانة لوح البناء **86.7px تصميميّ** لهدف اللمس و**78px** للوجه المرئيّ — **فوق الحدّ التصميميّ**، لأن اللوح محدود بـ`max-width:520px` فلا تصغر خاناته باتّساع النافذة. لا يحتاج النوع صورة ولا برومبت رسم. انظر السؤال التجريبي في `js/questions.js` تحت `"tst-math-types"`.

---

## ㉓ المقارنة — `compare`
**نوع خاصّ بالرياضيات** — فئته **أساسية**. يبني معنى الترتيب والمقارنة بين الأعداد ورمزَيها.
صفٌّ (أو صفوف) فيه عددان وخانةٌ بينهما، وبطاقاتُ الرموز الثلاثة `<` و`=` و`>` أسفلَه.

**قواعد بنيويّة ثلاث:**
- **الرمز الصحيح يُحسب من العددين ولا يُؤلَّف** — فلا يُكتب في البيانات أصلاً، ولا يمكن أن يخطئ المؤلِّف فيه.
- **الصفّ يُعرض بترتيب رياضيّ (يسار→يمين)** داخل الصفحة RTL: `a` يساراً و`b` يميناً، فيصحّ معنى «الرمزُ يفتح فمه نحو الأكبر».
- **رمزا `<` و`>` قابلان للانعكاس البِدِيّ** فيُعزلان في سياق LTR — وهذا **شرطُ صحّةٍ لا تجميل**: بغيره يُقلب الرمز بصرياً فيصير المعنى ضدّه.

بطاقاتُ الرموز **لا تُستهلك** (الرمز يصلح لعدّة صفوف)، والبطاقة تحمل الرمز وكلمتَه («أصغرُ من»/«يساوي»/«أكبرُ من») فتُبنى المفردةُ مع الرمز. السحب واللمس والنقر كلُّها تعمل، ونقرُ الخانة الممتلئة بلا بطاقةٍ مختارة يفرّغها.

| الحقل | الوصف |
|------|------|
| `pairs[]` | `{ a, b, unit? }` لكل صفّ — عددان يُقارنان (والوحدة اختياريّة تُعرض بعد العددين) |
| `unit` | (اختياري) وحدة عامّة لكل الصفوف (مثل `"سم"`) |

```js
{
  type: "compare",
  objective: "2Nn12: يقارن الأعداد ويرتّبها ويستخدم الرموز < و> و=",
  level: "knowledge",
  prompt: "ضَعِ الرَّمْزَ المُناسِبَ بَيْنَ كُلِّ عَدَدَيْنِ.",
  pairs: [ { a: "٣٤", b: "٤٣" }, { a: "٧٠", b: "٧٠" }, { a: "٥٨", b: "٤٩" } ]
}
```

> **مقيس:** الخانة والبطاقة ≥60px في البعدين، والعدد في كبسولة بيضاء عرضها ≥112px. لا يحتاج النوع صورة ولا برومبت رسم.

---

## ㉔ إكمال النمط — `pattern`
**نوع خاصّ بالرياضيات** — فئته **أساسية**. يكشف قاعدة التغيّر في متتابعة عدديّة أو شكليّة.
شريطٌ من العناصر بينها أسهمُ اتّجاه التتابع، وفيه خاناتٌ ناقصة تُملأ من بنك البطاقات.

**الاتّجاه:** الشريط يُقرأ **يميناً→يساراً كنصّ الصفحة** والسهم «←»؛ و`ltr:true` يقلبه إلى يسار→يمين مع السهم «→» (لمن أراد صورة الكتاب اللاتينيّة).

**المقارنة مزدوجة:** الأعداد تُقارن **قيمةً** (فالهنديّة واللاتينيّة سواء) وغيرها **نصّاً** — فيصحّ النمط بالأشكال والرموز (🔺🟦) كما يصحّ بالأعداد. منطق البنك هو **منطق `fill-blank` نفسه** (`wireBank`) فالبطاقة تُشطب بهويّتها لا بنصّها، وتصحّ القيم المكرّرة. و`rule` **تُكشف عند الصواب فقط** (كجملة المصفوفة) فلا تُفسد التفكير.

| الحقل | الوصف |
|------|------|
| `items[]` | عناصر النمط بالترتيب، و`"__"` لكل خانة ناقصة |
| `answers[]` | صواب الخانات **بترتيب ظهورها** |
| `bank[]` | (اختياري) بطاقات البنك — والافتراضي `answers` + `distractors` |
| `distractors[]` | (اختياري) بطاقات مشوّشة تُضاف إلى البنك |
| `rule` | (اختياري) نصّ القاعدة — يُكشف عند الصواب |
| `ltr` | (اختياري) `true` يعكس اتّجاه الشريط والسهم |

```js
{
  type: "pattern",
  objective: "2Nn4: يعدّ بالاثنينات والخمسات والعشرات ويكمل الأنماط العدديّة",
  level: "application",
  prompt: "أكْمِلِ النَّمَطَ: العَدُّ بِالخَمْساتِ.",
  items: ["٥", "١٠", "__", "٢٠", "__", "٣٠"],
  answers: ["١٥", "٢٥"],
  distractors: ["١٨", "٢٢"],
  rule: "نَزيدُ ٥ كُلَّ مَرَّةٍ"
}
```

> **مقيس:** بطاقة الشريط والخانة ≥84×66px، وبطاقة البنك ≥78×62px. لا يحتاج النوع صورة ولا برومبت رسم.

---

## ㉕ العد بالنقر — `count-tap`
**نوع خاصّ بالرياضيات** — فئته **أساسية**. يجعل العدَّ **مرئيّاً** لا خفيّاً: العدد يظهر على العنصر المنقور.
عناصرُ (رموزٌ نصّيّة/إيموجي) تُنقر واحداً واحداً فتحمل رقمَها بترتيب النقر، وعدّادٌ فوقها يتقدّم.

**الوضعان (`mode`):**
- **`each`** — عناصر مفردة عددها `count`، والمطلوب نقرُ `target` منها (والافتراضي عدّها كلّها). فيُقاس العدُّ الحقيقيُّ لا الترتيبُ: العناصر أكثر من المطلوب.
- **`step`** — `groups` مجموعةً في كلٍّ منها `step` عنصراً، فتُعَدُّ بالقفز: نقرُ المجموعة يزيد المجموعَ بمقدار `step`، والشارة تحمل **المجموع التراكميّ** (٥، ١٠، ١٥…) لا رقمَ المجموعة.

إعادةُ النقر تُلغي العدَّ وتُعيد الترقيم، والتحقّق يقارن المجموع بـ`target` ويصف الفرق («عددتَ ٥ — المطلوب ٧، تابع العدّ»).

| الحقل | الوصف |
|------|------|
| `mode` | `"each"` (الافتراضي) أو `"step"` |
| `glyph` | الرمز المعدود (إيموجي أو حرف) — الافتراضي 🔵 |
| `count` | عدد العناصر المعروضة في `each` |
| `step` / `groups` | مقدار القفزة وعدد المجموعات في `step` |
| `target` | المطلوب عدُّه (الافتراضي: `count` أو `step×groups`) |

```js
{
  type: "count-tap",
  objective: "2Nn2: يعدّ الأشياء حتى ١٠٠ بالتجميع",
  level: "knowledge",
  prompt: "انْقُرْ ٧ تُفّاحاتٍ وعُدَّها.",
  mode: "each", glyph: "🍎", count: 12, target: 7
}
```

> **مقيس:** كل عنصر ≥66×66px، والشارة قرصٌ قطره 30px على الحافّة الخارجيّة فلا تحجب الرمز. لا يحتاج النوع صورة ولا برومبت رسم.

---

## ㉖ القيمة المنزلية بالمكعبات — `place-value`
**نوع خاصّ بالرياضيات** — فئته **أساسية**. يجسّد بناء العدد من عشراتٍ وآحادٍ بالقياس لا بالكلام.
لوحٌ من ساحتَين — **عشرات** و**آحاد** — بترتيب المنازل (`direction:ltr`: العشرات يساراً والآحاد يميناً كما يُكتب العدد).

**العلاقة مرئيّة:** المكعّب وحدةُ الرسم (٢٢ في فضاء الـviewBox) و**العشرة عمودٌ من عشرة مكعّبات بالطول نفسه**، فيرى الطالب أن العشرة عشرُ آحاد. وكلُّ قطعة في غلافٍ لمسُه ≥62px والرسم داخله بقياسه الحقيقيّ.

**الوضعان (`mode`):**
- **`build`** — يبني الطالب `target` بزرَّي «＋ عشرة» و«＋ واحد»، **ونقرُ القطعة يحذفها**. وقراءةٌ حيّةٌ تحت اللوح: «٤ عشرات و٧ آحاد = ٤٧» بصيغ المثنّى والجمع الصحيحة.
- **`read`** — تُعرض `tens` و`ones` مرسومةً فيختار الطالب العدد من `options`، وتُكشف القراءة عند الصواب.

**التحقّق يشترط الصورة القانونيّة:** إن ساوت القيمةُ الهدفَ والآحادُ ≥١٠ فالرسالة «القيمةُ صحيحةٌ، لكن بدّل عشرَ آحادٍ بعشرةٍ واحدة» — إلّا إذا نُصَّ `allowExchange:true` لدروس التبديل.

| الحقل | الوصف |
|------|------|
| `mode` | `"build"` (الافتراضي) أو `"read"` |
| `target` | العدد المطلوب بناؤه في `build` |
| `tens` / `ones` | القطع المعروضة في `read` |
| `options[]` | خيارات `read` (الصواب = `tens×10+ones` حساباً) |
| `maxTens` / `maxOnes` | حدّ الإضافة — الافتراضي: العشرات ⌊الهدف÷١٠⌋ (عشراتُ الهدف بالضبط) والآحاد ١٠ (تتيح لحظة «بدّل عشر آحاد بعشرة») |
| `allowExchange` | (اختياري) `true` يقبل الصور غير القانونيّة ذاتَ القيمة نفسِها |

```js
{
  type: "place-value",
  objective: "2Nn3: يعرف القيمة المنزلية للعشرات والآحاد في الأعداد حتى ١٠٠",
  level: "knowledge",
  prompt: "ابْنِ العَدَدَ ٤٧ بِالعَشَراتِ والآحادِ.",
  mode: "build", target: 47
}
```

> **مقيس:** غلاف القطعة ≥62px، وعمود العشرة 21×180px والمكعّب 21×21px (مُصغَّران بنسبة واحدة فتبقى «العشرة = عشر آحاد» بالقياس) — والغلاف الشفّاف هو هدف اللمس لا الرسم. **وساحتا اللوح في `build` محجوزتان عرضاً وارتفاعاً لأقصى حالة يسمح بها الحدّان منذ البناء** (الحساب بأسوأ الحالات — `shoogp-ui` §١.٤ب) فلا يكبر السؤال أثناء الإجابة ولا يخرج من إطاره. لا يحتاج النوع صورة ولا برومبت رسم.

---

## ㉗ الساعة التفاعلية — `clock`
**نوع خاصّ بالرياضيات** — فئته **أساسية**. قراءةُ الوقت وضبطُه وحسابُ الزمن المنقضي على ميناءٍ حقيقيّ.
ميناءٌ SVG فيه ٦٠ علامةَ دقيقةٍ و١٢ رقمَ ساعةٍ بالأرقام الهنديّة، وعقربانِ: قصيرٌ للساعات وطويلٌ للدقائق.

**الأوضاع الثلاثة (`mode`):**
- **`read`** — الميناء يُظهر `target` فيقرؤه الطالب ويختار من `options`، و**الأوقات تُقارن تحليلاً لا نصّاً** (فـ`"٣:٣٠"` و`"3:30"` سواء).
- **`set`** — يضبط الطالب العقربين إلى `target` بدءاً من `from`، والدقائق **تلتقط إلى `snap`** (٥ افتراضاً) فلا تقع بين علامتين.
- **`elapsed`** — يبدأ من `start` ويُضيف `minutes`، **والهدف يُحسب حساباً** (وقت الطهي ونحوه)، وشريطٌ فوق الميناء يذكّر بالبداية والمقدار.

**عقربُ الساعات يتقدّم بكسر الساعة تلقائياً** (M×٠٫٥ درجة) فلا يكذب الميناء عند «٧:١٥». واللمس بهدفَين واسعين لا بمقبضَين دقيقين: **الحلقة الخارجيّة (نصف القطر ≥١٠٠) للدقائق والوسط للساعات**، وقراءةٌ حيّةٌ «٧:١٥» معزولةٌ في سياق LTR فلا تُقلب النقطتان موضعَ الساعة والدقيقة.

| الحقل | الوصف |
|------|------|
| `mode` | `"read"` (الافتراضي) أو `"set"` أو `"elapsed"` |
| `target` | الوقت المطلوب بصيغة `"H:MM"` (في `read` و`set`) |
| `options[]` | خيارات `read` (نصوص أوقات) |
| `from` | (اختياري) موضع البداية في `set` — الافتراضي `"12:00"` |
| `start` / `minutes` | البداية والمقدار المُضاف في `elapsed` |
| `snap` | (اختياري) دقّة ضبط الدقائق — الافتراضي ٥ |

```js
{
  type: "clock",
  objective: "2Mt4: يقيس الزمن بالدقائق ويحسب الزمن المنقضي",
  level: "application",
  prompt: "يَبْدَأُ الطَّهْيُ السّاعَةَ ٤:٠٠ ويَسْتَمِرُّ ٤٥ دَقيقَةً — اضْبِطْ وَقْتَ الانْتِهاءِ.",
  mode: "elapsed", start: "٤:٠٠", minutes: 45
}
```

> **مقيس:** الميناء `min(360px, 86vw)`، ورقمُ الساعة 34px تصميميّ، والعقربان 14px و9px سماكةً — ومساحة اللمس هي لوح الرسم كلُّه. لا يحتاج النوع صورة ولا برومبت رسم.

---

## ㉘ قراءة أداة القياس — `measure-tool`
**نوع خاصّ بالرياضيات** — فئته **أساسية**. قراءةُ المسطرة والميزان وضبطُهما — عمودُ وحدة القياس.
أداتان على **محرّك التدريج المشترك** نفسِه (`scaleTicks`/`scalePos`/`scaleVal`): مسطرةٌ خطّيّةٌ وميناءُ ميزانٍ قوسُه ٢٤٠ درجة.

**الأوضاع الأربعة (`mode`):**
- **`ruler-read`** — جسمٌ ممتدٌّ من الصفر بطول `value` فيقرأ الطالب طولَه ويختار من `options`.
- **`ruler-set`** — يمدّ الطالب الجسم حتى `target` — **رسمُ طولٍ مطلوبٍ** لا قراءتُه.
- **`scale-read`** — مؤشّر الميزان عند `value` فيقرأ الكتلة ويختار من `options`.
- **`scale-set`** — يُدير الطالب المؤشّر إلى `target`.

القيمة **تلتقط إلى `step`** فلا تقع بين تدريجتين، و`labelEvery` يحكم أيَّ تدريجةٍ تحمل رقماً — فيُقاس **العدُّ على التدريج** لا مجرّد النظر إلى الرقم. ورسائل الإخفاق تربويّة لا حُكميّة: «ابدأْ من الصفر وعُدَّ التدريجات حتى طرف الجسم».

| الحقل | الوصف |
|------|------|
| `mode` | `"ruler-read"` (الافتراضي) أو `"ruler-set"` أو `"scale-read"` أو `"scale-set"` |
| `min` / `max` | مدى التدريج (المسطرة تبدأ من الصفر؛ الافتراضي ٢٠ للمسطرة و١٠٠٠ للميزان) |
| `step` | خطوة التدريج (الافتراضي ١ للمسطرة و٥٠ للميزان) |
| `labelEvery` | كل كم خطوةٍ تحمل التدريجةُ رقماً (الافتراضي ٥ للمسطرة و٢ للميزان) |
| `value` | القراءة المعروضة في وضعَي `read` (وهي الصواب) |
| `target` | القيمة المطلوبة في وضعَي `set` |
| `options[]` | خيارات وضعَي `read` |
| `unit` | وحدة العرض (`"سم"`، `"غرام"`، `"مل"`…) |

```js
{
  type: "measure-tool",
  objective: "2Ml1: يقيس الأطوال بالسنتيمتر ويقرأ المسطرة",
  level: "knowledge",
  prompt: "كَمْ طولُ الشَّريطِ بِالسَّنْتيمِتْراتِ؟",
  mode: "ruler-read", max: 20, step: 1, labelEvery: 5, value: 12,
  unit: "سم", options: ["١٢", "١٠", "١٤", "٢٠"]
}
```

> **مقيس:** المسطرة `min(880px, 94%)` والميناء `min(400px, 86vw)`، ورقم التدريج 30px تصميميّ (26px على الميناء لتفادي التزاحم). لا يحتاج النوع صورة ولا برومبت رسم.

---

## ㉙ النقود العُمانية — `money`
**نوع خاصّ بالرياضيات** — فئته **أساسية**. تكوينُ مبلغٍ وعدُّه وحسابُ الباقي بالعملة العُمانيّة.
**كلّ القيم بالبيسة** (١ ر.ع = ١٠٠٠ بيسة) فالحساب صحيحٌ بلا كسورٍ عشريّة، **والعرض يُنسَّق تلقائياً** («٣٥٠ بيسة»، «١ ر.ع»، «١ ر.ع و٢٠٠ بيسة»).

**التمييز شكليّ لا لونيّ فحسب:** ما كان ≤١٠٠ بيسة يُرسم **قرصاً معدنيّاً**، وما فوقه **ورقةً نقديّة** — والقطع كلُّها **مرسومةٌ SVG داخل الكود، لا صورَ عملةٍ حقيقيّة**.

**الأوضاع الثلاثة (`mode`):**
- **`pay`** — يكوّن الطالب `target` من رفّ النقود `rack`، **ونقرُ القطعة في الصندوق يُرجعها**، والمجموع يُعرض حيّاً.
- **`count`** — قطعٌ معروضةٌ في `items` فيختار مجموعَها من `options`؛ **والمجموع لا يُكشف قبل الجواب** (يُعرض «؟») فلا يُفسد السؤال، ثم يظهر عند الصواب.
- **`change`** — `price` و`paid` **والهدف = الفرق حساباً**، وشريطٌ يبيّن الثمن والمدفوع.

| الحقل | الوصف |
|------|------|
| `mode` | `"pay"` (الافتراضي) أو `"count"` أو `"change"` |
| `target` | المبلغ المطلوب بالبيسة (في `pay`) |
| `price` / `paid` | الثمن والمدفوع بالبيسة (في `change`) |
| `items[]` | القطع المعروضة بالبيسة (في `count`) |
| `options[]` | خيارات `count` (يُقرأ منها العدد، فتصحّ «٣٥٠ بيسة») |
| `rack[]` | (اختياري) فئات الرفّ — الافتراضي `[5,10,25,50,100,200,500,1000]` |
| `maxPieces` | (اختياري) حدّ عدد القطع (لأسئلة «أقلّ عددٍ من القطع») |

```js
{
  type: "money",
  objective: "2Mm2: يكوّن مبالغ مالية بعملات مختلفة ويحسب مجموعها",
  level: "application",
  prompt: "ثَمَنُ البَسْكَويتِ ٣٥٠ بَيْسَةً — كَوِّنِ المَبْلَغَ.",
  mode: "pay", target: 350, rack: [25, 50, 100, 200, 500]
}
```

> **مقيس:** كل قطعة زرٌّ ≥66px، والقرص 72×72px والورقة 116×67px. لا يحتاج النوع صورة ولا برومبت رسم.

---

## ㉚ خط التماثل — `symmetry`
**نوع خاصّ بالرياضيات** — فئته **أساسية**. يميّز خطَّ التماثل ويُكمل الصورةَ المرآتيّة.

**الوضعان (`mode`):**
- **`line`** — شكلٌ (من مكتبة الأشكال بـ`shape`، أو مسارٍ حرٍّ بـ`path` في فضاء ٢٠٠×٢٠٠) وعليه خطوطٌ مرشَّحةٌ في `lines[]` لكلٍّ منها `ok`، فينقر الطالب ما يراه خطَّ تماثل. **والتحقّق على المجموعة** (لا تكفي إصابةٌ مع نقصٍ أو زيادة) — وبهذا يصحّ الشكلُ ذو خطَّي تماثلٍ أو أكثر. خطُّ اللمس شفّافٌ عريضٌ (٢٦ وحدة) فوق الخطّ المرئيّ الرقيق فيكون الهدفُ واسعاً على السبورة.
- **`mirror`** — شبكةُ `rows`×`cols` ومحورٌ في الوسط (`axis:"v"` عموديّ الافتراضيّ أو `"h"` أفقيّ)، وخلايا `shape` معطاةٌ على أحد الجانبين (ثابتةٌ لا تُعدَّل) فيُكمل الطالب صورتها المرآتيّة. **المقابلُ يُحسب حساباً** (`cols-1-c` أو `rows-1-r`) فلا يُؤلَّف ولا يُخطئ فيه المؤلِّف. أعمدةُ الشبكة تُملأ **من اليمين إلى اليسار** كسائر أنواع الرياضيات.

**مكتبة الأشكال (`shape`):** `square`، `rect`، `triangle`، `circle`، `rhombus`، `hexagon`، `heart`.

| الحقل | الوصف |
|------|------|
| `mode` | `"line"` (الافتراضي) أو `"mirror"` |
| `shape` | في `line`: اسمٌ من مكتبة الأشكال. في `mirror`: `[[r,c], …]` الخلايا المعطاة |
| `path` | (اختياري، `line`) مسار SVG حرٌّ في فضاء ٢٠٠×٢٠٠ يتقدّم على `shape` |
| `lines[]` | (`line`) `{ x1, y1, x2, y2, ok }` لكل خطٍّ مرشَّح في فضاء ٢٠٠×٢٠٠ |
| `rows` / `cols` | (`mirror`) مقاس الشبكة — الافتراضي ٦×٦ |
| `axis` | (اختياري، `mirror`) `"v"` (الافتراضي) أو `"h"` |

```js
{
  type: "symmetry",
  objective: "2Gs3: يتعرّف على خطوط التماثل في الأشكال الثنائية الأبعاد",
  level: "reasoning",
  prompt: "انْقُرْ خُطوطَ التَّماثُلِ في المُسْتَطيلِ.",
  mode: "line", shape: "rect",
  lines: [
    { x1: 100, y1: 35, x2: 100, y2: 165, ok: true },
    { x1: 15,  y1: 100, x2: 185, y2: 100, ok: true },
    { x1: 25,  y1: 45,  x2: 175, y2: 155, ok: false }
  ]
}
```

> **مقيس:** لوح الشكل `min(380px, 84vw)` والشبكة `min(520px, 90vw)` — وخانة الشبكة ٨٦.٧px تصميميّ في شبكة ٦ أعمدة. لا يحتاج النوع صورة ولا برومبت رسم.

---

## ㉛ قراءة التمثيل البياني — `chart-read`
**نوع خاصّ بالرياضيات** — فئته **أساسية**. قراءةُ تمثيلٍ بالأعمدة ومقارنةُ فئاته وبناؤه.
الأعمدةُ من `series`، والتدريجُ من `step` و`max` (**ويُحسب `max` تلقائياً** من أكبر قيمةٍ إن غاب).

**الاتّجاه:** المحورُ الرأسيُّ **يمينَ الرسم** والفئاتُ من اليمين إلى اليسار باتّجاه القراءة العربيّ — كمحرّك الشبكة المشترك، فلا يختلف الرسم عن سائر أنواع الرياضيات.

**الأوضاع الثلاثة (`mode`):**
- **`read`** — يقرأ الطالب الرسم ويختار من `options`، والصواب `answer`.
- **`tap`** — ينقر **العمودَ** الذي يحقّق المطلوب (الأكثر/الأقلّ/عددٌ معيّن) و`answer` عنوانُه؛ ومساحةُ اللمس عمودُ الفئة كلُّه من القاعدة إلى أعلى الرسم لا المستطيلُ المرئيُّ وحدَه.
- **`build`** — أعمدةٌ فارغةٌ يضبط الطالب ارتفاعَها لتطابق `series`، والأعدادُ المطلوبة في شريطٍ فوق الرسم؛ والنقرُ (أو السحب) في عمود الفئة يضبط ارتفاعَها إلى أقرب تدريجة، والقيمةُ تُكتب على رأس العمود.

| الحقل | الوصف |
|------|------|
| `series[]` | `{ label, value }` لكل فئة — بالترتيب من اليمين |
| `mode` | `"read"` (الافتراضي) أو `"tap"` أو `"build"` |
| `answer` | الصواب: نصُّ الخيار في `read`، وعنوانُ العمود في `tap` |
| `options[]` | خيارات `read` |
| `step` / `max` | خطوة التدريج (الافتراضي ١) وحدُّه الأعلى (يُحسب تلقائياً إن غاب) |
| `yTitle` | (اختياري) عنوان المحور الرأسيّ (مثل `"عددُ التلاميذ"`) |

```js
{
  type: "chart-read",
  objective: "2Dh1: يقرأ التمثيلات البيانية بالأعمدة ويجيب عن أسئلة عنها",
  level: "reasoning",
  prompt: "أيُّ الفاكِهَةِ اخْتارَها أكْثَرُ التَّلاميذِ؟",
  mode: "tap", answer: "الموز", yTitle: "عددُ التلاميذ",
  series: [
    { label: "التفاح", value: 4 }, { label: "الموز", value: 7 },
    { label: "البرتقال", value: 5 }, { label: "العنب", value: 3 }
  ]
}
```

> **مقيس:** لوح الرسم `min(880px, 96%)`، وعرضُ العمود ٦٢٪ من عرض خانة الفئة بحدٍّ أقصى 96 وحدة، وأرقامُ التدريج والعنوانُ 28px تصميميّ. لا يحتاج النوع صورة ولا برومبت رسم.

---

## ㉜ التشكيل — `tashkeel`
كلمةٌ تُعرَض **حروفُها منفصلةً**، وفوقَ كلِّ حرفٍ يحتاجُ حركةً **خانةٌ فارغة**. الطالبُ **ينقرُ الحركةَ** في البنكِ ثم **ينقرُ الحرفَ** فتستقرُّ فوقَه (نمطُ لوحةِ ألوانِ `color` لا السحب، لأنّ **بطاقةَ الحركةِ لا تُستهلَك** — الفتحةُ الواحدةُ قد تلزمُ ثلاثةَ حروف). ونقرُ الحرفِ بالحركةِ نفسِها **يمحوها** فلا يحتاجُ الطفلُ زرَّ تصحيح. عند التحقّق: الخانةُ الصحيحةُ خضراءُ والخاطئةُ حمراء، وعند اكتمالِ الصواب **تُكشَفُ الكلمةُ متّصلةً مشكولة**.

**البياناتُ حقلٌ واحدٌ فقط:** تُكتَبُ الكلمةُ **مشكولةً كاملةً**، والنوعُ **يقشّرُ حركاتِها برمجياً** فيبني الخاناتِ والأجوبةَ والبنك. فلا جدولَ أجوبةٍ منفصلٌ يتعارضُ مع الكلمة.

| الحقل | الوصف |
|------|------|
| `word` | ✅ الكلمةُ **مشكولةً كاملةً** (مثل `"كَتَبَ"`). كلُّ حرفٍ يحملُ حركةً تصيرُ فوقَه خانة، والحرفُ العاري (كألفِ «قال») يبقى بلا خانةٍ فلا يُطالَبُ الطفلُ بما لا حركةَ له. والحركاتُ المتتاليةُ على حرفٍ واحد (الشدّةُ معَ الكسرةِ في «مُعَلِّم») **وحدةٌ واحدةٌ وبطاقةٌ واحدة** — كما تُنطَق |
| `marks[]` | (اختياري) بنكُ حركاتٍ مخصّص. احذفه ليُشتقَّ تلقائياً: حركاتُ الكلمةِ نفسِها، ثم يُكمَّلُ من `َ ُ ِ ْ ّ` حتى **ثلاثِ بطاقاتٍ على الأقلّ** — كي لا يصيرَ السؤالُ بلا اختيارٍ حين تتّحدُ حركاتُ الكلمةِ كلُّها |

```js
{
  type: "tashkeel",
  objective: "يضبطُ أواخرَ الكلماتِ وحركاتِ حروفِها ضبطاً صحيحاً",
  level: "application",
  prompt: "ضعِ الحركةَ الصحيحةَ فوقَ كلِّ حرف.",
  word: "كَتَبَ"
}
```

> **السعةُ المقيسة: حتى ٨ حروف** (‏`يَسْتَخْرِجُونَ` على `qf-l` في منفذَي 1366 و1024). و**الكلمةُ لا تلتفُّ على سطرَين** بحالٍ — الالتفافُ يقطعُ تسلسلَ القراءةِ على الطفل، فالأعمدةُ تتقلّصُ ولا تنكسر.

---

## ㉝ ترتيب كلماتٍ لتكوين جملة — `sentence`
كلماتٌ مبعثرةٌ في بنك، والطالبُ يسحبُ كلَّ كلمةٍ إلى خانتِها بالترتيب (قراءة **يمين→يسار**) لتكوينِ جملةٍ مفيدة. هو **محرّكُ `arrange` نفسُه** بالضبط (`renderTokenOrder`) — الفرقُ أنّ الرمزَ المرتَّبَ **كلمةٌ لا حرف**: نقرُ الخانةِ الممتلئةِ يعيدُ كلمتَها للبنك، ويعملُ بالسحبِ (فأرة + لمس)، وعند التحقّقِ الكلمةُ في مكانِها خضراءُ والخاطئةُ حمراء، وعند اكتمالِ الصواب **يُكشَفُ نصُّ الجملةِ كاملاً**.

| الحقل | الوصف |
|------|------|
| `sentence` | ✅ الجملةُ الصحيحة. تُقسَّمُ على المسافاتِ فتصيرُ كلماتُها خاناتِ الترتيب، ورسالةُ النجاحِ تعرضُها |
| `words[]` | (اختياري) كلماتُ البنك. احذفه لتُشتقَّ من `sentence`. استخدمه لإضافةِ **كلمةٍ دخيلةٍ** أو لدمجِ كلمتَينِ متلازمتَينِ في بطاقةٍ واحدة |

```js
{
  type: "sentence",
  objective: "يرتّبُ كلماتٍ مبعثرةً لتكوينِ جملةٍ مفيدةٍ صحيحةِ التركيب",
  level: "application",
  prompt: "رتّبِ الكلماتِ لتكوينِ جملةٍ مفيدة.",
  sentence: "ذهبَ الولدُ إلى المدرسةِ مبكّراً"
}
```

> **السعةُ المقيسة: حتى ٥ كلمات** على `qf-l` (والستُّ تنقلُ السؤالَ إلى `qf-tall` — وهو إطارٌ حقيقيٌّ مقبولٌ لكنّ البطاقاتِ فيه أضيق). والجملةُ القصيرةُ أوضحُ تربوياً على كلِّ حال.

---

## ㉞ الحروف الشمسية والقمرية — `sun-moon`
كلماتٌ معرَّفةٌ بـ«ال» تُسحَبُ إلى صندوقَين: **شمسية ☀️** و**قمرية 🌙** (صناديقُ `classify` نفسُها، سحبُ فأرةٍ ولمس). وعندَ صحّةِ التصنيفِ كلِّه **تُكشَفُ على كلِّ بطاقةٍ صورةُ نطقِها**: الشمسيةُ بشدّةٍ على الحرفِ بعدَ اللام (`الشّمس`) والقمريةُ بسكونٍ على اللام (`الْقمر`) — فالتغذيةُ الراجعةُ **تعليمُ القاعدةِ** لا حكمٌ بصوابٍ أو خطأٍ فحسب.

**البياناتُ قائمةُ كلماتٍ فقط:** النوعُ **يستنتجُ** شمسيَّةَ الكلمةِ من قمريَّتِها بالحروفِ الأربعةَ عشرَ `ت ث د ذ ر ز س ش ص ض ط ظ ل ن`، فلا يُكتَبُ الجوابُ في البياناتِ ولا يقعُ المؤلِّفُ في خطأِ تصنيفِ حرف.

| الحقل | الوصف |
|------|------|
| `words[]` | ✅ كلماتٌ معرَّفةٌ بـ«ال» **نصّاً مجرّداً** (مثل `["الشمس","القمر"]`). ويجوزُ `{ word, kind }` بـ`kind:"sun"` أو `"moon"` لكلمةٍ شاذّةٍ أو غيرِ مبدوءةٍ بـ«ال» — وهو **استثناءٌ لا أصل** |

```js
{
  type: "sun-moon",
  objective: "يميّزُ اللامَ الشمسيةَ من القمريةِ في الكلماتِ المعرَّفةِ بـ«ال»",
  level: "reasoning",
  prompt: "صنّفِ الكلماتِ بحسبِ لامِها.",
  words: ["الشمس", "القمر", "الرجل", "البيت", "النور", "الكتاب"]
}
```

> **السعةُ المقيسة: حتى ٦ كلمات** (على `qf-tall`، والأربعُ أوسعُ راحةً).

---

## ㉟ توصيل الحرف بالصورة — `letter-picture`
عمودُ **حروفٍ** كبيرةٍ وعمودُ **صور**: يُنقَرُ الحرفُ ثم صورتُه فيُرسَمُ الخطُّ المنحني الواصل (محرّكُ `matching` نفسُه)، ويُنطَقُ الحرفُ ثم الكلمةُ صوتياً. وعندَ الوصلِ الصحيحِ **تظهرُ الكلمةُ تحتَ الصورة** — مخفيّةٌ قبلَه كي تكونَ مكافأةً لا تلميحاً.

**بطاقةُ الصورةِ ليست كبسولةً عن قصد:** الكبسولةُ زخرفةُ نصٍّ لا إطارُ صورة، فلها لوحُها الأبيضُ الهادئُ (`.lpcard`)، وتبقى الكبسولةُ لعمودِ الحروفِ النصّيِّ وحدَه.

| الحقل | الوصف |
|------|------|
| `pairs[]` | ✅ `{ letter, word, art?, svg \| img }` — `letter` الحرفُ المعروضُ في عمودِ الحروف، و`word` الكلمةُ التي تُكشَفُ تحتَ الصورةِ عندَ الوصلِ الصحيح |
| ↳ `art` | (اختياريّ) اسمُ **سُلَّمِ لون** ⇒ يُعرَضُ الحرفُ **رسماً مصمَّماً** بدلَ النصِّ الخام — انظر التوسعةَ أدناه |
| ↳ `svg` | رسمُ SVG **مكتوبٌ معَ السؤال** — وهو **الأصلُ** (قاعدةُ الرسوم في `CLAUDE.md`: كلُّ رسمٍ يُبنى SVG معَ السؤالِ مباشرة) |
| ↳ `img` | بديلاً عن `svg`: مسارُ أصلٍ موجودٍ في `images/`. **وأفضلُ مصدرٍ أصلٌ مستعمَلٌ في درسٍ آخر** — يوحّدُ الهويةَ ولا يضيفُ ملفاً |

```js
{
  type: "letter-picture",
  objective: "يربطُ الحرفَ بصوتِه في أوّلِ الكلمة",
  level: "knowledge",
  prompt: "صِلْ كلَّ حرفٍ بصورةِ الكلمةِ التي تبدأُ به.",
  pairs: [
    { letter: "ش", word: "شمس", svg: "<svg viewBox='0 0 100 100'>…</svg>" },
    { letter: "ق", word: "قمر", svg: "<svg viewBox='0 0 100 100'>…</svg>" },
    { letter: "ن", word: "نجمة", svg: "<svg viewBox='0 0 100 100'>…</svg>" }
  ]
}
```

> **السعةُ المقيسة: حتى ٤ أزواج** (على `qf-tall`)، ولوحُ الصورةِ 104×104 تصميميّ. ورسومُ الصورِ تتبعُ **ورقةَ المواصفاتِ البصريةِ المعتمدة** في `CLAUDE.md` (حدٌّ `#111111` رفيع، تدرّجُ ٣–٤ درجاتٍ بحزمٍ حادّة، بلا ملامحِ وجهٍ إطلاقاً).

### توسعةٌ اختيارية: `art` — الحرفُ رسماً مصمَّماً (دروسُ الحروف)

**قرارُ المالك ٢٠٢٦-٠٨-١٨:** في **دروسِ الحروف** («أحب لغتي» الصفُّ الأول) الحرفُ **موضوعُ
الدرسِ نفسُه**، فلا يُعرَضُ نصّاً خامّاً في كبسولة. يُكتَبُ في الزوجِ حقلُ `art` باسمِ
**سُلَّمِ لون**، فتُبنى بلاطةُ حرفٍ SVG بورقةِ المواصفاتِ البصريةِ المعتمدة: **أربعُ حزمٍ
حادّةِ الحوافِّ** من السُّلَّم، و**لمعةٌ بيضاءُ**، و**حدُّ `#111111` رفيعٌ مستديرُ الرؤوس**.

**غيابُ الحقلِ = نصٌّ خامٌّ كما كان حرفياً**، فلا يتأثّرُ أيُّ سؤالٍ مؤلَّفٍ قبلَ التوسعة.

| السلالم المتاحة | `green` · `orange` · `sky` · `ocean` · `red` · `amber` · `moon` · `steel` |
|---|---|

```js
pairs: [
  { letter: "بـ",  art: "green", word: "بَيْتٌ",  svg: "<svg viewBox='0 0 100 100'>…</svg>" },
  { letter: "ـبـ", art: "green", word: "شِبْلٌ",  svg: "…" },
  { letter: "ـب",  art: "green", word: "حاسوبٌ", svg: "…" }
]
```

> **و«بما يناسبُ السؤال» ثلاثةُ تكيُّفات:** الشكلُ **المنفصلُ** حين يكونُ المطلوبُ التعرّفَ
> على الحرف، و**أشكالُ الاتصالِ** (`بـ` · `ـبـ` · `ـب`) حين يكونُ المطلوبُ موضعَه في الكلمة،
> **والقياسُ يتقلّصُ تلقائياً** للأشكالِ المتّصلةِ (حرفٌ 90 · حرفانِ 76 · ثلاثةٌ 60) لتبقى
> داخلَ اللوحة.

> 🔧 **ولماذا `<text>` مقصوصٌ لا مساراتٌ يدوية:** المساراتُ تعني رسمَ كلِّ حرفٍ من العشرينَ
> بكلِّ أشكالِ اتصالِه — أربعُ نسخٍ للحرفِ الواحد. والقصُّ (`clipPath`) يجعلُ الدالّةَ تعملُ
> مع **أيِّ حرفٍ وأيِّ شكل** بلا رسمٍ جديد، وتبقى الحزمُ حادّةً كما تشترطُ الورقة.

> ⚠️ **سماكةُ الحدِّ هنا `2.5` من لوحةِ 100 لا `3` من لوحةِ 600** — المعيارُ في الورقةِ سماكةٌ
> **معروضةٌ** ≈٣ بكسل، والبلاطةُ تُعرَضُ 112px. والأخذُ بالرقمِ حرفياً يُخرِجُ الحدَّ شعرةً لا تُرى.

---

## ㊱ الحكم والتعليل — `judge-reason`
**خاصٌّ باللغة العربية · فئتُه أساسيّة · مستواه `evaluation` دائماً.**

بنيةُ `mcq` نفسُها للحكم (`options` + `answer`)، ويزيدُ عليها `reasons` و`reasonAnswer` للتعليل. **خطوتان:** تظهرُ أزرارُ الحكمِ وحدَها، فإن صحَّ الحكمُ ظهرتِ الأسبابُ الثلاثةُ فاختارَ الطالبُ تعليلَه. **والدرجةُ (رفعُ الصاروخ) لا تُمنَحُ إلا بصحّةِ الحكمِ والسببِ معاً** — صحّةُ الحكمِ وحدَها تُعطي سطرَ تغذيةٍ محايداً بلا صوتٍ ولا مكافأة.

**الحكمُ الخاطئُ يُعادُ اختيارُه** (لا يُعطَّلُ زرُّه) لأنّ الخيارَينِ اثنانِ فتعطيلُ الخطأِ يكشفُ الصواب. أمّا **السببُ الخاطئُ فيُعطَّل** كما في `mcq` لأنّ الأسبابَ ثلاثة.

| الحقل | الوصف |
|------|------|
| `prompt` | ✅ نصُّ الموقفِ المطلوبِ الحكمُ عليه |
| `options[]` | ✅ خيارا الحكم (عادةً «نعم» / «لا») — تُخلَط عند العرض |
| `answer` | ✅ فهرسُ الحكمِ الصحيح في `options` |
| `reasons[]` | ✅ **ثلاثةُ أسباب** — تُخلَط عند العرض، ولا تظهرُ إلا بعدَ صحّةِ الحكم |
| `reasonAnswer` | ✅ فهرسُ التعليلِ الصحيح في `reasons` |

```js
{
  type: "judge-reason",
  objective: "يُصدرُ حكماً مُعلَّلاً على تصرّفِ شخصيةٍ في النصّ",
  level: "evaluation",
  prompt: "هل كان تصرف الطفل مع الطائر صحيحاً؟",
  options: ["نعم", "لا"],
  answer: 0,
  reasons: [
    "لأنه أخذه إلى الطبيب واعتنى به حتى تعافى",
    "لأنه تركه في الحديقة",
    "لأن الطائر كان يطير"
  ],
  reasonAnswer: 0
}
```

> **صياغةُ الأسبابِ — شرطٌ لا زينة:** الأسبابُ الثلاثةُ كلُّها يجبُ أن تكونَ **متّسقةً مع الحكمِ الصحيح**، والفارقُ بينها في **دقّةِ التعليلِ لا في صحّةِ الحكم**. فلو كان سببٌ منها يناقضُ الحكمَ لَاستدلَّ الطالبُ على التعليلِ بالإقصاءِ بلا تفكير.

> ⚠️ **السعةُ المقيسة: خياران للحكمِ + ٣ أسبابٍ بسطرَين لكلٍّ** — يدخلُ إطارَ `qf-tall` (مقيسٌ: `clientHeight 675` والمحتوى `677`، والذيلُ داخلَ الإطارِ بهامش ‏+١٥٥px). **ولا تُزَد الأسبابُ عن ثلاثةٍ ولا يُطوَّل نصُّها**، لأنّ الإطارَ يُختارُ لأقصى امتدادِ السؤالِ من أوّلِ لحظة.

> 🔧 **لماذا كتلةُ الأسبابِ محجوزةُ الارتفاعِ من البداية:** هو **النوعُ الوحيدُ الذي ينمو بعدَ التفاعل**، والإطارُ يُختارُ مرّةً واحدةً عندَ العرض. وقد قِيسَ أنّ إنشاءَ الأسبابِ عندَ الكشفِ يُنمي المحتوى ‏١٨٦px فيفيضُ عن الإطار (‏`scrollHeight 534` مقابلَ `clientHeight 348`) **ولا تتدخّلُ شبكةُ أمانِ الأبعاد**. فتُبنى الكتلةُ من البداية محجوبةً بـ`visibility` (الصنف `.jr-locked`) ومعطّلةً وخارجَ ترتيبِ التركيز. **المقايضةُ المقبولة:** نصُّ الأسبابِ في DOM منذُ البداية (محجوبٌ بصرياً) — قُبِلَ لأنّ البديلَ يكسرُ قاعدةَ فحصِ الإطارِ الإلزامية.

---

## ㊲ أستمع وحدّد موضع الحكم — `listen-locate`

**مادّةُ «ديني حياتي» — مجالُ التلاوةِ والتجويد.** يسمعُ التلميذُ تلاوةً صحيحةً كاملةً،
ثمّ **يُشيرُ** إلى موضعِ الحكمِ في النصِّ المعروضِ أمامَه.

```js
{
  type: "listen-locate",
  objective: "يطبّق أحكام التجويد المبسَّطة في تلاوة السور المقررة",
  level: "application",
  prompt: "اِسْتَمِعْ ثُمَّ اُنْقُرْ عَلى الكَلِمَةِ الَّتي آخِرُها تَنْوينٌ يُدْغَمُ بِغُنَّةٍ.",
  rule:   "الغُنَّة",                    // اسمُ الحكمِ كما سمّاه الدرس
  surah:  "البلد",  ayah: 3,
  audio:  "audio/deeny/balad-3.mp3",   // تلاوةٌ صحيحةٌ بصوتِ قارئٍ حقيقيّ
  tokens: ["وَوَالِدٍ", "وَمَا", "وَلَدَ"],   // كلماتُ الآيةِ على ترتيبِها الصحيح
  answer: [{ token: 0, chars: [4] }],  // الحرفُ ٤ من الكلمةِ ٠ = «دٍ» بتنوينِ الكسر
  why:    "تَنْوينُ «وَوَالِدٍ» يُدْغَمُ في واوِ «وَمَا» بِغُنَّةٍ مِقْدارُها حَرَكَتانِ."
}
```

> ⛔ **حُرمةُ النصِّ الشريف — وهي علّةُ تصميمِ النوعِ كلِّه:** الآيةُ تُعرَضُ **كاملةً على
> ترتيبِها الصحيحِ مضبوطةً**، والتلميذُ **يُشيرُ ولا يحرّك**. فلا `shuffle` هنا إطلاقاً
> بخلافِ كلِّ نوعٍ ذي خيارات، ولا تنشأُ حالةٌ وسطى محرَّفةٌ في أيِّ لحظة
> (`CLAUDE.md` §① · `dini-hayati-question-types.json` §sacredText).

> 🔑 **`chars` فهارسُ حروفٍ لا فهارسُ محارف.** كلُّ حرفٍ يُضَمُّ إليه ما يتبعُه من حركاتٍ
> (`llClusters` في `js/app.js`)، فـ«وَوَالِدٍ» **خمسةُ حروفٍ لا تسعةُ محارف**:
> `0:وَ` · `1:وَ` · `2:ا` · `3:لِ` · `4:دٍ` — والتنوينُ ملتصقٌ بدالِه لا عنصرٌ مستقلّ.
> **والفهرسةُ على المحارفِ الخامِ تفصلُ الحركةَ عن حرفِها فيخرجُ
> النصُّ الشريفُ مشوّهاً على الشاشة** — وهو عينُ ما تمنعُه القاعدةُ أعلاه. و`chars`
> **اختياريّ**: بغيابِه تُبرَزُ الكلمةُ كلُّها. و`answer` **مصفوفةٌ** تحتملُ أكثرَ من
> موضعٍ، والتأليفُ اليومَ **بموضعٍ واحد**.

> ⚠️ **الحكمُ المؤلَّفُ عليه هو حكمُ الدرسِ المقرَّرِ لا كلُّ حكمٍ يصحُّ في الآيةِ لغةً.**
> نطاقُ الأحكامِ لكلِّ درسٍ في `dini-hayati-question-types.json` §`tajweedAudioScope`:
> **البلدُ حكمُه الغُنَّة**، **والمطففين ٢ حكمُه المدُّ الطبيعيّ**. والمدُّ الطبيعيُّ
> موجودٌ في البلدِ ٣ فعلاً (ألفُ «وَمَا») — **وهذا بالضبطِ موضعُ الزلل**: صحّةُ الحكمِ
> لغةً لا تجعلُه مقرّرَ الدرس. *(وقعَ فعلاً في أوّلِ صياغةٍ لهذا المثالِ ٢٠٢٦-٠٨-١١
> فصُحِّحَ إلى الغُنَّة.)*

> 🔎 **وغُنَّةُ البلدِ ٣ إدغامٌ عبرَ حدِّ الكلمتَين لا شَدّةٌ في كلمةٍ واحدة:** تنوينُ
> «وَوَالِدٍ» يُدغَمُ في واوِ «وَمَا». ولذلك **يسألُ النصُّ عن التنوينِ صراحةً** — فلولا
> ذلك لَصَحَّ النقرُ على الكلمتَين معاً ولا إجابةَ واحدة. **قاعدةٌ عامّةٌ للنوع: إن كان
> الحكمُ واقعاً بينَ كلمتَين فسمِّ في النصِّ أيَّهما تريد**، أو أدرجْ كلتَيهما في
> `answer` (فهو مصفوفة). وغُنَّتا الآيتَين ١٠ و١٧ أوضحُ: «النَّجْدَيْنِ» و«ثُمَّ» بشَدّةٍ ظاهرة.

> 🔊 **الصوتُ لا يُعطِّلُ السؤال:** إن تعذّرَ تحميلُ الملفِّ عُطِّلَ **زرُّ الاستماعِ وحدَه**
> وصارَ نصُّه «التسجيل غير متوفّر بعد»، ويبقى السؤالُ قابلاً للحلِّ بالنظرِ في النصّ
> (مقيسٌ حيّاً بمسارٍ غيرِ موجود: الزرُّ معطّلٌ والسؤالُ يُحَلُّ ويُعطي تغذيتَه الراجعة).
> **ولا يُؤلَّفُ سؤالٌ بلا تسجيلٍ فعليّ** — هذا المسارُ شبكةُ أمانٍ لا رخصةُ تأليف.

> 📐 **السعةُ المقيسة (1280×800):** آيةٌ من **٣ كلمات** ← إطارُ `qf-m` بانحرافِ نسبةٍ
> **0.00٪** · صفرُ قصٍّ · لا `qflex`. وكلُّ كلمةٍ **60px** ارتفاعاً في فضاءِ التصميم
> (حدُّ اللمسِ على السبورة)، وسطرُ الآيةِ `line-height:2.1` لأنّ حركاتِ المصحفِ ترتفعُ
> فوقَ سطرِ الحروف. ⚠️ **ونصُّ السؤالِ يُقاسُ**: الصيغةُ الأطولُ («اِسْتَمِعْ إِلى
> التِّلاوَةِ، ثُمَّ…») أكلَت **6px** من تنفّسِ النافذة، فقُصِّرت — و«إلى التلاوة»
> مكرّرةٌ في زرِّ الاستماعِ أصلاً.

---

## نموذج درس كامل (٥ أسئلة، بأنواع منفّذة مختلفة)
انظر المثال الحيّ في `js/questions.js` تحت المفتاح `"g4s-1-2"` (الهيكل العظمي للإنسان):
صواب/خطأ → اختيار من متعدد → توصيل → سحب وإفلات → تحديد الأجزاء (متدرّجة الصعوبة).

## كيف تُحدّد نِسَب `x`/`y` على صورة؟
`x` = المسافة من **يسار** الصورة ÷ عرضها × 100.
`y` = المسافة من **أعلى** الصورة ÷ ارتفاعها × 100.
(الغلاف يجعل النِسَب محسوبة على الصورة نفسها لا على الملعب، فتبقى دقيقة على كل الشاشات.)
حذفتوثيقوإعادةترقيمالأقسامتوزيعالرياضيات
