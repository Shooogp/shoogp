# قواعد التصميم الدائمة — منصّة شوجب

هذا الملف هو **المصدر المرجعي لقواعد التصميم الدائمة** التي يُطابَق عليها الكود.
عند أي تعارض بين الكود وما هو موثّق هنا، يُصحَّح الكود ليطابق القاعدة —
ولا تُعدَّل القاعدة إلا بقرار صريح من مالك المشروع.
(بقية قواعد المشروع — سير العمل، التأليف، التوزيع المعرفي — موطنها `CLAUDE.md`،
ويُشير كلٌّ منهما إلى الآخر دون تكرار.)

---

## احتواء محتوى السؤال داخل الإطار

يجب أن يبقى محتوى أي سؤال (لوحة/شبكة/صورة/svg/بطاقات...) بالكامل داخل
إطار السؤال دون أي تجاوز من الأعلى أو الأسفل أو الجوانب. إذا كان
المحتوى أكبر من المساحة المتاحة فإنه يتقلّص ليتلاءم مع الإطار مع
الحفاظ على نسبه الأصلية، ولا يُسمح له بالبروز خارج الإطار أو التداخل
مع العنوان أو أزرار الترويسة.

**المرجع التقني للتطبيق** (القاعدة الأساسية العامة في ملف الأنماط
المشترك `css/style.css` — قسم «قاعدة الاحتواء الأساسية»، تحتها كل
أنواع الأسئلة الحالية والمستقبلية: لوحة/سحب وإفلات/بازل/ترتيب/اختيار...):

- **حاويات السؤال** (`.qslides` · `.qcard` · `.qbody` · `.qframe` · `.qwin`):
  `min-height:0; min-width:0; box-sizing:border-box` — عنصر flex لا
  يتقلّص افتراضياً عن حجم محتواه (`min-*:auto`)، وغياب `min-height:0`
  هو **السبب التقني الجذري** لبروز المحتوى خارج الإطار.
- **`.qbody`**: `max-height:100%` + `overflow:hidden` — حدّ أمان نهائي
  ضد أي بروز مهما جدّ من محتوى.
- **الوسائط داخل `.qbody`** (`svg, img, canvas, video, .media`):
  `max-width:100%; max-height:100%` مع حفظ النسبة (`object-fit:contain`
  للصور/الفيديو، و`preserveAspectRatio` للـSVG). تُكتب بـ`:where()`
  (بلا وزن تخصيص) عمداً كي تبقى قاعدةً أساسية تغلبها حدود الأنواع
  الخاصة عند الحاجة. **ولا يُفرض `height:auto` عاماً على كل وسيط**:
  بعض رسوم الأسئلة طبقات مطلقة الموضع بارتفاع 100% (خطوط التوصيل
  `.dndlines`/`.matchsvg`، خطوط الخريطة الذهنية `.mmlines`، طبقة
  التلوين `.psvg`) يصفّرها `height:auto`؛ الوسيط الجاري في التدفّق
  يعلن `width:100%; height:auto` في قاعدة نوعه (مثل `.hcsvg` و`.nlsvg`
  و`.arsvg`).
- **الوسيط المرن** (الرسم الذي يجب أن *يتقلّص* ليتلاءم بدل أن يفيض —
  كشبكة لوحة المائة): يُوسم `qfit-flex` في مُنشئه فيصير عنصر flex
  قابلاً للتقلّص (`flex:0 1 auto; min-height:0`)، ولشبكة اللوحة تحديداً
  `aspect-ratio:1/1` يحفظ نسبتها المربّعة مهما تقلّصت — **بلا أي
  ارتفاع ثابت (px)**، وصف البطاقات أسفلها يبقى ظاهراً (لا يتقلّص).
  وطبقة الإطار (`js/shoogp-ui.js`) تقرأ الوسم فتمنح البطاقة أوسع إطار
  مسموح ليأخذ الرسم كل ما تبقّى من النافذة.

---

## أسئلة البازل — صورة القطع

في كل أسئلة البازل، يجب أن تكون الصورة المصدر ممتلئة المعالم بالكامل
دون مناطق فارغة أو خالية كبيرة. يجب أن تحمل كل قطعة من قطع البازل
محتوى/معالم مميّزة تسمح للطالب بالاستدلال على موضعها الصحيح ضمن
الصورة الكاملة. لا يُسمح بوجود قطعة خالية بلا معالم، لأنها تجعل تحديد
موضعها الصحيح متعذّراً.

**التفاصيل التقنية:**

- البازل صورة واحدة يقسّمها `renderPuzzle` (`js/app.js`) إلى شبكة
  (٣×٣ افتراضاً) عبر `background-image` بإزاحة لكل قطعة `.pzpiece` —
  فامتلاء المعالم **خاصية للصورة المصدر** قبل التقسيم.
- **عند تأليف صورة بازل (مولَّدة SVG/canvas أو جاهزة):** وزّع المعالم
  على كامل مساحة الصورة بحيث تقع في **كل خلية من خلايا الشبكة** معالم
  مميّزة: إطار زخرفي يمرّ بقطع الحواف، رموز مختلفة في الأركان الأربعة،
  عناصر متّصلة تعبر حدود القطع (استمرارية تعين على التركيب)، ومحتوى
  مختلف لكل خلية. الخلفية الملساء وحدها ليست مَعلماً.
- **حارس مركزي في `renderPuzzle`:** عند تحميل صورة البازل تُفحص كثافة
  المعالم في كل خلية من خلايا الشبكة (عبر canvas)، وأي خلية شبه خالية
  تُطلق تحذيراً صريحاً في الطرفية باسم السؤال والخلية — فلا تمرّ صورة
  مخالفة دون كشف أثناء التطوير. الحارس فحصٌ لا رفض: لا يكسر السؤال
  أمام الطالب، لكنه يمنع اعتماد صورة مخالفة أثناء التأليف.

---

## أسئلة السحب والإفلات — خطوط التوصيل

في أسئلة السحب والإفلات:

في سؤال السحب والإفلات نوعان من مناطق الإفلات الفارغة:

- **(أ) مناطق استقبال منفصلة** — صناديق `.target` بحدود منقّطة وعلامة
  «؟»، تُعرض حول الرسم/الصورة (كصناديق فوق صف الأعداد أو حول هيكل).
- **(ب) خانات فارغة داخل الرسم نفسه** — عناصر مرسومة داخل `<svg>`
  السؤال بحدّ متقطّع (مثل الخانات الناقصة بين الأرقام في صف لوحة
  المائة: ٤٠ [فارغ] ٣٨ [فارغ] ٣٦ ...).

القاعدة:

- **السلوك الافتراضي لكل المواد — وللنوع (أ) في كل المواد بما فيها
  الرياضيات:** يُرسم الخط الواصل والنقطة بين منطقة الاستقبال وموضعها
  على الرسم/الصورة.
- **استثناء مادة الرياضيات — للنوع (ب) فقط:** الهدف الذي يقابل خانةً
  فارغةً داخل الرسم لا يُرسم له خط واصل ولا نقطة، ويكون السحب مباشرة
  إلى الخانة الفارغة في مكانها بين الأرقام — لأن موضع الخانة صحيح
  وثابت في مكانه الصحيح فلا يحتاج خطاً يوجّه إليه. مناطق الاستقبال
  من النوع (أ) في السؤال نفسه تبقى على السلوك الافتراضي.

**التفاصيل التقنية:**

- الخط الواصل: `<line class="dndline">` داخل `<svg class="dndlines">`
  (لون `rgb(245,160,74)`، سماكة `2.5px`).
- النقطة: `<span class="dnd-dot">` عند طرف كل خط.
- مُعرِّف مادة الرياضيات: الصنف `subj-math` على حاوية السؤال
  (يضعه `js/shoogp-ui.js` على `#questionList` عند فتح درس رياضيات).
- **تمييز النوع (ب) تلقائيّ من الرسم الحيّ**: الخانة الفارغة عنصر
  `rect`/`circle`/`ellipse` يحمل `stroke-dasharray` داخل `svg` السؤال؛
  والهدف الذي تقع نقطته (`dot`) داخل حدود خانةٍ منها يُصنَّف نوعَ (ب)
  — فيصير صندوق `.target` طبقةَ إفلاتٍ شفافة (`.target-cell`) مطابقةً
  للخانة موقعاً ومقاساً، ولا يُنشأ له خط ولا نقطة **أصلاً في DOM**.
  الأهداف الأخرى في السؤال نفسه تبقى نوعَ (أ) بخطوطها ونقاطها.
- التطبيق **مركزيّ** في الدالة المشتركة `renderDragDrop` (`js/app.js`)،
  وأنماط الطبقة الشفافة في `css/style.css` و`css/shoogp-ui.css`
  (`.target-cell`). أي سؤال يُضاف لاحقاً يرث القاعدة تلقائياً دون
  تعديل بياناته.
- التغذية الراجعة عند التحقق (تلوين الصناديق الصحيحة/الخاطئة، وامتلاء
  الخانة عند الإفلات) واحدة في كل المواد ولا تتأثر بهذه القاعدة.
