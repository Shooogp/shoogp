# قواعد منصّة شوجب التعليمية

## عن المشروع
منصّة تفاعلية للمناهج العُمانية، للحلقة الأولى (الصفوف ١-٤).
البنية: فصل ← صف ← مادة ← وحدة ← درس ← أنشطة.
كل المحتوى بالعربية، بتصميم فضائي، ويعمل على السبورة الذكية.
الشعار: "شوجب" (برتقالي وأبيض على خلفية فضائية).

## سير العمل مع Git (إلزامي)
- **العمل يتمّ على فرع `main` مباشرة.**
- **لا تُنشأ فروع جانبية إلا بطلب صريح مني.**
- أيّ فرع يُنشأ (بطلبي) **يُدمج في `main` قبل نهاية الجلسة**، ولا يُترك عالقاً.

> ⚠️ **والفروعُ الباقيةُ على الريموت (`claude/…` و`docs/…`) بقايا مدموجةٌ تُتجاهَل — لا
> عملَ فيها ولا تُفحَص.** حالتُها مقيسةٌ ٢٠٢٦-٠٨-٢١ بعدَ تعميقِ التاريخ: محتواها كلُّه
> في `main`. وهي باقيةٌ لأنّ **قاعدةَ مستودعٍ تمنعُ الحذف** (الواجهةُ تردُّ
> «Branch could not be deleted») لا لأنّها معلّقة.
>
> **ولا يُستنتَجُ من عددِها أنّ عملاً ضاع.** الاستنساخُ في جلساتِ السحابةِ **ضحلٌ**
> (‏`.git/shallow` — يُظهرُ عشراتِ commits لا مئاتِها)، فتبدو الفروعُ بلا سلفٍ مشتركٍ
> مع `main` وتُقرَأُ «غيرَ مدموجة» وهي مدموجة. **فإن لزمَ الفحصُ يوماً فَـ`git fetch
> --unshallow origin` أوّلاً** — وقبلَه كلُّ مقارنةٍ باطلة، ‏`git diff main...branch`
> يفشلُ صامتاً بـ«no merge base» ويُخرِجُ صفراً يُقرَأُ «لا فرق».

## النشر وختم نسخ الأصول (تلقائي)
- النشر عبر GitHub Actions (`.github/workflows/pages.yml`): عند كل دفعة إلى `main` يُختم وسم `?v=` تلقائياً ببصمة النشرة قبل النشر (كسر ذاكرة المتصفحات) في **ثلاثة مواضع**:
  - **ملفات HTML** — وسوم `css/js` (يُستبدل الوسم القائم).
  - **`url(../images/…)` في ملفات CSS** — لا وسم فيها في المستودع فيُضاف عند النشر.
  - **صور الإطارات المحمَّلة من JS** — ترثه تلقائياً: `imgURL()` في `js/shoogp-ui.js` يلتقط `?v=` من رابط السكربت نفسه ويُلحقه بكل صورة.
- **لا تُرقَّ `?v=` يدوياً** — القيم في المستودع مبدئية تُستبدل عند النشر.
- ⚠️ **صورةٌ حُدِّثت ونُشِرت ولا تظهر = ذاكرةُ متصفّحٍ لا نشرٌ فاشل.** تحقّق أولاً من نجاح النشرة، ثم من أنّ رابط الصورة يحمل `?v=` (بلا وسمٍ يبقى القديمُ مخزَّناً شهوراً). وقعت فعلاً مع `frame-arabic-tall.png` — ٢٠٢٦-٠٨-٠٦.

### 🔴 إعدادُ مصدرِ Pages — شرطٌ لِعملِ الختمِ أصلاً (قرارُ المالك ٢٠٢٦-٠٨-١٧)

**`Settings ← Pages ← Build and deployment ← Source` يجبُ أن يبقى `GitHub Actions`،
ولا يُعادُ إلى `Deploy from a branch` أبداً.**

**العلّةُ مقيسةٌ لا مفترَضة:** حين كان على وضعِ الفرعِ كانَ **ناشرانِ يعملانِ على الدفعةِ
نفسِها وفي الثانيةِ نفسِها**، كلاهما ينجح — سيرُ عملِنا `pages.yml` (يختمُ)، والناشرُ
المدمجُ `dynamic/pages/pages-build-deployment` (**ينشرُ المستودعَ خاماً بلا ختم**).
والمدمجُ يفوز، فيصلُ المحتوى الجديدُ ويبقى `index.html` يشيرُ إلى **الوسمِ المبدئيِّ نفسِه
المخزَّنِ في متصفّحِ المعلّمِ منذُ شهور** — فيرى الموقعَ القديمَ ويظنُّ النشرَ فاشلاً.

> **ولا يُصلحُ ذلك تعديلُ `pages.yml`**: فيه `actions/configure-pages@v5` أصلاً ولم يكفِ.
> العلّةُ **إعدادُ مستودعٍ لا كود**.

**وقاعدةُ التحقّقِ بعدَ كلِّ دفعة:** الحكمُ **بالختمِ الحيِّ لا بنجاحِ سيرِ العمل** —
يُجلَبُ `index.html` بلا ذاكرةٍ ويُنظَرُ إلى `?v=`: إن كان **ثمانيَ خاناتٍ ستّ عشريةً**
فالنشرُ سليم، وإن كان القيمةَ المبدئيةَ في المستودعِ فقد فازَ ناشرٌ خامٌ والإعدادُ ارتدَّ.

## نطاق المواد (الحلقة الأولى: الصفوف ١–٤)
تصنيف المواد بحسب حالة التفعيل في المنصّة:
- **نشطة (مبنيّة ومؤلَّفة):** العلوم.
- **مخطّط لها (ضمن النطاق، لم تُبنَ/تُؤلَّف بعد):** الرياضيات، اللغة العربية، التربية الإسلامية.
- **خارج النطاق حالياً:** اللغة الإنجليزية، عالمي الرقمي، الدراسات الاجتماعية — لأنها غير موجودة في الحلقة الأولى.

القواعد أدناه (التوزيع، المستوى المعرفي، ربط الهدف) مُطبَّقة على **العلوم** و**الرياضيات** و**اللغة العربية** (لكلٍّ توزيعه المعرفي الخاص أدناه)؛ ويُحدَّد توزيع بقية المواد عند تفعيلها. هذا الملف (`CLAUDE.md`) هو **موطن القواعد**، وتشير إليه بقية الملفات دون تكرار.

## بنية التنقل والتبويبات
**مصدر الفهرسة:** جميع المواد تعتمد فهرس **كتاب التلميذ** في بناء تبويبات الدروس، **باستثناء الرياضيات** التي تعتمد فهرس **دليل المعلم**، لأنه المرجع التدريسي الفعلي للمعلمين في السلطنة.
> ⛔ **ولا يُفهرَس من `الفهرس.pdf` ولا من `ملخص-الفهرس.pdf`** — هما ملفّا **تخزينٍ** عند المعلّم لا مصدرَا فهرسة، والفهرسة من المصدرَين أعلاه حصراً (§مسارات المصادر وتسمية ملفّاتها).

**بنية الرياضيات:** ثلاثة مستويات كما في دليل المعلم: **الوحدة** (١أ، ١ب، ١ج، ٢أ — ويحمل عنوانها لاحقة «وحل المشكلات») ← **الفصل** (مرقّم تسلسلياً ١–١٧ ولا يُعاد ترقيمه مع كل وحدة) ← **النشاط الأساسي** (مثل ١-١، ٣-٢) وهو **الدرس القابل للنقر**. أسئلة النشاط الأساسي الواحد تغطّي **جميع صفحات كتاب التلميذ** التابعة له، وأهدافه هي **قائمة الأهداف المرمّزة** المذكورة في دليل المعلم لذلك النشاط. يُعرض عنوان الوحدة مصدّراً برمزها (مثل «١أ: العدد وحل المشكلات») لأن ١أ و٢أ يشتركان في العنوان نفسه. وعند تعارض ملف الأهداف مع فهرس الدليل فالدليل هو الحاكم. الصف الثاني — الفصل الدراسي الأول: ٤ وحدات، ١٧ فصلاً، ٣٦ نشاطاً أساسياً.

**صيغة الترقيم:** الصيغة المعتمدة هي **فصل-نشاط**، وتُكتب في الملفات بالترتيب المنطقي: رقم الفصل أولاً ثم رقم النشاط (مثال: ١٢-١ للنشاط الأول من الفصل ١٢). تُستخدم الأرقام الهندية وجوباً، فيتولّى المتصفح عرضها بالهيئة نفسها الظاهرة في دليل المعلم. لا يُعكس الترقيم يدوياً لمطابقة صورة الفهرس، لأن ما في الصورة ترتيب بصري لا ترتيب كتابة.

**قفل الوحدات:** الوحدة الأولى من كل كتاب مفتوحة مجاناً وما بعدها يُفتح برمز — القاعدة كاملةً (النطاق، البصمات، الانتهاء، `SALT`) في **`js/unlock.js`** وحدَه، ولا تُكرَّر هنا.

## تأليف الدروس والأنشطة التفاعلية
مرجع التأليف العمليّ (أنماط التصميم والعرض والتفاعل) هو مهارة **`lesson-authoring`** (`.claude/skills/lesson-authoring/SKILL.md`) — استخدمها عند إضافة درس جديد، أو تأليف أسئلة تفاعلية، أو تعديل أسئلة درس. القواعد الملزمة نصُّها هنا في `CLAUDE.md`، والمهارة تشير إليها دون تكرار.

**مراجع الأنواع:**
- **`question-types.md`** — كتالوج أنواع الأسئلة المشتركة الـ١٦ (٨ أساسية + ٨ إثرائية) وفئاتها (أساسية/إثرائية) وتنظيمها بحسب النطاق (**أنواع مشتركة تصلح لأي مادة** / **خاصة بالعلوم** / **خاصة بالرياضيات** / **خاصة باللغة العربية**)، والقوائم التوثيقية للأنواع المخطّطة لكل مادة. **قسم «الخاصة بالعلوم» يضمّ نوعاً واحداً** («العدسة المكبّرة» — مبني قيد الاختبار) نُقل إليه من الفئة الإثرائية المشتركة. وقسم **«الخاصة بالرياضيات» يضمّ ثلاثة عشر نوعاً** مبنيّةً وموثّقة. وقسم **«الخاصة باللغة العربية» يضمّ ستة أنواعٍ** مبنيّةً وموثّقة: `sentence` (ترتيب الجملة) و`judge-reason` (الحكم والتعليل — مستواه `evaluation` دائماً) **أساسيّان**، و`tashkeel` و`arrange` و`sun-moon` و`letter-picture` **إثرائية**. هو **الحَكَم** عند أي تعارض في وصف نوع أو فئته.
- **`نماذج-الأسئلة.md`** — قوالب الأنواع المبنية برمجياً؛ أثناء التأليف استخدم فقط الأنواع الموثقة فيه.

**قاعدة التوزيع لكل درس (إلزامية — العلوم والرياضيات):**
- **٥ أسئلة أساسية ثابتة** لكل درس، تُختار من **الفئة الأولى (الأنواع الأساسية)** حسب المحتوى — ٥ أنواع مختلفة، لا يتكرّر النوع في الدرس الواحد.
- **سؤال إثرائي واحد اختياري** من **الفئة الثانية (الأنواع الإثرائية)**، يُضاف فقط إذا ناسبت طبيعة الدرس (لا يُحشر).
- سؤال الإثراء يُختار **بالتناوب** من الأنواع الإثرائية الثمانية في `question-types.md` بما يناسب محتوى الدرس، و**لا يتكرّر النوع نفسه في درسين متتاليين من الوحدة الواحدة**. والأسئلة التأسيسية **لا تلتزم ترتيباً ثابتاً واحداً للأنواع عبر الدروس**.
- **الخلاصة:** كل درس **٥ أو ٦ أسئلة**، مرتّبة تصاعدياً حسب المستوى المعرفي: المعرفة أولاً ثم التطبيق ثم الاستدلال، والإثرائي آخراً. (وفي **اللغة العربية** الترتيبُ على سُلَّمِها الرباعيّ: `knowledge` ثمّ `reasoning` ثمّ `application` ثمّ `evaluation` — §قاعدة المستوى المعرفي.)
- إن كان نوعٌ من الفئة الأولى غير مناسب للدرس، **أخبر المستخدم قبل استبداله** ولا تكرّر نوعاً آخر تلقائياً.
- هذه القاعدة (٥ + ١) مُطبَّقة على **العلوم والرياضيات واللغة العربية**؛ ويُحدَّد توزيع بقية المواد المخطّط لها **عند تفعيلها**. وفي الرياضيات تُختار الأنواع الأساسية الخمسة من **الفئة الأولى المشتركة** ومن **الأنواع الأساسية الخاصة بالرياضيات** معاً (انظر `question-types.md`). وفي **اللغة العربية** لا يُختارُ النوعُ حُرّاً بل **بقالبِ الدرس** (§قالبا تأليف اللغة العربية أدناه): ففي **درسِ القراءةِ وفهمِ المقروء** تُختارُ الأساسيةُ الخمسةُ من **الفئة الأولى المشتركة** ومن النوعَين الأساسيَّين الخاصَّين بها `sentence` (ترتيب الجملة) و`judge-reason` (الحكم والتعليل) معاً — و**`judge-reason` هو الذي يؤدّي حصّة `evaluation` المفروضة في الخمسة**، فمستواه `evaluation` بطبيعته؛ أمّا `tashkeel` و`arrange` و`sun-moon` و`letter-picture` فهي **إثرائيةٌ في درسِ القراءةِ** — لأنّ كلاً منها معلّقٌ بمهارةٍ لغويةٍ بعينِها لا تردُ في كلِّ درسِ قراءة، فحشرُها فيه يُفسدُها. وفي **الدرسِ اللغويِّ والإملائيِّ تنقلبُ الآيةُ**: هذه الأربعةُ هي عمودُه الفقريُّ وتؤلّفُ خمستَه الأساسية، و`judge-reason` لا يُطلَبُ فيه.

**الأنواع المسموحة في «أحب لغتي» — قائمةُ سماحٍ مغلقة (إلزامية):**
هذه **قائمةُ ما يُسمَح**، لا قائمةُ ما يُمنَع. **وما لم يُذكرْ فيها لا يُستعمَلُ في هذه المادة**، سواءٌ أكان نوعاً مشتركاً أم خاصاً بمادةٍ أخرى.

| | الأنواع |
|---|---|
| **مشتركة — ١٤** | الاختيار من متعدد `mcq` · صح وخطأ `true-false` · النقطة الساخنة `hotspot` · الترتيب التسلسلي `sequence` · التصنيف `classify` · التوصيل `matching` · ملء الفراغ بالسحب `fill-blank` · السحب والإفلات `drag-drop` · الاستبعاد `exclude` · الخريطة الذهنية `mindmap` · بطاقات الذاكرة `memory` · التلوين بالتعليمات `color` · السؤال الصوتي `audio-q` · اكتشف الخطأ `find-error` |
| **خاصة بالعربية — ٦** | ترتيب كلمات لتكوين جملة `sentence` · التشكيل `tashkeel` · ترتيب الحروف `arrange` · الشمسية والقمرية `sun-moon` · توصيل الحرف بالصورة `letter-picture` · الحكم والتعليل `judge-reason` |

**المجموع ٢٠ نوعاً**، تنقسم بالتساوي: **١٠ أساسية** (الثمانية المشتركة + `sentence` + `judge-reason`) و**١٠ إثرائية** (الستة المشتركة + `tashkeel` و`arrange` و`sun-moon` و`letter-picture`). فالخمسةُ الأساسيةُ تُختار من العشرة الأولى، والإثرائيُّ الاختياريُّ من العشرة الثانية.

> **الخارجان من الأنواع المشتركة: الشريط المتدرّج `slider` والبازل `puzzle`** — لضعف صلتهما بمهارات اللغة: الأول تقديرٌ وقياسٌ رقميّ، والثاني إدراكٌ بصريٌّ مكانيّ، وليس فيهما مهارةٌ لغويةٌ تُقاس.
> **ولا يعني ذلك حذفهما ولا التقليل منهما:** كلاهما **باقٍ في الكتالوج المشترك بلا مساسٍ إطلاقاً**، متاحٌ للعلوم والرياضيات كما كان. الاستثناءُ **من مادةٍ واحدةٍ لا من المنصّة**.

> وبقيّةُ الأنواعِ خارجُ القائمةِ بحكمِ نطاقِها أصلاً: **العدسة المكبّرة `lens`** خاصةٌ بالعلوم، **والأنواعُ الثلاثةَ عشرَ الخاصةُ بالرياضيات** خاصةٌ بها. الكتالوجُ الكاملُ وفئةُ كلِّ نوعٍ في `question-types.md`.

**قاعدة المستوى المعرفي (إلزامية):**
- كل سؤال يحمل حقل `level`. القيم **الثلاث المشتركة في كل المواد**: `knowledge` (معرفة) أو `application` (تطبيق) أو `reasoning` (استدلال)، **وتنضمّ إليها قيمة رابعة `evaluation` (تقويم) في مادة اللغة العربية «أحب لغتي» وحدَها**. المستوى **خاصية للسؤال الفرديّ لا للنوع** (يُحدَّد حسب صياغة السؤال لا حسب قالب النوع).
- **المستويات الثلاثة المشتركة (من الأدنى إلى الأعلى):**
  - **معرفة** — تذكّر واسترجاع حقيقة أو مصطلح أو تعريف مباشر (الأكثر شيوعاً في الحلقة الأولى). مثال: «كم عدد عظام جسم الإنسان؟».
  - **تطبيق** — توظيف معلومة معروفة في موقف عمليّ: تسمية أجزاء على صورة، إكمال جملة بتطبيق قاعدة. مثال: «اسحب اسم كل عظم إلى مكانه».
  - **استدلال** — تحليل وربط وتفكير عالٍ: مقارنة، تصنيف، اكتشاف خطأ، ترتيب منطقي، ربط سبب بنتيجة. مثال: «صنّف الحيوانات إلى فقاريات ولافقاريات».
- **المستوى الرابع `evaluation` (تقويم) — مقصورٌ على اللغة العربية:** إصدارُ حكمٍ مُعلَّلٍ أو إبداءُ رأيٍ مسنَدٍ إلى النصّ (تفضيلُ صياغةٍ على أخرى، الحكمُ على تصرّفِ شخصيةٍ، اختيارُ العنوانِ الأنسبِ مع التعليل). مثال: «أيُّ العنوانَين أنسبُ للنصّ؟ ولماذا؟».
  **⛔ لا يُستعمل `evaluation` في العلوم ولا في الرياضيات إطلاقاً** — هاتانِ المادّتانِ باقيتانِ على مستوياتِهما الثلاثةِ وتوزيعِهما بلا مساس.
- **التوزيع المعرفي للأسئلة الأساسية الخمسة:** العلوم **٣ معرفة + ١ تطبيق + ١ استدلال**؛ الرياضيات **٢ معرفة + ٢ تطبيق + ١ استدلال** (مادة إجرائية يغلب عليها التطبيق)؛ **اللغة العربية — في دروس القراءة وفهم المقروء وحدَها** — **١ `knowledge` + ١ `reasoning` + ٢ `application` + ١ `evaluation`**. السؤال الإثرائي مستواه حرّ ولا يدخل في الحساب. ويُحدَّد توزيع بقية المواد عند تفعيلها.

**نطاقُ التوزيع في اللغة العربية — نوعانِ من الدروس لا نوعٌ واحد (إلزاميّ):**
التوزيعُ الرباعيُّ أعلاه **ليس عامّاً في كلِّ دروسِ العربية**، بل **مقصورٌ على دروسِ القراءةِ وفهمِ المقروء**.

| نوعُ الدرس | الأسئلةُ الأساسية | التوزيعُ المعرفيّ | `evaluation` |
|-----------|:---:|---|:---:|
| **قراءة وفهمُ مقروء** | ٥ | **١ `knowledge` + ١ `reasoning` + ٢ `application` + ١ `evaluation`** — إلزاميّ | **مطلوب** |
| **أنماطٌ لغويةٌ وإملاء** | ٥ | **لا توزيعَ معرفيٌّ إلزاميّ** — يُختارُ `level` كلِّ سؤالٍ بحسبِ صياغتِه | **غير مطلوب** |

**سندُ التفريق — الوثيقةُ نفسُها تفرّقُ بينهما:** *وثيقة تقويم تعلّم التلاميذ في مادّتَي المجال الأول للصفوف ١–٤* (وزارة التربية والتعليم، ٢٠٢٥م) **توزّعُ الدرجاتِ الخمسَ على فهمِ المقروء**، وتجعلُ **الكتابةَ والأنماطَ اللغويةَ قسماً مستقلاً** بدرجاتِه الخاصّة: **أنماط ٢ · إملاء ٢ · تعبير ٣**. فالتوزيعُ الرباعيُّ مشتقٌّ من قسمِ فهمِ المقروء، وحملُه على دروسِ الأنماطِ والإملاءِ حملٌ لقسمٍ على قسمٍ آخرَ لم يوضَعْ له.

> **ولماذا لا `evaluation` في الدرسِ اللغويِّ والإملائيّ:** «التقويم» حكمٌ مُعلَّلٌ على **مضمونٍ** — على تصرّفِ شخصيةٍ أو ملاءمةِ عنوانٍ أو جودةِ صياغة. أمّا ضبطُ الحركةِ وتمييزُ الشمسيةِ من القمريةِ ورسمُ الهمزةِ فإجابتُها **صوابٌ أو خطأٌ بقاعدة**، ولا مجالَ فيها لحكمٍ مُعلَّل. ففرضُ سؤالِ تقويمٍ عليها يُنتجُ سؤالاً متكلَّفاً لا يقيسُ المهارةَ المقصودة.

> **وقاعدةُ «٥ + ١» نفسُها لم تتغيّر** في النوعَين: خمسةُ أسئلةٍ أساسيةٍ وسؤالٌ إثرائيٌّ واحدٌ اختياريّ. المتغيّرُ **التوزيعُ المعرفيُّ وحدَه**.

**مستويات «أحب لغتي» — جدول المقابلة بين مفاتيح الكود ومسمّيات الوثيقة (إلزاميّ):**
مصدرُ التقسيمِ الرباعيّ: **وثيقة تقويم تعلّم التلاميذ في مادّتَي المجال الأول للصفوف ١–٤** (وزارة التربية والتعليم، سلطنة عُمان، ٢٠٢٥م) — <https://home.moe.gov.om/images/library/file/Book470046.pdf> — وفيها نصّاً: «لا يفضل الاكتفاء بتقسيم: معرفي – فهم – استدلال».

| مفتاح الكود | مسمّى الوثيقة في «أحب لغتي» | حصّته من الأساسية الخمسة |
|------------|------------------------------|:---:|
| `knowledge` | **التذكّر** | ١ |
| `reasoning` | **الفهم والاستدلال المباشر** | ١ |
| `application` | **التطبيق / التحليل / الإبداع** | ٢ |
| `evaluation` | **التقويم** | ١ |

> **هذه المسمّياتُ خاصّةٌ بمادّةِ «أحب لغتي» دونَ غيرِها.** في العلومِ والرياضياتِ تبقى المفاتيحُ الثلاثةُ على معانيها المذكورةِ أعلاه (معرفة / تطبيق / استدلال) بلا أيِّ تغييرٍ في الاسمِ أو المعنى أو التوزيع. **ولم يُعَدْ تسميةُ أيِّ مفتاحٍ قائم** — `evaluation` إضافةٌ صرفةٌ فوقَ الثلاثة.
> **والجدولُ أعلاه — بحصصِه — يخصُّ دروسَ القراءةِ وفهمِ المقروء** (§نطاقُ التوزيع أعلاه)؛ أمّا دروسُ الأنماطِ والإملاءِ فلا حصصَ مفروضةً فيها.

## قالبا تأليف اللغة العربية (إلزاميّان)

لكلِّ نوعٍ من الدرسَين قالبُه الثابت. **الأنواعُ المذكورةُ في كلِّ خانةٍ اقتراحاتٌ متكافئة** يُختارُ منها ما يناسبُ المحتوى، أمّا **عددُ الخاناتِ وترتيبُها ومستوياتُها فملزِمة**.

### ① قالب درس القراءة وفهم المقروء — ٥ أسئلة

| # | الوظيفة | `level` | الأنواع |
|:-:|--------|--------|--------|
| ١ | **تذكّر** | `knowledge` | `mcq` · أو `hotspot` (النقطة الساخنة) للصف الأول |
| ٢ | **فهم** | `reasoning` | `mcq` · أو `matching` (توصيل سبب ↔ نتيجة) |
| ٣ | **تحليل** | `application` | `sequence` (الترتيب التسلسلي) · `classify` (التصنيف) · `mindmap` (الخريطة الذهنية) |
| ٤ | **إبداع** | `application` | `mcq` **بإجابات متعددة** (`answer` مصفوفةً) · أو `exclude` (الاستبعاد) |
| ٥ | **تقويم** | `evaluation` | `judge-reason` (الحكم والتعليل) |
| + | **إثرائيّ اختياريّ** | حرّ | نصُّ الاستماع (`mcq` بحقل `audio`) · `tashkeel` (التشكيل) · `sun-moon` (الشمسية والقمرية) |

> الخانةُ ④ هي موضعُ **`answer` مصفوفةً** في `mcq`: «اقترحْ عنواناً آخرَ للنصّ» تصحُّ فيها أكثرُ من إجابة. والخانةُ + هي موضعُ **`audio` في `mcq`** لنصِّ الاستماع.

### ② قالب الدرس اللغويّ / الإملائيّ — ٥ أسئلة

**لا توزيعَ معرفيٌّ إلزاميٌّ هنا، ولا `evaluation`.** يُكتَبُ `level` لكلِّ سؤالٍ بحسبِ صياغتِه.

| # | الوظيفة | الأنواع |
|:-:|--------|--------|
| ١ | **التعرّف على الحرف** | `letter-picture` (توصيل الحرف بالصورة) |
| ٢ | **بناء الكلمة** | `arrange` (ترتيب الحروف) |
| ٣ | **الضبط** | `tashkeel` (التشكيل) |
| ٤ | **قاعدة لغوية** | `sun-moon` (الشمسية والقمرية) · أو `mcq` «أيُّ الكلماتِ كُتبت خطأً؟» |
| ٥ | **بناء الجملة** | `sentence` (ترتيب كلمات لتكوين جملة) · أو `fill-blank` (ملء الفراغ بالسحب) |
| + | **إثرائيّ اختياريّ** | الإملاءُ المسموع (`arrange` بحقل `audio`) |

> ⚠️ **تنبيهٌ يرفعُ تعارضاً ظاهرياً:** أربعةٌ من أنواعِ هذا القالب (`letter-picture` · `arrange` · `tashkeel` · `sun-moon`) مصنّفةٌ **إثرائيةً** في `question-types.md`. **والتصنيفُ صحيحٌ ومنسوبٌ إلى دروسِ القراءة**: علّتُه هناك أنّ كلاً منها معلّقٌ بمهارةٍ لغويةٍ بعينِها لا تردُ في كلِّ درسِ قراءة، فحشرُها فيه يُفسدُها. أمّا في **الدرسِ اللغويِّ والإملائيِّ فهذه المهاراتُ هي الدرسُ نفسُه**، فهي عمودُه الفقريُّ لا إثراؤه. **فالفئةُ (أساسيّ/إثرائيّ) نسبيّةٌ بنوعِ الدرسِ لا مطلقة**، وهذا القالبُ هو الحاكمُ في دروسِ الأنماطِ والإملاء.

> والخانةُ + هي موضعُ **`audio` في `arrange`**: يسمعُ الطالبُ الكلمةَ ثمّ يرتّبُ حروفَها.

**⚠️ وليس كلُّ صفٍّ فيه النوعانِ — والمصدرُ هو الفاصل (قرار المالك ٢٠٢٦-٠٨-١٨):**
القالبانِ يفترضانِ أنّ **درسَ القراءةِ والدرسَ اللغويَّ مدخلانِ منفصلان**، وهذا صحيحٌ حيثُ
فصلَهما الكتاب. **فإن جاءَ كتابُ صفٍّ بالأنماطِ اللغويةِ والإملائيةِ مدمَجةً داخلَ الدرسِ
القرائيِّ نفسِه، فالدرسُ درسُ قراءةٍ يتبعُ القالبَ ① كاملاً** (بتوزيعِه الرباعيِّ
و`evaluation`)، **وتُحمَلُ الأنماطُ في السؤالِ الإثرائيِّ الاختياريّ**.

> ⛔ **ولا يُقسَمُ الدرسُ الواحدُ مدخلَينِ في المنصّةِ لِيُصنَعَ له درسٌ لغويّ، ولا يُزادُ
> سؤالٌ إثرائيٌّ ثانٍ** — الأولُ يخالفُ فهرسةَ الكتاب، والثاني يخالفُ قاعدةَ «٥ أو ٦».
>
> **سابقةٌ مقيسة: «أحب لغتي — الصف الثالث».** مقدّمةُ كتابِه ص ١١ تنصُّ أنّ المحورَ
> «ثلاثةُ دروسٍ قرائيّةٍ **تتضمَّنُ** الأنماطَ اللغويةَ والإملائيةَ والخطَّ والتعبير»،
> ومخرَجاهُ ⑧ و⑨ يؤكّدانِ ذلك — فلا درسَ لغويّاً مستقلّاً فيه أصلاً. التفصيلُ في
> `الأهداف.md` الخاصِّ به.

**قاعدة ربط السؤال بالهدف (إلزامية):**
- كل سؤال يحمل حقل `objective` = نصّ الهدف الذي يخدمه من أهداف الدرس (`الأهداف.md`).
- كل سؤال يخدم هدفاً واحداً محدّداً، **ولا يبقى هدفٌ رئيسيّ** في الدرس دون سؤال يغطّيه (الأسئلة مجتمعةً تغطّي الأهداف الرئيسية). يجوز أن يخدم أكثر من سؤال الهدفَ نفسه بمستويات معرفية مختلفة.
- **مصدر الأهداف عند التأليف:** عند تأليف أي درس، تُقرأ أهداف الدرس من **ملف الأهداف (md)** في مجلد المصادر الخاص بالمادة والصف، ويُوسم كل سؤال **برمز الهدف** الذي يخدمه (مثل `2Nn10`). **التغطية تراكمية:** الهدف قد يُغطّى جزئياً في الدرس ويكتمل عبر الكتاب.
- **ملفات PDF المصدرية (كتب وأدلة) محلية على جهاز المعلم ولا تُرفع للمستودع** (مستثناة في `.gitignore`)؛ ملفات الأهداف md تُستخرج منها وتُرفع وهي المرجع في جلسات التأليف.
  > ⚠️ **وملفُّ أهدافٍ جديدٌ لا يُتتبَّعُ إلا بـ`git add -f`** — فَـ`المصادر/` مستبعَدٌ بكامله، والإخفاقُ صامتٌ لا يظهرُ في `git status`. التفصيلُ في §مسارات المصادر وتسمية ملفّاتها.

**قاعدة بناء الأنواع الجديدة (إلزامية — لكل المواد):**
- الأنواع المعتمدة للتأليف هي **المبنية برمجياً والموثقة في `نماذج-الأسئلة.md` فقط**. لا يجوز اختراع أو بناء نوع جديد **أثناء تأليف درس** مهما بدا مناسباً.
- بناء نوع جديد يتمّ **فقط بطلب صريح مستقل** (لا أثناء التأليف)، ويشمل: كود النوع + توثيق صيغته في `نماذج-الأسئلة.md` + نقله من قائمة الأنواع المخطّطة/غير المنفّذة إلى الموثقة.
- إن استدعى محتوى الدرس نوعاً غير موجود، **أخبر المستخدم أولاً** ولا تنفّذه من تلقاء نفسك.
- التأليف يعني إنتاج بيانات أسئلة بمنطق الأنواع الموجود (اكتب المنطق مرة، يعمل مع كل البيانات) — لا برمجة أنواع جديدة.
- هذه القاعدة **عامة لكل المواد** (العلوم النشطة والمواد المخطّط لها عند تفعيلها).

**قاعدة فحص الإطار بعد التأليف (إلزامية — لكل المواد):**
- بعد إضافة أو تعديل أي سؤال، يُفحص **صنف إطاره المعروض**: إن ظهر `qflex` في أصناف `.qframe` فالسؤال أثقل من الإطارات الحقيقية، وذيله (سطر التغذية الراجعة غالباً) سيمتد **تحت نهاية رسم الإطار** على شريط التعبئة الخام فيبدو خارج الحاوية — وهي علة واجهة موثقة في حالة الحاوية المرنة، لا يمنعها «حارس الشاشة» (يضمن البقاء داخل الشاشة لا داخل حدود الرسم).
- العلاج عند التأليف: **تخفيف محتوى السؤال** حتى يدخل إطاراً حقيقياً (`s/m/l/tall`)، لا تعديل الواجهة. **قيود السعة المقيسة لكل نوع موثقة في `نماذج-الأسئلة.md`** عند نوعها (اليوم: التصنيف حتى ٣ مجموعات و٤ عناصر، وملء الفراغ حتى فراغين — في الرياضيات).
- طريقة الفحص: افتح السؤال وتحقق من أصناف `.qframe` (على مصفوفة المنافذ عند الشك، وبالنداءات المتزامنة `frameize()+fitFrame()+ShoogpFit.apply()+placeChrome()` إن كان لوح المعاينة مخفياً).
- لا يُعتمد سؤال جديد وهو على الحاوية المرنة إلا بقرار صريح مني (كأسئلة التصنيف الثقيلة المتعمدة).

**خطوات التأليف (إلزامية قبل كتابة الأسئلة):**
1. اقرأ `الأهداف.md` للدرس أولاً واستخرج أهدافه الرئيسية.
2. اعرض عليّ **قائمة الأهداف**، ومعها لكل سؤال مقترح: نوعه، والهدف الذي يخدمه، ومستواه المعرفي (`level`) — **قبل** كتابة نصوص الأسئلة.
3. تأكّد أنّ الأسئلة الأساسية الخمسة تحقّق توزيعَ مادّتِها: **٣ معرفة + ١ تطبيق + ١ استدلال** (العلوم)، **٢ معرفة + ٢ تطبيق + ١ استدلال** (الرياضيات)، **١ `knowledge` + ١ `reasoning` + ٢ `application` + ١ `evaluation`** (اللغة العربية — **دروسُ القراءةِ وفهمِ المقروء وحدَها**؛ ودروسُ الأنماطِ والإملاءِ بلا توزيعٍ إلزاميّ، تتبعُ قالبَها). وفي اللغةِ العربيةِ حدّدْ **نوعَ الدرسِ أوّلاً** ثمّ اتبعْ قالبَه (§قالبا تأليف اللغة العربية). وتأكّدْ أنّ الأهداف الرئيسية كلها مغطّاة بأسئلة.
4. بعد موافقتي، اكتب الأسئلة كنص، ثم أضفها للبيانات.

## فروق الصفوف في تأليف أسئلة «أحب لغتي»

**سندُها: وثيقة تقويم تعلّم التلاميذ في مادّتَي المجال الأول للصفوف ١–٤** (وزارة التربية والتعليم، سلطنة عُمان، ٢٠٢٥م) — <https://home.moe.gov.om/images/library/file/Book470046.pdf>.

القالبانِ وقاعدةُ «٥ + ١» والتوزيعُ المعرفيُّ **واحدةٌ في الصفوفِ الأربعة**؛ المتغيّرُ **صيغةُ عرضِ السؤالِ** بحسبِ ما يستطيعُ التلميذُ قراءتَه في صفِّه.

| الصف | صيغةُ العرض |
|:---:|------------|
| **الأول** | **صورةٌ واضحةٌ في كلِّ سؤال** · التعليماتُ **لا تتجاوزُ سطراً** · **الخياراتُ صوراً لا كلماتٍ كلّما أمكن** |
| **الثاني** | **كلماتٌ قصيرةٌ مشكولة** · **صورٌ مساندة** |
| **الثالث والرابع** | **نصٌّ مكتوبٌ أطول** · **خياراتٌ كلامية** |

> **الأثرُ العمليُّ على اختيارِ النوع** (لا على القالب): في الصفِّ الأول تُقدَّمُ الأنواعُ التي هدفُها صورةٌ — `hotspot` (وهو بديلُ `mcq` في الخانة ① من قالبِ القراءة) و`letter-picture` و`audio-q` — ويُتجنَّبُ ما يعتمدُ على قراءةِ خياراتٍ نصّيةٍ طويلة. وفي الرابعِ يتّسعُ المجالُ لِـ`judge-reason` بأسبابِه الثلاثةِ المكتوبةِ ولِـ`mcq` بإجاباتٍ متعددة.

**قاعدةُ تناوبِ النصوصِ — على مستوى المنهجِ لا الدرس (الصفّان الثالث والرابع):**

**نصوصُ القراءةِ تتناوبُ بين أدبيٍّ ومعلوماتيّ — ولا تتوالى نصوصٌ أدبيةٌ.**

- **يُتحقَّقُ من التناوبِ عندَ تأليفِ كلِّ درسٍ جديدٍ** بالنظرِ إلى **الدروسِ السابقةِ له في المحورِ نفسِه**: إن كان السابقُ أدبياً فالتالي معلوماتيّ.
- **الأدبيّ:** قصّةٌ · شِعرٌ · نشيدٌ · نصٌّ سرديّ. **والمعلوماتيّ:** تقريرٌ · وصفُ مكانٍ أو مهنةٍ · مستندٌ بصريّ · نصٌّ إجرائيّ.
- **هذه القاعدةُ لا تُقاسُ داخلَ الدرسِ الواحدِ بل عبرَ المحور** — فهي **الوحيدةُ في هذا الملفِّ التي لا يكفي فيها فحصُ الدرسِ المؤلَّفِ وحدَه**. اقرأْ ترتيبَ دروسِ المحورِ قبلَ التأليف.
- وإن جاءَ **مصدرُ الكتابِ نفسُه** بنصَّين أدبيَّين متتاليَين، **فالكتابُ هو الحاكمُ ولا يُخترَعُ نصّ** — يُنبَّهُ المالكُ ويُذكَرُ ذلك في تعليقِ الدرس.

## قواعدُ مادّةِ التربيةِ الإسلاميةِ «ديني حياتي» (إلزاميّة)

هذا **موطنُ قاعدتَي المحتوى** في المادّة، ولا تُكرَّرانِ في `question-types.md` ولا في مهارةِ `lesson-authoring` — تشيرانِ إليهما فحسب.

> **لا وثيقةَ تقويمٍ وزاريةً للمادّةِ في الصفوفِ ١–٤.** فمرجعُ المحاورِ والمعاييرِ **جدولُ معاييرِ التعلّمِ ومخرجاتِه في دليلِ المعلّم** وعناصرُ الدرسِ في كتابِ التلميذ — بخلافِ «أحب لغتي» التي لها وثيقةٌ رسمية.

### ① حُرمةُ النصِّ الشريف — لا يُعرَضُ مبعثراً ولا مخلوطاً ليُعادَ ترتيبُه

**يُمنَعُ عرضُ النصِّ القرآنيِّ أو الحديثِ أو الذكرِ المأثورِ مبعثراً أو مخلوطاً ليعيدَ التلميذُ ترتيبَه. أمّا عرضُه صحيحاً مع فراغٍ يُملأُ فمسموح.**

ويشملُ المنعُ تحديداً:

- **ترتيبَ كلماتِ آيةٍ أو حديثٍ** — أن تُعرَضَ كلماتُ النصِّ مبعثرةً ليرتّبَها التلميذ.
- **سحبَ كلماتِ النصِّ المبعثرةِ وإفلاتَها** — أن يُفكَّكَ النصُّ إلى كلماتِه لتُسحَبَ كلٌّ منها إلى موضعِها.

**والنصُّ في كلِّ الأحوالِ يُعرَضُ مضبوطاً بالشكلِ صحيحَ الرسمِ، ولا يُعرَضُ في صورةٍ محرَّفةٍ ولو لحظةً.**

> **علّةُ المنع — ولماذا يفترقُ الخلطُ عن الفراغ:** الممنوعُ هو **الحالةُ الوسطى المحرَّفة**: نصٌّ شريفٌ كلماتُه مخلوطةٌ **معروضةٌ على الشاشة** بترتيبٍ يخالفُ ترتيبَه الصحيح. ولا يرفعُ الحُرمةَ أنّ الصوابَ يُصلِحُه بعدَ قليل، لأنّ الخطأَ وقعَ في العرضِ لا في النتيجة.
>
> أمّا **الفراغُ فلا يُنتِجُ حالةً محرَّفةً في أيِّ لحظة**: ما يُعرَضُ من النصِّ معروضٌ على ترتيبِه الصحيح، والفراغُ موضعُ صمتٍ لا كلمةٌ في غيرِ مكانِها. **وهو أسلوبُ التسميعِ المتعارَفُ عليه** في تعليمِ القرآنِ منذُ القِدَم. **فالمعيارُ: هل يظهرُ لفظٌ في غيرِ موضعِه؟** — لا: هل النصُّ كاملٌ؟
>
> **وترتيبُ الأحداثِ والخطواتِ العمليّةِ** مصوغةً بعبارةِ المؤلِّفِ لا بلفظِ النصِّ الشريف — **مباحٌ بكلِّ الأنواع**، فليس المرتَّبُ لفظاً شريفاً.
>
> **وقيدُ بدائلِ «إكمال آية»** (أن تكونَ البدائلُ من خارجِ النصِّ ولا تُشتقَّ من كلماتِ الآيةِ نفسِها) موضعُه وصفُ النوعِ في `question-types.md` — فهو **الحَكَمُ في وصفِ نوعٍ وقيودِه**.

### ② لا صورةَ للنبيِّ ﷺ ولا للصحابة

**لا تُرسَمُ صورةٌ للنبيِّ ﷺ ولا للصحابةِ رضي الله عنهم** — لا في رسومِ SVG ولا في برومبتاتِ توليدِ الصورِ الخارجية.

**وتُمثَّلُ أحداثُ السيرةِ بالمكانِ والرمزِ لا بالأشخاص:** الغارُ، والمسجدُ، والنخلةُ، والطريقُ، والرايةُ، والمِحفَظةُ — لا شخصٌ يُرسَمُ ولا ظِلٌّ يُشارُ به إليه.

> وهذا **أشدُّ** من قاعدةِ «بلا ملامحِ وجهٍ» العامّة (§قاعدة الرسم بلا ملامح وجه): تلك تمنعُ **الملامحَ** وتُبقي الجسمَ، وهذه تمنعُ **الشخصَ كلَّه**.

## إنشاء بطاقات الكتب (القاعدة المعتمدة)
عند إضافة كتاب جديد، أنشئ بطاقته وخلفيته بهذه الخطوات:

**مصدر بيانات البطاقة (إلزامي):** بطاقة أي كتاب تُبنى **حصراً من غلاف كتاب التلميذ** (اللون + العنوان + بيانات الغلاف) — لا من دليل المعلم، ولا الفهرس، ولا أي ملف آخر. هذا ينطبق **حتى على المواد التي تعتمد فهرس دليل المعلم في تبويب الدروس** (مثل الرياضيات): الفهرسة شيءٌ والبطاقة شيءٌ آخر — **البطاقة دائماً من كتاب التلميذ**. إن لم يتوفّر كتاب التلميذ في المصادر، **توقّف واطلبه** قبل بناء البطاقة (لا تستعض عنه بدليل المعلم).

**١) استخراج اللون:**
- افتح **صفحة الغلاف (الأولى)** من ملف `كتاب-التلميذ.pdf` نفسه — لا حاجة لملف غلاف منفصل.
  (إن كان الكتاب مقسّماً، الغلاف من `كتاب-التلميذ-ج1.pdf`. ولا يُؤخذ الغلاف من `دليل-المعلم.pdf` أبداً.)
- استخرج **اللون السائد** منها.
- **اعرض اللون عليّ أولاً** (بالرقم مثل #8bd123 ووصفه) لأؤكّده قبل المتابعة.

**٢) بناء البطاقة الأصلية:**
- صمّم بطاقة **أصلية** بذلك اللون — لا نسخ للغلاف الحقيقي (حقوق النشر).
- بنفس أسلوب بطاقات المنصّة: عنوان المادة، شارة الصف (رقم)، رمز أصلي مناسب
  للمادة (مثل: مجهر للعلوم، أشكال هندسية للرياضيات)، الفصل الدراسي.
- عناصر البطاقة متناسقة مع اللون (بيضاء/ذهبية للتباين الواضح).

**٣) اشتقاق الخلفية:**
- استنبط لون خلفية صفحة الكتاب من نفس اللون، حسب قاعدة الخلفيات:
  بنفسجي فضائي داكن + لمسة خفيفة من لون الكتاب (لا لون فاقع).

**٤) الربط:**
- اربط البطاقة والخلفية بالكتاب في البيانات (cover + theme).
- **وإن كان الكتابُ وحدةً قابلةً للبيعِ فاكتبْ له `onSale` صريحاً** (§ما يُطرَحُ للبيع
  أدناه) — غيابُه يُقرَأُ `false`، فيُنشَرُ الكتابُ الجديدُ **صامتاً غيرَ قابلٍ للبيع**.

**٥) إنشاء ملف برومبتات الرسومات (تلقائياً مع كل بطاقة):**
- اقرأ دروس الكتاب من `كتاب-التلميذ.pdf` والفهرس (والفهرسة في الرياضيات من `دليل-المعلم.pdf`).
- لكل درس اكتب: اسمه ورقمه، نوع السؤال البصري الأنسب (تحديد أجزاء / سحب على صورة)،
  برومبت رسم **بأسلوب محادثي بجُمَل كاملة يناسب ChatGPT (DALL·E)** — لا كلمات مفتاحية
  مقتضبة، بل وصف كامل متّصل (يبدأ بـ "أنشئ رسماً تعليمياً..." و"Create an educational
  illustration...")، عربي + إنجليزي، واسم ملفّ عربي مقترح للصورة
  (بشَرطات، بلا مسافات، مثل: `حلزون-اجزاء.png`).
- مواصفات البرومبتات: أسلوب توضيحي علمي مناسب للحلقة الأولى، خلفية بيضاء نظيفة،
  بدون تسميات مكتوبة، أجزاء منفصلة واضحة، مع "no text labels" في الإنجليزي.
- **أضف في نهاية كل برومبت**: "صورة واحدة مستقلّة فقط." و"One single standalone image only."
- **مرساة الأسلوب (إلزامية في نهاية كل برومبت)** — لتوحيد الهوية البصرية بأسلوب **مسطّح واحد عبر كل المنصّة** (لا صور واقعية). نصُّها مشتقٌّ من **ورقة المواصفات البصرية المعتمدة** أدناه (§قاعدة التفصيل البصري) وهي الحَكَم عند أي تعارض:
  • عربي: «رسم كرتونيّ متّجهي لامع بأسلوب ملصقات الفضاء، بألوانٍ صريحةٍ مشبعة، لكلِّ جسمٍ **تدرّجُ حجمٍ من ثلاثِ إلى أربعِ درجاتٍ من لونِه نفسِه** (فاتحةٌ ثمّ أساسيةٌ ثمّ غامقةٌ ثمّ ظِلّ) **بحوافَّ فاصلةٍ واضحةٍ بين الدرجاتِ لا مزجٍ ناعم**، مع **لمعةٍ بيضاءَ عريضةٍ** على الأسطحِ المنحنية، وحدٍّ خارجيٍّ **أسودَ رفيعٍ واضحٍ** ‎#111111‎ مستديرِ الرؤوسِ يحيطُ كلَّ جسمٍ وأجزاءَه الداخلية (‏**خطٌّ نظيفٌ رفيعٌ لا حدٌّ ثقيل**)، بأشكالٍ ممتلئةٍ مستديرةٍ بلا زاويةٍ حادّة، والأجسامُ طافيةٌ بلا خطِّ أرضٍ ولا ظلٍّ مُلقًى، وبلا أيِّ ملامحِ وجهٍ على أيِّ شيءٍ إطلاقاً.»
  • إنجليزي: "glossy cartoon vector illustration in a space-sticker style, bold saturated colors, each object shaded with a 3-4 step ramp of its own hue (highlight, base, shade, deep shade) as crisp discrete bands with hard edges and no soft blending, a broad white gloss highlight on curved surfaces, a thin crisp black outline #111111 with rounded caps around every object and its inner parts (a clean fine line, not a heavy border), plump rounded forms with no sharp angles, objects floating with no ground line and no cast shadow, absolutely no facial features on anything"
- **أضف في أعلى الملف**:
  • خطوات التوليد في ChatGPT (كل برومبت في صورة منفصلة تُحفظ باسمها).
  • تعليمة بارزة تُلصق أول المحادثة في ChatGPT: تطلب صورة واحدة مستقلّة لكل برومبت،
    وعدم جمع رسومات درسين أو إنشاء شبكة صور — مع السماح برسم عناصر البرومبت الواحد
    معاً في صورة واحدة (مثل عناصر سؤال التصنيف).
- دروس المراجعة ("تحقّق من تقدّمك") تُعيد استخدام رسومات الوحدة، لا برومبتات جديدة.
- احفظ الملف باسم `برومبتات-الرسومات.docx` **في مجلّد المصادر بجانب `كتاب-التلميذ.pdf`**.

**٦) التطبيق التدريجي:**
- نفّذ **كتاباً واحداً أولاً** كنموذج (بطاقته + ملف برومبتاته)، وأرِني النتيجة قبل تنفيذ البقية.
- إن كان الغلاف متعدّد الألوان بلا لون سائد واضح، أخبرني لأحدّد اللون يدوياً.

## ما يُطرَحُ للبيع — حقلُ `onSale` (إلزامية)

**`onSale` هو المصدرُ الوحيدُ لِما يُباع، ويسكنُ البياناتِ نفسَها لا صفحةَ الدفع.**

**والقاعدةُ الحاكمةُ: `onSale` خاصّيةُ الوحدةِ القابلةِ للبيعِ لا خاصّيةُ كلِّ كتاب.**

| الصنف | العدد اليوم | يحملُ `onSale`؟ | أين |
|------|:---:|:---:|-----|
| **كتابٌ مستقلٌّ** (لا حزمةَ له) | ٨ | ✅ صريحاً | `data/books.json` **و**`js/data.js` بالقيمةِ نفسِها |
| **حزمة** | ٨ | ✅ صريحاً | `data/bundles.json` |
| **جزءُ حزمةٍ** (`g1-dini-1`…) | ١٦ | ⛔ **لا يحملُه إطلاقاً** | — حالتُه حالةُ حزمتِه |

> **لماذا يُمنَعُ الحقلُ عن أجزاءِ الحزم:** الجزءُ لا يُباعُ وحدَه، فحقلٌ فيه
> **ادّعاءٌ كاذبٌ ومصدرُ انحرافٍ** — الحزمةُ `true` والجزءُ `false`، فأيُّهما
> يُصدَّق؟ وصفحةُ الدفعِ تطوي الأجزاءَ إلى حزمتِها **قبلَ** أن تسألَ عن البيع
> (`collapseBundles` ثمّ `isOnSale` في `pay.html`)، فالمسؤولُ عنه دائماً مفتاحُ
> وحدةٍ لا مفتاحُ جزء.

**⛔ ولا تُكتَبُ قائمةُ المطروحِ في `pay.html`.** كانت `CONFIG.ON_SALE` مصفوفةً
ثابتةً هناك وأُزيلت — ووجودُها من جديدٍ ارتدادٌ إلى مصدرَينِ للحقيقةِ الواحدة،
**يفشلُ عليه الحارسُ فيمنعُ النشر**.

**ولا يقرأُ `js/unlock.js` هذا الحقلَ ولا يعرضُه.** رابطا الشراءِ يظهرانِ لكلِّ
الكتبِ بلا استثناء، وصفحةُ الدفعِ وحدَها تقولُ «مطروحٌ» أو «لم يُطرح للبيع بعد» —
فلا تُكرَّرُ معرفةُ المطروحِ في طرفَين.

**الرقيب:** `tools/check-data-sync.mjs` (‏`npm run check`، وخطوةٌ في `pages.yml`
قبلَ الختم) يفرضُ البنيةَ أعلاه ويفحصُ معها تطابقَ مفاتيحِ `books.json` مع
احتياطِ `js/data.js` وصحّةَ أعضاءِ الحزم. **تفصيلُ الفحوصِ في رأسِ الأداةِ نفسِها
ولا يُكرَّرُ هنا.**

## قواعد حقوق النشر (مهم للنشر العام)
- الأسئلة من تأليف أصلي مستوحى من الكتاب، لا نسخ حرفي لفقراته.
- لا صور أغلفة حقيقية في المنصّة؛ فقط بطاقات أصلية (الغلاف يُقرأ لاستخراج اللون فقط).
- الصور الواقعية يجب أن تكون حرّة الحقوق أو من تصوير المعلّم.

**قاعدة الرسوم عند التأليف (إلزامية):**
كل رسم يُبنى SVG مع السؤال مباشرة، دون placeholders أو قوائم صور خارجية.
عند طلب المعلم استبدال رسمة إيضاحية ثابتة، تُستبدل بالصورة المحددة من `images/`
بنفس الأبعاد دون المساس بمنطق السؤال.

> **استثناءٌ واحدٌ مقيسٌ: `letter-picture` يستعملُ حقلَ `img` لا `svg`** (قرارُ المالك
> ٢٠٢٦-٠٨-٢٦). بطاقةُ الحرفِ **صورةٌ كاملةٌ لا مناطقَ تفاعليةً فيها** — التفاعلُ نقرُ
> البطاقةِ كلِّها — فتنطبقُ عليها قاعدةُ «أين يقعُ التفاعل» أدناه: **مسموحٌ الاستبدال**.
> والدالّةُ تقبلُه سلفاً (`pr.svg || <img src="${pr.img}">` في `js/app.js`)، و`css/style.css`
> يُنسّقُ `.lpimg img` و`.lpimg svg` بالقاعدةِ نفسِها — فلا تعديلَ في كودٍ ولا نمط.
>
> **والعلّةُ الحاسمةُ وزنٌ مقيسٌ لا تفضيل:** الصورةُ المولَّدةُ متتبَّعةً بـ`vtracer` تُنتجُ
> **‏117 KB و35 مساراً**، مقابل **‏14 KB** للصورةِ ملفّاً (‏300×300، 32 لوناً). فلو ضُمِّنَت
> رسومُ «أحب لغتي ١» الـ٧٦ متتبَّعةً لانتفخَ `questions.js` من ٢٫٤ ميغابايت إلى نحوِ ١٠ —
> وهو ملفٌّ يُحمَّلُ في متصفّحِ السبورةِ **مع كلِّ درس**. والملفّاتُ المستقلّةُ يخزّنُها
> المتصفّحُ مرّةً، فَـ`questions.js` **ينكمشُ** بحذفِ المضمَّن بدلَ أن ينتفخ.
>
> ⛔ **ولا يتعدّى الاستثناءُ هذا النوعَ**: `hotspot` و`color` و`find-error` وأنواعُ
> الرياضياتِ الرسميةُ تبقى SVG مبنيّاً، لأنّ مساراتِها تحملُ `spot`/`cpart` التي تُقاسُ
> عليها الإجابة.

**حدُّ المنع من الاستبدال بصورة — المعيارُ «أين يقعُ التفاعل» لا «هل السؤالُ تفاعليّ»
(تصحيحُ صياغةٍ بقرارِ المالك ٢٠٢٦-٠٨-٠١):**
- **ممنوعٌ الاستبدال** حين يكونُ **الرسمُ نفسُه هو التفاعل**، أي حين تحملُ مساراتُه
  المعنى وتُقاسُ عليها الإجابة: **التلوين** (`color` — كلُّ منطقةٍ `cpart` تُلوَّن)،
  و**تحديدُ الأجزاء** و**اكتشافُ الخطأ** (`hotspot`/`find-error` — النقرُ على موضعٍ
  في الرسم)، وأنواعُ الرياضياتِ الرسمية (خطُّ الأعداد، لوحةُ المائة، المصفوفات).
  تحسينُ هذه **بتجويدِ الـSVG نفسِه** لا باستبدالِه.
- **مسموحٌ الاستبدال** حين يكونُ الرسمُ **خلفيةً** والتفاعلُ في عناصرَ فوقَها:
  **السحبُ والإفلات** (`drag-drop`) — صناديقُ الأهدافِ ونقاطُ الارتساءِ طبقةٌ مستقلّةٌ
  فوقَ الخلفية، فيستوي أن تكونَ الخلفيةُ صورةً أو SVG. **وهذا هو عُرفُ المنصّةِ
  المنفَّذُ أصلاً:** ستةٌ من أسئلةِ السحبِ السبعةِ في العلومِ تستعملُ صوراً
  (‏`skeleton.png`، `muscles-arm.jpg`، `نبتة-اجزاء.png`، `طفل-أشعة.png`).
- **عند الاستبدالِ في السحبِ والإفلات:** تُعادُ حتماً إحداثياتُ `dot` (نِسَبٌ من
  **الصورةِ الجديدة** لا من الرسمِ القديم)، ويُراجَعُ التخطيطُ (نسبةُ الصورةِ ≥ 1.2
  تُحوّلُه إلى الصفِّ الأفقيّ — `shoogp-ui` §١.٧‑هـ)، وتبقى `box` كما هي (مرجعُها
  ارتفاعُ `.stage` الثابت).
- **وأفضلُ مصدرٍ للصورة: أصلٌ موجودٌ في `images/` مستعمَلٌ في درسٍ آخر** — يوحّدُ
  الهويةَ البصريةَ ولا يضيفُ ملفاً. *(سابقة: `g1s-3-3` س٤ استعارَ `طفل-أشعة.png` من
  `g4s-1-1` س٦، وكانَ خالياً من ملامحِ الوجهِ أصلاً فحقّقَ القاعدةَ بلا تعديل.)*

**قاعدة التفصيل البصري للرسوم (إلزامية — لكل المواد، تلقائية بلا طلب):**
المعيار المستهدف لرسوم الأسئلة هو **مستوى قريب من فنّ المنصّة المصقول (رسم الصاروخ)**، ويتحقق تلقائياً مع كل تأليف على مسارين — لا ينتظر أيٌّ منهما طلب تحسين من المعلم:
- **المسار الفوري (مع كتابة السؤال):** كل رسم يُبنى SVG فوراً بأعلى تفصيل يدوي ممكن — كل جسم مركّب يُظهر **مكوناته الطبيعية** (عربة = صندوق بألواح ومسامير ومقبض، عجلة = إطار وصُرّة وأضلاع...) مع **عمق مسطّح بسيط** (ظل أرضي، خط أرض، درجتان للون) — لا أشكال هندسية عارية تنوب عن الجسم.
- **مسار الترقية إلى مستوى الصاروخ (تلقائي التسجيل):** كل رسم «جسم غني» يجتاز البوّابة الرباعية في مهارة `shoogp-graphics` (مناطق تفاعلية ≤ ٤، حدود مغلقة، لا تداخل، لون فريد لكل منطقة) **يُسجَّل له أثناء التأليف نفسه بندٌ كامل في `graphics-queue.md`** (برومبت التوليد + جدول المناطق وألوان العزل). وحين يولّد المعلم الصور تُتبَّع بـ`vtracer` ويُستبدل الرسم اليدوي عبر بوّابة الجودة — دون مساس بمنطق السؤال. فالرسم اليدوي الغني **جسرٌ مؤقت لا سقف**.
- **الثوابت لا تُمس في المسارين:** الأسلوب المسطّح بحدود سميكة، قاعدة «بلا ملامح وجه»، سقف نسبة `viewBox` (٢٫٢)، والعقد التفاعلي — أي إغناء أو استبدال يُتبع بإعادة حساب `spot`/`dot`/`cpart` وفحص الإطار (قاعدة فحص الإطار أعلاه). و**التفاصيل الزخرفية لا تُشوّش هدف السؤال**: منطقة النقر/السحب تبقى أوضح عناصر الرسم.
- **الرسوم الهندسية الصرفة** (خط أعداد، شبكات، لوحات، أشكال هندسية) تبقى يدوية دائماً — التفصيل فيها وضوحُ التدريج والتباين لا الزخرفة، ولا تدخل طابور الترقية.
- القاعدة تسري على التأليف الجديد، ولا تُطبَّق بأثر رجعي على رسوم مؤلفة إلا بطلب. النموذج المعتمد الأول: عربة `g2m-9-3` س٦ (مسار فوري مُغنى + بند ترقية في الطابور).

**ورقة المواصفات البصرية المعتمدة (إلزامية — قائمة فحصٍ تُطبَّق على كل رسم جديد):**
**المرجع المعتمد (قرار المالك ٢٠٢٦-٠٨-٠١، نسخةٌ ثانيةٌ تنسخُ الأولى):** أسلوبُ **ملصقاتِ الفضاءِ الكرتونيةِ اللامعة** — وهو **فنُّ المنصّةِ نفسُه** لا أسلوبٌ مستوردٌ عليه. والقيمُ أدناه **مقيسةٌ برمجياً من أصولِ المنصّةِ المُودَعةِ في `images/rocket/`** (لا مقروءةً بالعين ولا منقولةً عن فنٍّ محميّ)، فهي أدقُّ مرجعٍ ممكن وأسلمُه حقوقياً. وهي **الحَكَم** عند أي تعارض:

| البند | القيمة المعتمدة |
|------|------------------|
| **الحدّ الخارجي** | **أسودُ شبهُ خالص `#111111`** لكلِّ جسمٍ **ولأجزائِه الداخلية** (مقيس: أغمقُ بكسلٍ في `rocket-body.png` = `#000000`، وفي `satellite.png` = `#060B18`) |
| **سماكة الحدّ** | عند لوحة ٦٠٠ عرضاً: **`stroke-width:3` للحدِّ الخارجيّ و`2` لفواصلِ الحُزَمِ والأجزاءِ الداخلية** — خطٌّ **رفيعٌ واضحٌ** لا حدٌّ ثقيل. (القياسُ من أصولِ المنصّةِ أعطى حدّاً أسمكَ بكثير — البكسلُ الداكنُ يشغلُ ٨–١٤٪ من مساحةِ الرسم: ١٣٫٥٪ في الصاروخ و١١٫٤٪ في العَلَم و٨٫٥٪ في القمر الصناعيّ، أي ما يقابلُ `8`/`5` — لكنّ **المالكَ خفّضها مرّتَين بقرارٍ صريحٍ ٢٠٢٦-٠٨-٠١** (‏`8`←`5`←`3`) لأنّ رسومَ الأسئلةِ تُعرَضُ أصغرَ بكثيرٍ من أصولِ الواجهةِ فتُثقلُها السماكةُ الكاملة. **القرارُ يعلو على القياس، ولا تُرفَعُ السماكةُ استناداً إلى الأرقامِ أعلاه.**) |
| **رؤوس الخطوط** | `stroke-linecap:round` و`stroke-linejoin:round` دائماً |
| **الاستدارة** | أشكالٌ **ممتلئةٌ منتفخةٌ** بلا زاويةٍ حادّة |
| **التعبئة والعمق** | **تدرّجُ حجمٍ من ٣–٤ درجاتٍ من لونِ الجسمِ نفسِه** (فاتحة ← أساسية ← غامقة ← ظِلّ). مثالٌ مقيسٌ من الصاروخ: `#FF8000` ← `#FF6000` ← `#E04000` ← `#C04000` |
| **شكلُ التدرّج** | **حزمٌ منفصلةٌ بحوافَّ حادّة، لا مزجٌ ناعم** — قرارٌ هندسيّ لا جماليّ فقط: الحزمُ المسطّحةُ وحدَها تُتبَّعُ بـ`vtracer` وتحتملُ تقنيةَ لونِ العزل؛ والتدرّجُ الناعمُ يُنتجُ عشراتِ المسارات ويُسقطُ بوّابةَ الجودة |
| **اللمعة** | **لمعةٌ بيضاءُ عريضةٌ** على الأسطحِ المنحنيةِ كلِّها (لا على الزجاجِ وحدَه) — شريطٌ أو هلالٌ أبيضُ مصمتٌ داخلَ الحدّ |
| **العمق الأرضيّ** | **لا خطَّ أرضٍ ولا ظلَّ مُلقًى** — الأجسامُ طافيةٌ (كما في كلِّ أصولِ `images/rocket/`). الحجمُ يأتي من تدرّجِ اللونِ واللمعةِ لا من الظلِّ الأرضيّ |
| **الوجوه** | **بلا ملامح وجه إطلاقاً — لا على الكائنات الحية ولا على الجمادات.** (المرجعُ لا يخالفُ هذا: قناعُ رائدِ الفضاءِ معتمٌ بلمعتَين، بلا عينٍ أو فمٍ مرسوم) |
| **الخلفية** | بيضاء نقيّة `#FFFFFF` في الصور المولّدة خارجياً؛ وشفافةٌ (لون الملعب) في SVG |

**سلالم الألوان المعتمدة** (ثمانيةٌ **مقيسةٌ** من `images/rocket/`، واثنانِ **مشتقّانِ مثبَّتان** —
يُؤخذُ **السُّلَّمُ كاملاً** لا درجةٌ منه):

| العائلة | فاتح | أساسي | غامق | ظِلّ | المصدر |
|--------|------|-------|------|-----|----------------|
| برتقالي | `#FF8000` | `#FF6000` | `#E04000` | `#C04000` | `rocket-body.png` |
| سماويّ | `#40C0FF` | `#20A0FF` | `#2080E0` | `#1060A0` | `satellite.png` |
| أزرق محيطيّ | `#00A0E0` | `#0080C0` | `#0060A0` | `#004880` | `earth-pad.png` |
| أخضر | `#80C020` | `#60C020` | `#4A9018` | `#356810` | `earth-pad.png` |
| أصفر قمريّ | `#FFFFC0` | `#E0E0A0` | `#E0C080` | `#E0C060` | `moon.png` |
| أحمر | `#FF4020` | `#FF2020` | `#E02000` | `#B01800` | `flag.png` |
| كهرمانيّ | `#FFA000` | `#E08000` | `#C06000` | `#984800` | `satellite.png` |
| معدنيّ رماديّ | `#C0C0C0` | `#808080` | `#606060` | `#404040` | `rocket-body.png` (الفوهة) |
| **بشرة** | `#F2C3A0` | `#E0A87F` | `#C88A5E` | `#AA7550` | **مشتقّ** — مثبَّت بقرار المالك ٢٠٢٦-٠٨-٠١ |
| **تربة** | `#B98551` | `#9A6636` | `#7A4C22` | `#68411D` | **مشتقّ** — مثبَّت بقرار المالك ٢٠٢٦-٠٨-٠١ |

> الدرجاتُ المؤشَّرةُ بـ«ظِلّ» **مشتقّةٌ حسابياً** (خفضُ إضاءةٍ ≈١٥٪ عن «الغامق») حيث لم يبلغْها القياسُ في الأصلِ المصدر؛ وبقيّةُ القيمِ **مقيسةٌ مباشرة**.
> **الأبيضُ والرماديُّ** ليسا لوناً في اللوحة بل **جسمُ الصاروخِ نفسُه**: `#FFFFFF` ← `#F9F8F3` ← `#F7F6F1` (مقيسٌ من `rocket-body.png`).
>
> **سُلَّما البشرةِ والتربةِ مشتقّانِ لا مقيسان — ومثبَّتانِ بقرارِ المالك ٢٠٢٦-٠٨-٠١.** لا مقابلَ لهما في
> `images/rocket/` أصلاً: رائدُ الفضاءِ **مُغطًّى بالكامل** فلا بشرةَ في المرجع، وليس في أصولِ الواجهةِ
> تربةٌ. فاشتُقَّ السُّلَّمانِ من الاستعمالِ القائمِ في `js/questions.js` (البشرةُ في يدِ درسِ الأدوية
> وبصماتِ `g3s-3-4`، والتربةُ في دروسِ التربةِ بالصفِّ الثاني)، ودرجةُ «الظِّلّ» في كلٍّ منهما محسوبةٌ
> بقاعدةِ الـ≈١٥٪ أعلاه. **وهما الآنَ ملزمانِ كبقيّةِ السلالم**: لا يُخترَعُ لونُ بشرةٍ أو تربةٍ جديدٌ
> في أيِّ رسمٍ أو برومبتٍ، بل يُؤخَذُ السُّلَّمُ كاملاً من هنا.
> **وإن أُضيفَ لاحقاً أصلُ واجهةٍ فيه بشرةٌ مكشوفةٌ أو تربةٌ، فالقياسُ منه يَنسخُ الاشتقاقَ** — القياسُ
> يعلو على الاشتقاق، كما أنّ قرارَ المالكِ يعلو على القياسِ في سماكةِ الحدّ.

**ظلُّ التماسّ:** الأجسامُ طافيةٌ بلا ظلٍّ مُلقًى، **إلا ظلَّ التماسِّ** بين جسمَين متلامسَين في المشهدِ الواحد (كومةُ أقراصٍ مستقرّةٌ في كفّ) — فهو يوضّحُ العلاقةَ المكانيةَ لا يُلقي الجسمَ على أرض.

**القيمُ أعلاه مقيسةٌ برمجياً** من `images/rocket/*.png` بفحصِ البكسل (`System.Drawing`)، لا مقروءةً بالعين. لإعادةِ القياسِ بعدَ أيِّ تحديثٍ للأصول: هستوغرام مكمَّمٌ إلى مضاعفاتِ ٣٢ مع إسقاطِ البكسلِ منخفضِ التشبّع (`max-min < 60`)، وأغمقُ بكسلٍ يُعطي لونَ الحدّ.

**مسار الرسومات التفصيلية التفاعلية** (توليد صورة مسطّحة خارجياً ثم تتبّعها بـ`vtracer` إلى SVG تفاعليّ بمناطق مسمّاة) موطنه مهارة **`shoogp-graphics`** (`.claude/skills/shoogp-graphics/SKILL.md`)؛ استخدمها عند الحاجة إلى رسم تفصيليّ بمناطق تفاعلية، ولا تُكرَّر قواعدها هنا.

**قاعدة الرسم بلا ملامح وجه (إلزامية — عُمِّمت بقرار المالك ٢٠٢٦-٠٨-٠١):**
في التأليف القادم: **لا ملامح وجه على أيِّ شيء إطلاقاً** — لا على الكائنات الحية
(الإنسان والحيوان) **ولا على الجمادات والأدوات** (دورق، مسطرة، زجاجة، كرة أرضية...):
بلا عيون ولا أنف ولا فم ولا خدود ورديّة، في رسومات SVG وفي برومبتات توليد الصور
الخارجية على السواء. سببُ التعميم: أسلوب «تشخيص الأدوات» الشائع في مكتبات الرسوم
يجعل دورقاً يبتسم بجانب إنسانٍ بلا وجه — تناقضٌ بصريّ يلاحظه التلميذ قبل المعلّم.
استثناء وحيد: إذا كانت الملامح مقصودة لذاتها في السؤال — كأسئلة الحواس وأعضاء الوجه —
تُرسم الأعضاء التي يتطلبها السؤال فقط. القاعدة لا تسري بأثر رجعي على الأسئلة المؤلفة قبلها.

**قاعدة خطوط التوصيل في السحب والإفلات (إلزامية — مرجعها `DESIGN_RULES.md`):**
- **السلوك الافتراضي لكل المواد — ولمناطق الاستقبال المنفصلة (النوع أ) حتى في الرياضيات:** يُرسم الخط الواصل (`.dndline` داخل `svg.dndlines`) والنقطة (`.dnd-dot`) بين منطقة الاستقبال وموضعها على الرسم.
- **استثناء مادة الرياضيات — للخانات الفارغة داخل الرسم فقط (النوع ب)** (الصنف `subj-math` + خانة بحدّ متقطّع داخل `svg` السؤال تقع نقطة الهدف داخلها): لا خط ولا نقطة — **لا تُنشأ عناصرها أصلاً في DOM** — والسحب مباشرة إلى الخانة الفارغة في مكانها بين الأرقام (طبقة `.target-cell` شفافة عليها)، لأن موضع الخانة صحيح وثابت فلا يحتاج خطاً يوجّه إليه.
- الشرط **مركزيّ** في الدالة المشتركة `renderDragDrop` (`js/app.js`) فترثه تلقائياً كل أسئلة النوع الحالية والمستقبلية دون تعديل بياناتها. التغذية الراجعة عند التحقق (تلوين الصناديق) واحدة في كل المواد.
- **النص المرجعي الكامل للقاعدة في `DESIGN_RULES.md`** (§أسئلة السحب والإفلات — خطوط التوصيل)؛ عند أي تعارض يُطابَق الكود على ما هو موثّق هناك.

**قاعدة تخطيط أسئلة السحب والإفلات ذات نقاط الارتساء (إلزامية — طبقة النوع المشتركة):**
- تخطيط السؤال يتبع **نسبة الصورة** (عرض ÷ ارتفاع): الصورة **العرضية** (النسبة ≥ **1.2**) ← البطاقات صفٌّ أفقيّ في الأعلى والصورة بالعرض الكامل أسفلها، فلا يقتطع العمودُ الجانبيُّ عرضَها فتظهر صغيرة؛ الصورة **الطولية أو المربّعة** (النسبة < **1.2**) ← العمود الجانبي كما هو.
- **احتياط:** إن التفّ صفُّ البطاقات إلى أكثر من سطرين على أضيق منفذ، يعود التخطيط تلقائياً إلى العمود الجانبي.
- المنطق في `renderDragDrop` بطبقة النوع المشتركة (`js/app.js`) — يعمل مع كل أسئلة السحب والإفلات دون لمس بياناتها؛ وإعادةُ اختيار الإطار القمريّ بعد تغيّر التخطيط تتكفّل بها شبكةُ أمان الأبعاد (ResizeObserver) في `js/shoogp-ui.js`.

## قواعد الواجهة (تصميم، أزرار، حاوية السؤال، البطاقات)
موطن قواعد الواجهة — **التقسيم صورة/CSS**، **العناصر المصوّرة**، **الحاوية القمرية** ونسبتها المقدّسة، **البطاقات وحالاتها**، **قاعدة التنفّس البصري** والحدود الدنيا — هو مهارة **`shoogp-ui`** (`.claude/skills/shoogp-ui/SKILL.md`)؛ استخدمها عند أي عمل على الواجهة، ولا تُكرَّر قواعدها هنا.

## مسارات المصادر وتسمية ملفّاتها (إلزامية)
لكل مادة مجلّد في: `المصادر\الفصل الدراسي X\الصف Y\مادة Z\`.
عند ذكر المسار استخدم علامات اقتباس بسبب المسافات.

**مبدأ التسمية (قرار المالك ٢٠٢٦-٠٨-٠١): الاسم يقول الدَّور، والمجلّد يقول المادة والصف.**
فلا تُصدَّر الملفّات باسم المادة أو الصف (`الرياضيات-الثاني-الكتاب.pdf` ✗) — الأسماء **مجرّدة
وموحّدة في كل المواد**، فلا يتعارض اسمُ ملفٍّ مع مجلّده إن نُقل، ولا يتغيّر ما أبحث عنه من مادة لمادة.

| الملف | الدور | إلزاميّ؟ |
|------|------|---------|
| **`كتاب-التلميذ.pdf`** | **مصدر المحتوى للتأليف**، و**صفحته الأولى هي الغلاف** الذي يُستخرج منه لون البطاقة | ✅ دائماً |
| **`دليل-المعلم.pdf`** | المرجع التدريسيّ: **الفهرسة والأهداف المرمّزة** | ✅ في الرياضيات · اختياريّ في غيرها |
| `الفهرس.pdf` | **تخزينٌ فقط — لا يُستنبط منه** (انظر التحذير أدناه) | اختياريّ |
| `ملخص-الفهرس.pdf` | **تخزينٌ فقط — لا يُستنبط منه** | اختياريّ |
| **`الأهداف.md`** | **الأهداف المستخرَجة** بالرموز — وهي **المرجع في جلسات التأليف** | ✅ يُنشأ عند أول تأليف |
| `برومبتات-الرسومات.docx` | يُضاف تلقائياً عند إنشاء البطاقة | يُنشأ تلقائياً |

> ⛔ **`الفهرس.pdf` و`ملخص-الفهرس.pdf` ملفّا تخزينٍ عند المعلّم لا مصدرَي فهرسة.**
> **لا تُبنى منهما تبويباتُ الدروس إطلاقاً**، ولا يُحتجُّ بهما في خلاف. مصدرُ الفهرسة **حصراً**:
> **`دليل-المعلم.pdf` في الرياضيات** (§بنية التنقل والتبويبات)، و**`كتاب-التلميذ.pdf` في بقية المواد**.
> فإن خالفَ ما في `الفهرس.pdf` ما في مصدرِه، **فالمصدرُ هو الحاكمُ بلا نقاش** — لأنّ القصاصةَ
> قد تكونُ من طبعةٍ أقدمَ أو من الكتابِ الخطأِ أصلاً، ولا سبيلَ للتحقّق من ذلك من الملفِّ نفسِه.
> وإن غابَ المصدرُ ولم يكن في المجلّدِ إلا القصاصة، **توقّفْ واطلبِ المصدرَ** ولا تفهرسْ منها.

**لماذا كتابان في الرياضيات، ومن يحكم ماذا:** الرياضيات وحدَها لها مصدرانِ لأنّ أدوارَهما مقسومة —
**الفهرسةُ والأهدافُ من `دليل-المعلم.pdf`** (فهو المرجع التدريسيّ الفعليّ للمعلّمين في السلطنة،
§بنية التنقل والتبويبات)، بينما **البطاقةُ والغلافُ ولونُه من `كتاب-التلميذ.pdf` حصراً**
(§إنشاء بطاقات الكتب)، **والمحتوى من الاثنين**: أسئلةُ النشاط الأساسيّ الواحد تغطّي صفحاتِ
كتاب التلميذ التابعةَ له، وأهدافُه مأخوذةٌ من الدليل. **وعند التعارض في الفهرسة فالدليل هو الحاكم.**
> ⚠️ **المصيدة التي تمنعُها التسميةُ نفسُها:** حين كان الملفّان في مجلّدٍ واحدٍ وأحدُهما اسمُه
> «الكتاب» المجرّد، كان من السهل فتحُ **غلافِ الدليل** لاستخراج لون البطاقة — وهو خطأٌ صريح.
> الاسمانِ الآن يحملانِ القاعدةَ: البطاقةُ من `كتاب-التلميذ.pdf` لا من `دليل-المعلم.pdf` أبداً.

**إن لم يتوفّر `كتاب-التلميذ.pdf` توقّف واطلبه** ولا تستعض عنه بالدليل لبناء البطاقة.
**ملفات PDF محلية على جهاز المعلم ولا تُرفع** (مستثناة في `.gitignore`)، **و`الأهداف.md` يُرفع** وهو المرجع.
> ⚠️ **ومنذ ٢٠٢٦-٠٨-١١ صارَ `المصادر/` مستبعَداً بكامله** لا الـPDF وحدَها، **فأيُّ ملفِّ أهدافٍ جديدٍ
> لن يُتتبَّعَ إلا بـ`git add -f` صراحةً** — والملفّاتُ المرفوعةُ سلفاً باقيةٌ تُحدَّثُ طبيعياً.
> **والخطرُ أنّ الإخفاقَ صامت:** بلا `-f` لا يظهرُ الملفُّ في `git status` أصلاً، فلا شيءَ يُنبّهُ
> إلى نسيانِه. فبعدَ إنشاءِ `الأهداف.md` لمادّةٍ أو صفٍّ جديدٍ **أضِفْه بـ`-f` وتحقّقْ من إدراجِه**.

مثال: "أسئلة علوم الصف الرابع الفصل الأول"
← اقرأ من: `"المصادر\الفصل الدراسي الاول\الصف الرابع\مادة العلوم\"`

### أدلة المعلم — ديني حياتي (الصفوف ١–٤)

- **التنظيم بالمسار كما في الرياضيات:** الاسم `دليل-المعلم.pdf` داخلَ مجلّدِ الصفِّ والفصل — `المصادر\الفصل الدراسي الاول\الصف <س>\مادة ديني حياتي\`.
- **النسخةُ المدرسيةُ المعتمدة.** ⚠️ **ولا سِجِلَّ لسنةِ الطبعةِ في المجلّدِ** (لا ملفَّ مرافقاً ولا وسماً) — فإن لزمَتِ السنةُ فهي في صفحةِ حقوقِ الدليلِ نفسِها (مثال: قرارٌ وزاريٌّ ‎315/2016‎ في دليلِ الصفِّ الأول).
- **⚠️ المتوفّرُ الآنَ: الفصلُ الدراسيُّ الأولُ وحدَه** — أربعةُ أدلّةٍ للصفوفِ ١–٤. **ومجلّدُ `الفصل الدراسي الثاني` فارغٌ تماماً** (صفرُ ملفّات)، فلا تُؤلَّفْ دروسُ الفصلِ الثاني حتى تصلَ أدلّتُه.
- **ما فيها ولا يوجد في كتاب التلميذ:** مخرجاتُ التعلّمِ بأفعالِها الإجرائيةِ لكلِّ درس، وجدولُ معاييرِ التعلّمِ للصفوفِ ١–٤، ونطاقاتُ الآياتِ بأرقامِها للصفَّينِ الثالثِ والرابع.
- **قاعدة: لا يُؤلَّفُ درسٌ في ديني حياتي إلا ودليلُ صفِّه متاح** — المخرَجُ في الدليلِ لا في كتابِ التلميذ، **بخلافِ الرياضيات**.

> ⚠️ **تنبيهٌ خاصٌّ بهذه الأدلّة: نصُّها غيرُ قابلٍ للاستخراجِ المباشر.** الخطوطُ مضمَّنةٌ بترميزٍ خاصٍّ بلا خريطةِ `ToUnicode` صالحة، فيُخرِجُ `pdftotext` حروفاً لاتينيةً مشوّشةً لا عربية (**مقيسٌ على دليلِ الصفِّ الأول: ٨ محارفَ عربيةٍ من ٧٠١٤** — و`áeó≤e` هي «مقدمة»). **والأرقامُ وحدَها تُستخرَجُ سليمة.** ولا يسري هذا على أدلّةِ المواد الأخرى.
>
> **والطريقةُ التي نجحَت — تصييرٌ إلى صورةٍ ثمّ قراءةٌ بصرية، لا تعرّفٌ ضوئيّ:**
> ```powershell
> # PowerShell + Windows.Data.Pdf: يُصيّر الصفحةَ صورةَ PNG بعرض 1700px
> Add-Type -AssemblyName System.Runtime.WindowsRuntime
> [Windows.Data.Pdf.PdfDocument, Windows.Data.Pdf, ContentType=WindowsRuntime]
> # (السكربتُ الكاملُ يُعادُ توليدُه عندَ الحاجة — LoadFromFileAsync ثمّ RenderToStreamAsync)
> ```
> ثمّ تُقرأُ الصورةُ الناتجةُ **قراءةً بصريةً مباشرة**. مقيسٌ: صفحةُ المحتوياتِ وجدولُ المعاييرِ خرَجا **كاملَي التشكيل**.
>
> **ولماذا هذا أسلمُ من التعرّفِ الضوئيِّ لا مجرّدَ بديلٍ عنه:** لا محرّكَ OCR مثبَّتٌ أصلاً على الجهاز (`tesseract` و`pytesseract` و`easyocr` كلُّها غائبة)، **والأهمُّ أنّ التعرّفَ الضوئيَّ طبقةُ نسخٍ تخطئُ في الشكل** — وحركةٌ واحدةٌ تُخطئُ في آيةٍ أو حديثٍ تُفسِدُ النصَّ الشريف. والتصييرُ **لا طبقةَ نسخٍ فيه**: تُقرأُ الصفحةُ كما طُبِعَت. فإن لزمَ استخراجٌ نصّيٌّ آليٌّ يوماً فليكنْ بعدَ مراجعةٍ بشريةٍ للشكل.

> ⚠️ **مجلّدٌ مزدوجٌ للمادّةِ في الصفِّ الرابعِ يحتاجُ توحيداً:** `مادة التربية الإسلامية\الأهداف.md` (متتبَّعٌ ومرفوع) بجانبَ `مادة ديني حياتي\` (فيه الدليلُ والكتابان). **فاسمُ المادّةِ الواحدةِ على مجلّدَين** — والأهدافُ في أحدِهما والمصادرُ في الآخر. يُوحَّدُ باسمِ `مادة ديني حياتي` عندَ أوّلِ تأليفٍ للمادّة.

## الكتب المقسّمة إلى جزأين (كتاب واحد لفصلٍ واحد)
**بعض كتب التلميذ تصدر في مجلَّدَين اثنَين لِلفصلِ الدراسيِّ الواحد** — ليس انقساماً في الملفّ
ولا فصلَينِ دراسيَّين. الغلافانِ يقولانِ ذلك صراحةً: «الجُزْءُ الأوَّل» و«الجُزْءُ الثّاني»،
وكِلاهما «الفَصْلُ الدِّراسيُّ الأوَّل».
> **مُتحقَّقٌ منه:** أغلفةُ «أحب لغتي» و«ديني حياتي» للصفِّ الرابع — الجزآنِ في كلٍّ منهما
> يحملانِ «الفَصْل الدِّراسيّ الأوَّل». **فالجزآنِ يسكنانِ مجلّدَ الفصلِ نفسِه**، ولا يُوضَع
> «الجزء الثاني» في مجلّدِ الفصلِ الدراسيِّ الثاني ظنّاً أنّه فصلٌ آخر.

- سمّ الملفّين: **`كتاب-التلميذ-ج1.pdf`** و**`كتاب-التلميذ-ج2.pdf`** (ولو انقسم الدليل: `دليل-المعلم-ج1.pdf` ونظيره).
- **الدروس متسلسلة عبر الجزأين** (تكمل ترقيمها من ج1 إلى ج2 دون انقطاع)، فهو **كتابٌ واحدٌ تربوياً**.
- عند التأليف: الدروس الأولى في ج1، والتالية في ج2 — استعن بالفهرس لمعرفة موضع كل درس.
- إن لم تجد درساً في جزء، أكمل البحث في الجزء الآخر.
- **ملف برومبتات رسومات واحد** للجزأين في مجلّد المصادر.

**العرض في المنصّة: بطاقةٌ لكلِّ جزءٍ لا بطاقةٌ للكتاب (قرار المالك ٢٠٢٦-٠٨-٠١).**
الكتابُ واحدٌ **في المصدرِ والتسلسلِ التربويّ**، لكنّه في الواجهة **مدخلانِ منفصلان**:

| البند | القاعدة |
|------|---------|
| المفتاح | `<صف>-<مادة>-1` و`<صف>-<مادة>-2` — مثل `g4-arabic-1` و`g4-arabic-2` |
| البطاقة واللون | **لكلِّ جزءٍ بطاقتُه، ولونُها من غلافِ ذلك الجزءِ نفسِه** لا من ج١ |
| عنوان الفهرس | «أحب لغتي (الجزء الأول) — الصف الرابع» |
| رمز الدروس | يحملُ رقمَ الجزء: `g4a1-…` لِج١ و`g4a2-…` لِج٢ |
| الوحدات | تتوزّعُ على الجزأين بلا تكرارٍ وتُكمِلُ ترقيمَها (المحور الأول في ج١، الثاني في ج٢) |

> **لماذا مدخلانِ لا واحد:** لأنّ الجزأينِ مجلَّدانِ مطبوعانِ مستقلّانِ في يدِ التلميذ، لكلٍّ
> غلافُه وترقيمُ صفحاتِه (يستأنفُ من ١ — انظر الفخَّ أدناه). فالمدخلانِ يطابقانِ ما يمسكُه
> التلميذُ فعلاً، ويجعلانِ «ج٢ ص١٩» قابلاً للتتبّعِ من الواجهةِ إلى الورق.

**فخّان مقيسان في الكتب ذات الجزأين — يُتنبَّه لهما وجوباً:**

**١) لكلِّ جزءٍ غلافٌ بلونٍ مختلف — فبطاقةُ كلِّ جزءٍ من غلافِ جزئِه هو.**
غلافُ «أحب لغتي ٤» **فيروزيٌّ في ج١ ومشمشيٌّ في ج٢**، ولذلك بطاقتاه تركوازيةٌ وبرتقالية.
**ولا يُقرأُ غلافُ ج١ لبطاقةِ ج٢ ولا العكس** — الخطأُ هنا لا يُكتشَفُ إلا بعدَ بناءِ البطاقةِ
كلِّها، لأنّ اللونَ يبدو سليماً في نفسِه.
> **وحيث يتشابهُ الغلافانِ** (كما في الصفوف ١–٣ من «أحب لغتي» و«ديني حياتي»: الجزآنِ بغلافٍ
> واحدٍ لا يفترقانِ إلا بشارةِ الجزء) **يُؤخَذُ اللونُ نفسُه بدرجتَينِ: الأفتحُ لِج١ والأغمقُ لِج٢** —
> فيبقى الصدقُ للمصدرِ وتبقى البطاقتانِ متمايزتَينِ في الشبكة. ونصُّ «الجزء الأول/الثاني» على
> البطاقةِ هو الفارقُ الحاسمُ في الحالَين.

**٢) ترقيمُ الصفحاتِ يبدأُ من جديدٍ في كلِّ جزء — فرقمُ الصفحةِ بلا اسمِ الجزءِ لا معنى له.**
الجزءُ الثاني **لا يُكمِلُ** ترقيمَ الأوّل. مقيسٌ في «أحب لغتي ٤»: صفحةُ PDF ‏٨٥ في **ج١**
تحملُ رقمَ **٨٣**، وصفحةُ PDF ‏٢١ في **ج٢** تحملُ رقمَ **١٩** — أي أنّ ج٢ استأنفَ من ١
(وإزاحةُ كلٍّ منهما **صفحةُ الكتاب = صفحةُ PDF − ٢**، وتُقاسُ لكلِّ جزءٍ على حدة).
فاكتبْ دائماً «ج٢ ص١٩» لا «ص١٩»، وإلا أشارَ المرجعُ إلى صفحتَينِ مختلفتَين في كتابٍ واحد.
**طريقةُ القياس:** افتحْ صفحةَ PDF معلومةً في وسطِ الجزءِ واقرأْ رقمَها المطبوعَ أسفلَ الصفحة،
والفرقُ بينهما هو الإزاحة — تُعادُ لكلِّ جزءٍ ولا تُفترَضُ واحدة.

## منهجية العمل (مهم — المستخدم مبتدئ)
- اشرح كل خطوة قبل تنفيذها.
- نفّذ على مراحل صغيرة، واختبر بعد كل مرحلة.
- عند تأليف الأسئلة: اعرض المحتوى والأهداف أولاً للمراجعة، ثم الأسئلة كنص، ثم أضفها بعد الموافقة.
- احتفظ بالبنية العامة قابلة لإعادة الاستخدام (اكتب المنطق مرة، يعمل مع كل البيانات).
